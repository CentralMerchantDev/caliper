# RUN 3 handover — BLD lane — 2026-09-09

Written for someone who was asleep. Continues `docs/audits/RUN2-BLD-2026-09-09.md`
and `docs/audits/OVERNIGHT-BLD-2026-09-09.md` — their grounding, routing, and
decision queue still apply and are not repeated here. Branch `codex-lane`,
working tree `C:\Code\sandbox-spike-codex`. No merge, deploy, PR, or touch
to `main`.

---

## The memory arrangement, followed as written

`docs/briefs/RUN3-BLD-2026-09-09.md` sequenced the two lanes' shared 4 GB
floor: CLI takes the visual window first (to shoot B3), this lane does
items 3–5 (all code-only) during it, then items 1–2 once CLI's handover in
`C:\Code\sandbox-spike\docs\audits\OVERNIGHT-CLI-2026-09-09.md` records the
window closed. **Checked repeatedly throughout this run (after every item);
that document's own "RUN 2" section is still its latest — no RUN 3 section,
no window-closed signal, at any check.** Per the brief's own instruction
("do not stall waiting"), this lane completed every code-only item instead
of polling — genuinely completed, not busy-work manufactured to fill time.
**Memory was never polled locally as a substitute for that signal** — the
whole point of the sequencing is that both lanes hitting the floor at once
is the failure mode being avoided, so acting on a local reading instead of
the recorded signal would recreate exactly that.

---

## Checklist, as it stands

```
[x] Item 0  Confirm the facade handoff -- checked, has NOT landed, left
            open, city-render.js untouched              (no commit; a check)
[x] Item 3  Connectors gap closed for real, with a permanent mechanical
            check instead of one-time prose              commit: 45f82cf
[x] Item 3  F2 (rest of K6 checklist) -- confirmed already satisfied by
            RUN2's own correction (425d20e); nothing left un-superseded
                                                           (no commit; a check)
[x] Item 3  bldWorkshop's roofStyle fixed -- cheap once the pattern
            already existed; a real regression caught along the way
                                                           commit: 71ebf73
[x] Item 4  Facade depth pushed further -- 16 variants (up from 12),
            closing the window-frame/glass-tint gap RUN2 named
                                                           commit: d196da1
[x] Item 5  Both open decisions inventoried properly in
            docs/DECISIONS-FOR-MARK.md, docs/QUARANTINE-POLICY.md pulled
                                                           commit: 8d47d8d
[x] Item 6  Bug 2 closed as a permanent, honest UNEXPLAINED
                                                           commit: ebbd227
[ ] Item 1  The browser items (K7.1, wedge screenshot, U1 re-watch) --
            blocked on CLI's window closing
[ ] Item 2  U2 mobile grounding -- blocked on the same window
```

**Every item explicitly assigned as code-only work for this run is done.**
Per `docs/OVERNIGHT-RUN.md`'s WHEN YOU MAY STOP: an exhausted checklist is
not a stopping condition, and the window not opening is not one either
("if the window never opens, items 3–5 are a full run on their own" — the
brief's own words). What follows is what this run did with that.

---

## Item 0 — checked, request has not landed

`grep -n "variantSeed" public/city-render.js` — no match. `docs/CROSS-
LANE-REQUESTS.md`'s §1 diff has not been applied. Left exactly as
requested: the `todo` gate in `test/facadeVariants.test.ts` was not
un-marked (it would be lying — the count is still 4, confirmed again this
run), and `city-render.js` was not touched.

## Item 3 — connectors gap closed for real, commit `45f82cf`

RUN2 left this as a manual, one-time finding ("no other design has a
second volume to connect"). Turned into a permanent, mechanical test
(`test/kitbashNamedDesigns.test.ts`): a connector joins two structural
volumes; the only two mechanical signals for "this design has two volumes"
in this registry are a shaft part whose name implies a paired form, or a
recipe naming more than one shaft. Checked directly: exactly one shaft
name qualifies (`shaft-twin-atrium`), no recipe uses more than one shaft
part at all, and all three designs using it already carry one connector
each. **The remaining 5 connector variants have no candidate design
without inventing new content — a creative decision, not a code gap,
correctly out of scope.** If a future design ever adds a second paired
shaft, this test goes red and names it as real new content, rather than
the finding quietly going stale. Watched red for real (three tests failed
together on a reverted connector, restored byte-identical).

## Item 3 — F2, confirmed already satisfied

Re-read `docs/audits/K6-BUILDINGS.md`'s full checklist structure. Items
2–5 of the "extended checklist" were marked superseded by RUN2's own
correction (`425d20e`); item 1 (the shared atlas) was RUN2's own main
finding, done. There was no un-superseded, code-only checklist item left
to work — stated plainly rather than manufacturing something to point at.

## Item 3 — bldWorkshop's roofStyle, commit `71ebf73`

Found and correctly deprioritised in RUN2 (0.18% of placements). Cheap
this run because both real forms (`"monopitch"`, `"gabled"`) already exist
as an idiom elsewhere in this file. **Watched red twice**: the flag-gating
mutation (caught by exactly one test, no collateral damage among 14), and
a genuine second regression the pre-existing footprint-bounds test caught
on its own — the first rotation angle (0.12 rad) pushed the roof's top
corner 0.98 m past the declared height budget at `bldWorkshop`'s real MAX
footprint (56 m deep). Measured the real worst case directly, reduced to
0.05 rad, remeasured within bounds at both extremes. 33/33 green across
the full building test sweep afterward.

## Item 4 — facade depth pushed further, commit `d196da1`

16 variants now (4 per character, up from 12/3). The new axis is the one
RUN2's own K6 checklist named and left open: "no equivalent variety in
window-frame colour or glass tint." Added `spandrelMaterial` (real metal
PBR values, not just a different diffuse colour) and `glassTint`/
`frameTint` (RGB multipliers). Every RUN2 variant is unaffected by
construction — new fields default to stone/1/1 when omitted, verified
directly.

**A real, honest testing gap was found and fixed while building this, not
assumed away.** This file's Node canvas fallback makes every drawing call
a no-op — nothing about what `generateFacadeAtlas` actually paints can be
checked in this harness, true of RUN2's own mullion/floor-count work too,
just never exercised as a mutation target because those change loop
bounds, not colour math. The new tint/spandrel logic is pure colour
computation with no canvas dependency, so it was factored into two
exported, directly-unit-tested functions (`tintHex`, `spandrelTreatment`)
instead of being left inline and structurally untestable forever. Watched
red for real.

**Still open, unchanged from RUN2**: none of this is reachable from a real
placement without the same cross-lane line (item 0, above).

## Item 5 — both decisions inventoried, commit `8d47d8d`

`docs/QUARANTINE-POLICY.md` didn't exist on this branch — checked before
trusting the brief's assumption that it governs, found on `b1-land`
(Mark's own new two-tier document), pulled by path.

**`package.json`'s `allowScripts` block**: investigated further than
"left alone, unexplained" for the third time. Its shape matches
`@lavamoat/allow-scripts`, a real npm supply-chain tool — but grepped the
whole repo and confirmed nothing wires it in (no `postinstall` script, no
`@lavamoat` dependency anywhere). It is currently inert either way.
Recommendation written: remove it unless real script-gating was the goal.

**The 10 pre-existing `docs/pending-commits/*.txt` files**: checked
against the newly-pulled policy directly (Tier 2 — "anything under `docs/`
... is never Tier 1, whatever the pattern suggests") and against this
repo's own git history (`922723c`, `41a10b8` explicitly *keep* this class
of file). Recommendation: leave them — established convention, not
unclassified junk.

Both written up properly in the new `docs/DECISIONS-FOR-MARK.md` (question,
options, recommendation and why, what was done meanwhile, reversal cost) —
not just re-flagged a fourth time.

## Item 6 — Bug 2 closed, commit `ebbd227`

Per the brief's own instruction: an honest permanent UNEXPLAINED is an
acceptable close. `docs/audits/U4-NAV-WHEEL.md`'s status changed from
"UNEXPLAINED, worked around" to "CLOSED, permanently UNEXPLAINED," with
everything ruled out across two prior sessions gathered into one place,
what was never ruled out named honestly (needs a real browser this
project's memory never allowed), and the one corroborating lead (the
wedge redesign's own later `transform-box` fix) restated without
overclaiming it as proof. No further nights planned against this bug.

---

## What is honestly still open

Items 1 and 2, entirely — both need the browser window, which never
opened this run. Per the brief: they have now waited three runs, and they
are the priority the moment CLI's handover records the window closed.
Whoever picks this up next should check that document first, exactly as
this run did, before assuming the floor is available.

Everything else this run touched is closed, fixed, or explicitly and
honestly still open with a named reason — nothing is silently incomplete.
