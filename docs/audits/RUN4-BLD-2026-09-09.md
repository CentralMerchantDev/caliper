# RUN 4 handover — BLD lane — 2026-09-09

Written for someone who was asleep. Continues `docs/audits/RUN3-BLD-2026-09-09.md`
— its grounding, routing, and decision queue still apply and are not repeated
here. Branch `codex-lane`, working tree `C:\Code\sandbox-spike-codex`. No
merge, deploy, PR, or touch to `main`.

---

## The change in the memory arrangement, followed as written

RUN3 sequenced CLI first, then this lane, on the shared 4 GB floor. CLI did
not use its window. RUN4's brief changed the arrangement: **the visual
window is now unconditionally this lane's**, no signal to wait for, no
handover to check for permission. This run followed that instruction
exactly — `C:\Code\sandbox-spike\docs\audits\OVERNIGHT-CLI-2026-09-09.md`
was **not** checked at any point this run, on purpose, because doing so
would have been exactly the waiting-for-permission behaviour the brief
explicitly ended.

**What was checked instead, repeatedly**: free memory, directly
(`Get-CimInstance Win32_OperatingSystem`). Readings across this run: 3.55,
2.94, 2.83, 2.48, 2.57 GB — below the 4 GB floor at every check, never
clearing it. `Get-Process` (read-only) showed three concurrent `claude`
processes and multiple `Antigravity IDE` processes at the last check,
consistent with other lanes genuinely active and sharing this pressure —
not this lane's own leak. No process was killed or considered for killing.

**Consequence, stated plainly**: items 1 and 2 (the browser work) were not
reachable this run — not because of any waiting-for-signal behaviour (that
was correctly retired per the brief), but because the floor itself did not
clear at any point this run's memory was checked. This is a real,
different constraint than RUN2/RUN3's "waiting on CLI" — there is no
signal to wait for any more, and it still did not open.

---

## Checklist, as it stands

```
[x] Item 0  Facade handoff checked again -- still has NOT landed (three
            runs now); process finding recorded below       (a check)
[ ] Item 1  Browser items (U2 first, then K7.1, wedge screenshot, U1
            re-watch) -- floor never cleared this run
[ ] Item 2  Look at the facades -- blocked on item 0 landing AND the floor
            clearing; neither happened
[x] Item 3  Bounds-defect survey: RUN3's rotation-overshoot shape checked
            against the OTHER typologies; a real, permanent coverage gap
            found and closed for 4 previously-unchecked typologies
                                                          commit: 736b983
[x] Item 3  Real storey-height variation (groundFloorMult), applied to one
            existing variant per character, not a new 17th
                                                          commit: 5cc72ac
[x] Item 3  F2 -- reconfirmed already satisfied (unchanged since RUN3)
                                                              (a check)
[x] Item 4  Process finding grounded and recorded, not swept for
                                                              (a check)
```

---

## Item 0 — checked again, still open, now a process finding

`grep -n "variantSeed" public/city-render.js` — no match, same as RUN3.
**This request has now been open for three runs** (filed RUN2, checked
RUN3, checked again here). Per the brief's own instruction, this is worth
recording as a finding about the cross-lane protocol itself, not just a
repeated pending-task note: a one-line, fully-specified, pre-verified diff
(`docs/CROSS-LANE-REQUESTS.md` §1, with the exact line, the reasoning, and
the test that will confirm it) has sat unactioned across three runs despite
being about as low-friction a cross-lane request as this protocol can
produce. Whatever is causing that — the request not being seen, competing
priorities on the other side, or the request channel itself not being
checked by whoever picks up CLI's own work — is worth someone's attention
independent of this specific one-line change. Not touched: `city-render.js`
remains untouched by this lane, and `test/facadeVariants.test.ts`'s `{ todo
}` gate stays marked, honestly, because the count is still 4.

## Item 3 — bounds-defect survey, commit `736b983`

Asked: does RUN3's rotation-overshoot defect shape (geometry escaping its
own declared bounds) recur elsewhere? Checked mechanically: all 7 real
`rotate*()` calls in `public/buildings.js` belong to the 8 typologies
`test/buildingExplicitSize.test.ts`'s existing MIN/MAX bounds check already
covers, and that test passes cleanly — no recurrence among those 8. But 4
real typologies (`bldTerrace`, `bldTownhouse`, `bldHighStreetTerrace`,
`bldBusinessParkBlock`) were never covered by *any* bounds check, because
the existing test is keyed on `cellW`/`cellD`, which they don't take.
Audited by hand first (48 checks, no violations), then built permanent
coverage. **Caught a real gap in the new test while writing it**: an
8-seed plain sweep of `bld-terrace` never once rolled `hasDormers=true`,
so a deliberately planted out-of-bounds mutation on the dormer passed
clean — fixed by forcing every optional flag explicitly rather than
hoping a seed rolls it. Watched red for real afterward.

## Item 3 — real storey-height variation, commit `5cc72ac`

`groundFloorMult` (new, default 1) gives the ground floor genuine extra
height instead of every floor sharing the atlas grid equally. Applied to
**one existing variant per character**, not a new fifth one — this run's
own item 2 names the real open question as whether the existing 16
variants read as variety at street distance at all, and a bigger number
doesn't answer that question, so building toward it while item 2 is still
blocked was deliberately avoided. Exported and directly unit-tested
(`floorLayout`), matching RUN3's established pattern for the parts of this
file a no-op canvas stub cannot verify by running it. Watched red for real.

## Item 3 — F2, reconfirmed

Re-read `docs/audits/K6-BUILDINGS.md`'s checklist structure; unchanged
since RUN3's confirmation. Still nothing un-superseded and code-only left
to work.

## Item 4 — process finding, grounded, not swept

Both findings were checked against this repository before being trusted,
per the brief's own instruction not to sweep for them:

1. **`docs/BUILD-LOOP.md`'s offending line is at line 158 in this branch's
   current copy, not line 165 as the brief states** — read directly
   before trusting the citation. The substance is confirmed correct
   ("Nothing is deleted... This applies to... git lock files," directly
   contradicting `docs/QUARANTINE-POLICY.md`'s Tier 1 git-lock
   purge-eligibility), just the line number has drifted since whatever
   produced it. Not fixed — this lane has no other reason to touch this
   file tonight, and the brief's own instruction is to fix it only when
   touched for another reason, not to sweep.
2. **`docs/briefs/RUN-DATUM-2026-09-09.md` does not exist in this
   repository at all** — checked directly (`test -f`, missing). The
   brief's phrasing ("in this repository that includes...") does not hold
   for this specific file; it is very likely a DATUM-project path cited
   from the corpus-wide reconciliation and carried into this brief without
   the "which repository" qualifier surviving the copy. Named so the next
   reader does not spend time looking for a file that is not here.
3. **`docs/AUDIT-PROTOCOL.md`'s "failure patterns A through F"** — noted
   for future reports rather than fixed now: if this lane cites a pattern
   letter in a report, it will say what is meant by it inline, since A, B,
   and C are not defined anywhere in this project's own copy of the
   document.

---

## What is honestly still open

Items 1 and 2, entirely — the floor never cleared across this run's
repeated checks. Per `docs/OVERNIGHT-RUN.md`'s WHEN YOU MAY STOP, this is
not itself a stopping condition, but there is also no further code-only
work queued that has not already been done or explicitly, honestly
deferred (item 2's own caution against a "seventeenth variant" applies
with real force now — the facade-variety axis has had real, tested depth
added twice this run already, and more of the same without the visual
check item 2 describes would be exactly the low-value padding that caution
warns against).

**What to do next, and why**: check free memory first, the same way this
run did (`Get-CimInstance Win32_OperatingSystem`) — no signal to wait for
any more. The moment it clears, item 1's own order stands: U2 first (three
runs unattempted, the item Mark raises most often), then K7.1, the wedge
screenshot, and U1's re-watch. If item 0 has landed by then, item 2 (the
facade street-level/mid-distance check) is worth doing before any further
facade-texture work of any kind — its answer redirects or validates
everything built in items 3/4 of the last three runs at once.
