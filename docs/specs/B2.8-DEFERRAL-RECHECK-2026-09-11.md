# B2.8's deferral, re-checked — 2026-09-11, b1-land, CLI, autonomous run 2

Item 1 of `docs/briefs/CLI-2026-09-11-autonomous-2.md`: for each of B2.8's own
"~30 need B4 first" old-world tests, a verdict — still blocked, or
re-pinnable now — with the specific import (or, where that's not what
decides it, the specific finding) that decides it. Read only; nothing built,
nothing re-pinned, per the brief's own instruction.

## The brief's own premise, checked first, and found not to hold as stated

The brief's own grounds for re-examining the deferral: *"B4 has since
moved: `ROAD_WIDTH` is retired, roads carry real classes, the board is
regenerated to 21,007 pieces."* Checked directly, against `ca413a0`'s own
commit message (the commit that wrote B2.8): the four files whose imports
decide the deferral are `public/city-plan.js`, `public/layout.js`,
`public/instance-groups.js`, `public/road-network.js` — B2.8's own words,
*"their real replacements are B4's job (\"kits wire by construction\")"*,
about wiring real BUILDING typologies/kits onto board pieces, not about
road width. `ROAD_WIDTH`'s retirement happened entirely inside
`public/board-generator.js` and `public/bridge-generator.js` — confirmed by
`grep`, none of the four files B2.8 names import either module, and
`public/road-network.js`'s own road-width source (`ROAD_STANDARDS`, from
`public/roadkit.js`) was already independent of `board-generator.js`'s own
retired constant before tonight, and remains so.

**So: `ROAD_WIDTH`'s retirement does not bear on whether ANY of these tests
can be re-pinned.** It is a real, separate, already-landed piece of work
(tonight's earlier session) that happens to also be about roads, but it
touched a different pipeline than the one these ~30 tests exercise. This is
`rule://escape-clause`, applied: the brief's specific reasoning is wrong,
named plainly, but not load-bearing enough to stop the run — the
per-test verdict work below is unaffected by which reason motivated
re-checking it, and was done regardless.

## Method

Every one of the 12 files was confirmed, by direct `grep` of its own
`import` lines, to import `public/city-plan.js` (or a module that imports it
transitively) — never `public/board-generator.js` or
`public/bridge-generator.js` — with one exception (`isolate.test.ts`, no
direct `public/` import at all, named separately below). Then every file was
actually run (`node test/run.mjs <file>`, not read by eye) to see its REAL,
current failure reason, not assumed from the test's own title.

## Verdict, by file (37 tests, 12 files)

### STILL BLOCKED — 36 of 37 tests, 11 of 12 files

| File | Tests | Deciding import | Real failure found by running it |
|---|---|---|---|
| `cityWorld.test.ts` | 20 | `public/city-plan.js` (direct) | Multiple, independent: `buildings stand on dry land` fails with **"71.8% of plots fall in the water"** — the exact figure already named in `COMPLETION-PLAN.md`'s own R4 line as the 2026-09-09 incident, a known, separate, pre-existing defect. `every building part has a finite, positive, sane size` fails with "only 35999 parts emitted -- the world did not build". Several terrain/water assertions fail too (`the lagoon is not water`, `sea bed is 20.2m deep at the beach`, `harbour: could not find both shores`) — not board-related at all. |
| `cityJoin.test.ts` | 3 | `public/city-plan.js` (direct) | "5 bridge ends meet no road (budget 0)"; "roads laid mostly over water" (2 named); "the world's own report must agree with an independent measurement -- 0 != 5". Old road/bridge network's own internal disagreement. |
| `worldOccupancy.test.ts` | 2 | `public/city-plan.js` (direct) | A `TypeError: Cannot read properties of undefined (reading 'axis')` — a real code error, not a threshold miss; and "expected roughly 1,380 road entries, got 419" -- a 3.3x drift from the road-network's own historical baseline. |
| `ground.test.ts` | 2 | `public/world-registry.js` (old-world) | "a car must be placeable on a carriageway: slope -- ground moves 5.70 m across the footprint, limit 5.00 m"; a second assertion expected `'occupied'`, got `'slope'` -- these are real terrain/occupancy disagreements in the old world's own ground model. |
| `connectivityBridges.test.ts` | 2 | `public/road-network.js`, `public/city-plan.js` | A STALE PIN: "expected 52 components (Step 4's own baseline), got 48 -- the world generator changed, or this pin is stale." The old world's own network has drifted since this pin was taken, for a reason unrelated to `board-generator.js`. |
| `cityConnectivity.test.ts` | 2 | `public/city-plan.js` (direct) | "15 settlement(s) cannot be driven to" (named); "the largest network is only 26% of all roads" -- the old road network's own connectivity has regressed since these gates were last green. |
| `roadNetwork.test.ts` | 1 | `public/road-network.js` (direct) | "1 of 2489 joins failed under real terrain: Socket mating failed: position mismatch (0.711m apart)" -- a real, small, precision bug in `road-network.js`'s own junction geometry, unrelated to `board-generator.js`'s road classes. |
| `planSeed.test.ts` | 1 | `public/city-plan.js` (direct) | "plot count moved -- the pin below is no longer describing this city: 45522 != 17586" -- a **2.6x drift** in the old pipeline's own plot count from its last measured baseline, for reasons this item did not trace (out of its own read-only scope) but plainly unrelated to `board-generator.js`. |
| `layout.test.ts` | 1 | `public/city-plan.js` (direct) | "only 14.5% of plots were placed" -- consistent with the same drift `planSeed.test.ts` shows; the old layout pipeline is placing far fewer buildings than its own historical baseline expects. |
| `isolate.test.ts` (the neighbour test only -- see exception below) | (0 of this file's 1 failure fits this bucket) | — | — |
| `instanceGroups.test.ts` | 1 | `public/city-plan.js`, `public/layout.js` | "only 6600 real placements -- too few to call this a scale test" -- same drift again. |
| `umaaFindings.test.ts` | 1 | `public/world-registry.js` (old-world) | "a parasol belongs on a beach: terrain -- open water carries nothing that stands on the ground... water at (-7999.0, -4400.0)" -- a real placement-vs-terrain disagreement in the old world model. |

**A pattern worth surfacing on its own, beyond the per-test verdicts asked
for:** `planSeed.test.ts`, `layout.test.ts`, and `instanceGroups.test.ts`
all point at the SAME underlying fact from three different angles -- the old
world's own plot/placement pipeline is producing dramatically different
output than its own historical pins (45,522 vs 17,586 plots; 14.5% vs a much
higher expected placement rate; 6,600 vs an expected larger placement
count). This is a real, live, currently-uninvestigated drift in
`city-plan.js`'s own generation, independent of anything B4 or
`board-generator.js` would fix -- named here because "still blocked on B4"
undersells it; the honest state is "blocked on B4, AND the thing it would
eventually replace has independently drifted further since B2.8 was
written."

### NOT BLOCKED BY B2.8'S OWN REASONING — 1 of 37 tests, in `isolate.test.ts`

`test/isolate.test.ts`'s own failing test -- *"P4.3: every reported
neighbour is a building -- roads and bridges from the same local query are
excluded"* -- is genuinely different from all 36 above. Read directly: this
test (and the six `P4.3 (wiring)` tests around it, all passing) call
`neighboursOf(plotId, this._boardPieces, ...)` against **real
`board-generator.js` data, already wired** (`world-render-3d.js`'s own
`_boardPieces`/`_boardData`, from B3's picking work earlier tonight) -- not
against `city-plan.js`'s old world at all. A board-side equivalent does not
merely "now exist" here; it was already in use before this session started.

The real, current failure is a TEST METHODOLOGY bug, not a missing
capability: *"the probe's own local query found no non-building piece near
the sample -- this test cannot prove the filter does anything; pick a
denser sample."* `docs/DECISIONS-FOR-MARK.md` #2's own characterization
("carries one already-catalogued old-world-pin failure") is now STALE for
this specific test -- it no longer describes what's actually failing here.

**Not re-pinned this run, per instruction** -- but named as a small,
independent fix (widen the probe's sample density) that is NOT gated on
item 2's own instancing work, B4, or anything else in this deferral at all.

## Summary

| Verdict | Count |
|---|---|
| Still blocked (no board-side equivalent; `city-plan.js`/`layout.js`/`instance-groups.js`/`road-network.js` import, own real failures found) | 36 of 37 |
| Not blocked by B2.8's own reasoning (already uses real board data; current failure is unrelated, a test-methodology bug) | 1 of 37 (`isolate.test.ts`) |

**B2.8's own deferral still holds, for 36 of 37 -- confirmed by running
every file, not assumed from the import chain alone.** The brief's own
stated reason for re-checking (`ROAD_WIDTH`'s retirement) does not actually
apply to any of the code these tests exercise; named per
`rule://escape-clause` rather than silently worked around. One test
(`isolate.test.ts`'s own neighbour-finding check) is genuinely not blocked
by B2.8's reasoning at all and was miscategorized by leaving it folded into
the same bucket as the other 36 -- exactly the failure mode this item's own
gate warned against, and the reason to check every one rather than infer
from the import chain alone.

Nothing was built or re-pinned. This is the list; acting on any of it
(fixing the old pipeline's own drift, widening `isolate.test.ts`'s probe,
or anything else) is separate, larger work Mark should see this list for
first, per the brief's own instruction.
