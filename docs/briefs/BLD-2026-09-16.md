# LANE BRIEF — BLD

**Terminal:** BLD · **Repository:** `C:\Code\sandbox-spike-codex` · **Branch:** `codex-lane`
**Mode:** autonomous, long run. Stopping point spans items.

Linked worktree — `.git` is a FILE. Commit messages to
`$env:TEMP\COMMIT_MSG_BLD.txt`.

**`docs/specs/PLAN.md` is the new governing plan**, replacing `REBUILD-PLAN.md`;
`docs/specs/CHECKLIST.md` replaces `REBUILD-CHECKLIST.md` and the registry points
at it. Both arrive from `main` — sync first. Read PLAN.md §0, §1, §3 and §5.

Policy is cited, never restated — `rule://lane-brief`.

---

## 0. START

Sync `main` into `codex-lane` first — the new plan and checklist are there, and
CLI is pushing FIX-2 early because your FIX-1 depends on it.

`process_at("session-start")`, then at every moment transition. Project id
`caliper-bld`. Generate this run's brief with the `lane_brief` prompt and commit
it.

## 1. THE ORDER, AND WHY FIX-1 IS NOT FIRST

**FIX-3, FIX-4, then FIX-1, CAT-2, CAT-3, CAT-4.**

FIX-1 needs a real storey count in the catalogue so a piece's height follows from
its own data rather than from a constant. That field is CLI's FIX-2, landing
early. **Do not wait for it** — FIX-3 and FIX-4 have no dependency at all. Check
for FIX-2 by name at each moment transition; `node scripts/query-event-log.mjs`
will show CLI's `pushed` event without guessing a ref.

If FIX-2 has not landed by the time FIX-4 is done, carry on to CAT-2 and take
FIX-1 when it appears. A lane that waits is stalled.

## 2. FIX-3 — MARK JUDGED THE FOG AND IT FAILED

`25-board-scene-pass.png`, in his words: hazier and more of a dust bowl than the
`14-board.png` it was meant to improve. That is the gate — *the pair, judged by
Mark* — and it came back no.

**Revert to 14's settings and retune from there.** Do not try to nudge 25 back.
`14` had readable ground texture, real shadows and a defined board edge; the wash
removed all three and the buildings are losing contrast against the background.

The problem RC4 was solving is real — a ground that ends at a visible edge. The
cure was worse than the disease. Solve it without erasing the ground.

## 3. FIX-4 — THE READOUT'S EVIDENCE IS EMPTY

`26-readout-real-numbers.png` shows a cursor marker and **no number anywhere in
frame.** The likely explanation is benign: an HTML overlay is not captured by
`toDataURL()`, which reads the WebGL canvas. That would make the number real and
the evidence worthless.

RC5's gate is *"the number changes as the cursor moves"*. That shot cannot
demonstrate it either way, and this is the same shape as the blank-canvas
render that reported `draw calls: 1` — which **you caught yourself**.

Produce evidence that shows the number changing across cells of genuinely
different value. If the original shot was incomplete, say so plainly rather than
quietly replacing it.

## 4. FIX-1 AND CAT-3 — THE RANGE IS THE GAME

Mark's ruling: **a mega-tower is 100+ storeys, and the range descends from
there.** Shanghai, New York, Hong Kong, Tokyo, Melbourne, Sydney, Toronto —
Toronto is the clearest reference because it holds the whole range in one view,
a 72-storey tower and three-storey semis a kilometre apart.

`mega-tower-a` currently renders at **26.88 m**, about eight storeys. The 6× cap
added during L12 to stop odd scaling is now the defect. §T7 has real dimensions,
§R9 the massing rules.

**Compressing the range would remove the thing scoring exists to reward.**

And his second ruling, which is CAT-3: **a city builder is about diversity in
design, shape, size and height — within the plot limits — as in real buildings in
a real city.** Two towers of the same footprint should not be the same tower.
**§R2's variation numbers are far lower than instinct suggests — read them before
deciding how many variants.** §C1.5 gives the real starting counts.

## 5. CAT-2 — kenney.nl IS AUTHORISED

A standing CC0 source, on the existing discipline: every import records URL,
licence and SHA-256 in `public/vendor/kits/LICENCES.md`; anything not CC0 stops
and asks. **You do not need to ask again per download.**

Four shapes you already verified — `road-crossroad`, `road-intersection`,
`road-end`, `road-end-round` — cover the lane/street/avenue/highway tiers through
existing scaling and unblock 26 of the 38 in one pass.

**Correct `street-cross` while you are there.** It binds `road-crossing.glb`,
which your own top-down renders proved is a straight road with a crosswalk, not a
four-way. The real one is `road-crossroad.glb`.

**Bind meshes; do not touch game data.** No new entries, no changed category,
footprint or adjacency — those are CLI's. An entry with no plausible match is a
**finding**, not a licence to force a mesh that does not fit its footprint class.

## 6. CAT-4 — THE CONTACT SHEET EARNED ITS KEEP

It is what made the scale problem visible, and Mark judged the kit **coherent**
across three CC0 packs — that question is answered and the answer is good.

Two things to fix: put it **on a grid** rather than a diagonal strip in a field
of black, and show the real height range so the scale is visible rather than
inferred.

Say which pieces look wrong beside the others. A wall of thumbnails presented as
a pass is the failure this exists to prevent.

## 7. ATTEMPT BUDGET AND THE STOPPING POINT

Attempt budget: **2 per item.**

**Do not ask between items.** Finish, record the gate, commit, push, take the
next. Keep going.

**Stop only when:** CAT-4 is committed; or budget is low with enough reserved for
the handover; or a genuine blocker `rule://decision-queue` cannot route around —
wanting Mark's opinion is a queue entry, never a stop; or the escape clause fires
on a load-bearing error.

Past CAT-4 with budget left: `rule://queue-exhaustion`, descend to the next
written item.

## 8. FILES

`public/board-renderer.js`, `public/pointer-interaction.js`,
`public/look-proof-*`, the overview page, `scripts/link-catalogue-meshes.mjs`,
`scripts/normalise-kit-textures.mjs`, `scripts/shoot-look-proof.mjs`, the vendor
kit directories, `docs/look-proof-shots/`.

**Not `public/scoring.js`, not the migration script, not the catalogue's game
fields.** CLI is live in those. Cross-lane needs go in
`docs/CROSS-LANE-REQUESTS.md`.

## 9. STANDING GUARDS

`rule://background-promotion-evidence` — a render is a long command and will be
promoted to background. **Exit 0 is not evidence a shot exists. Open the file and
look at it.**

`_TO-DELETE/` was deleted on Mark's explicit instruction with the purge recorded
in the ledger. Do not restore it. Files reappearing there is a finding.

CLI runs concurrently on one machine. A stall at 0% CPU may be contention — check
before concluding anything is broken, and kill nothing you did not spawn.
