# RUN 2 — CLI LANE — 2026-09-09

Continues `docs/briefs/OVERNIGHT-CLI-2026-09-09.md`. That brief's routing,
memory, discipline, handover and escape-clause sections **still apply
unchanged** — read them, do not re-derive them. This document replaces only its
§4 (ground) and §5 (the work).

**Read `docs/OVERNIGHT-RUN.md` first, including its new section WHEN YOU MAY
STOP.** The last run planned B2.7, blind-reviewed it, corrected it, committed
it, and stopped — forty-five minutes against a night, with nothing built. That
was a defect in the operating manual, now fixed. A finished plan is not a
stopping condition. **You implement what you plan, in the same run.**

---

## 1. WHERE

`C:\Code\sandbox-spike`, branch `b1-land`. Confirm before touching anything.

---

## 2. GROUND — check, then report, then keep moving

Last run's own commits are the baseline. Verify rather than trust:

- `adf3362` — `docs/MODULE-MAP.md` generated for this branch. **Read it before
  proposing to build anything.**
- `95cf588` — the B2.7 plan, blind-reviewed and corrected. Four defects were
  found and fixed in it, including a 9,000 m threshold where `roadkit.js`'s real
  buildable ceiling is 800 m. **This plan is your implementation spec.**
- `73f84b6` — mutation evidence now 109 of 119 CAUGHT, 10 named NEVER RUN.
- `76e8d61` — the 38 old-world pins categorized, not yet re-pinned.

Report what you find, then go straight to the work. Do not re-plan what is
already planned and reviewed.

---

## 3. THE WORK — in order, each one built, not just planned

### 0. B3 — THE RENDER PATH. FIRST, BEFORE ANYTHING ELSE.

**Reordered 2026-09-09 on Mark's challenge, and he was right to make it.**

Grep `public/` for `board-load`, `fetchBoard` or `loadBoard` and you will find
**zero references outside `board-load.js` itself**. `public/board.generated.json`
is 7.5 MB holding 35,365 real pieces on the real archipelago, and nothing opens
it. `city-render.js` still imports `planCity` from `layout.js` and builds the
city the way it always has.

`BOARD-REBUILD-PLAN.md` opens by naming nine capabilities built beside the
renderer and never drawn. **The board is the tenth** — produced by the plan
written to end that pattern. Every hour spent on correctness debt while this
holds adds to a pile nobody can see.

Build it. Draw board pieces and nothing else.

- The bootstrap decision B2.6 explicitly left open is yours: `createWorld()` is
  synchronous, `fetchBoard()` is inherently async. Decide it, write down why.
- *Gate:* fails if the render path reads anything but the board. Watch it red
  against today's `planCity` import before you trust it.
- *Exit condition:* `city-render.js` quarantined to `_TO-DELETE/`, because
  nothing reaches it any more.
- Partial credit counts here. A render path that draws roads and buildings from
  the board and nothing else yet is worth far more than another phase of
  invisible correctness. Commit it and keep going.

Everything below this line waits until the world can be seen.

### 1. B2.7 — implement the corrected plan at `95cf588`

The plan exists, has been blind-reviewed, and its defects are fixed. Build it.
Test first, watched red, then green, then mutate and prove CAUGHT, then commit.
Do not re-open the plan unless implementation proves it wrong — and if it does,
that finding goes in the handover.

### 2. The mutation-harness deadlock — implement the allowlist

`_mutcheck.mjs` refuses to run against a red baseline; B2.5's gate is honestly
red on purpose. Two correct controls disabling each other — Failure pattern F,
second instance. Of the two options queued for Mark, **take the allowlist**:
teach the harness that a named, documented honest-red test is an expected
baseline condition rather than a broken tree. It preserves the honest red *and*
unblocks the 10 controls, where converting B2.5's gate to `{todo}` would hide a
real measurement.

Record it in `docs/DECISIONS-FOR-MARK.md` as **decided provisionally, reversible
in one commit**, with the reasoning. Then run the 10 and get to 119 of 119, or
name honestly what still will not run.

### 3. B2.8 — re-pin the 38, now that B2.7 has landed

The categorization at `76e8d61` is the input. Its own sequencing rule said to
re-pin once B2's generator is coherent end to end — B2.7 is that point. Never
loosen an assertion to make it pass; a vacuous test is worse than a red one.

### 4. B3 — the render path

**The largest remaining item and the one that makes the world visible again.**
Draw board pieces. Nothing else. The gate fails if the render path reads
anything but the board.

`createWorld()` is synchronous today and `fetchBoard()` is inherently async —
B2.6 named that as a real design decision left for whoever builds this
bootstrap. That is you. Decide it, write down why, and build it.

### 5. B4 — the kits wire by construction

Roads from `roadkit`. Buildings from the kit. Trees from `propModel`. Props from
the manifest. Not integration — the only way a piece can exist. Gate: the
dead-export check, watched red, and it must list `propModel` today.

### 6. B6 — quarantine

`city-plan.js`, `city-render.js`, `layout.js`, and `board-adapter.js` once B3
and B4 are green. `_TO-DELETE/<reason>/`, never deleted. `board-adapter.js` goes
only after B2's pieces are what `world-render-3d.js` reads — not before.

### 7. B7 — farmland, the range, greenery

### 8. The published claim

The README and the live page say *"98 deliberate defects injected, all 98
caught."* That is now false: it is 109 of 119 with 10 named. If the
generated-claims gate does not already catch this, that gate has a hole — find
it, and fix the claim either way.

---

## 4. IF YOU REACH THE END OF THIS LIST

Say so in the handover, then keep going on the most valuable thing you can name
and justify. Running out of work is not a stopping condition. Re-read
`docs/OVERNIGHT-RUN.md`'s **WHEN YOU MAY STOP** before ending the run, and if
your reason is not on that list, you are not finished.
