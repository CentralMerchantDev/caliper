# RUN 4 — BLD LANE — 2026-09-09

Continues `docs/briefs/RUN3-BLD-2026-09-09.md`. Routing, discipline and handover
sections unchanged. Read `docs/OVERNIGHT-RUN.md`, including **WHEN YOU MAY
STOP**.

RUN 3 did what it was asked and did it well — the connectors finding turned into
a permanent self-checking test, the colour maths factored out of an
unverifiable canvas stub rather than left structurally untestable, and both open
decisions inventoried instead of re-flagged a fourth time. Three of five items
found something better than the item asked for. Keep doing that.

---

## THE VISUAL WINDOW IS YOURS NOW — take it first

RUN 3 sequenced the 4 GB floor: CLI first, then you. **CLI did not use it.** You
checked its handover repeatedly and correctly did not poll memory locally to
work around it — that was the right call and it is why the arrangement is being
changed rather than repeated.

**The window is now yours unconditionally.** Do not wait for a signal, do not
check CLI's handover for permission. Items 1 and 2 below have waited three runs
and they are the reason a link Mark sends is worth opening.

Check free memory, and if it is above the floor, go. If it is below, work items
3 and 4 and check again — but the window is not something you are queueing for
any more.

---

## ITEM 0 — the facade handoff

Check again whether `public/city-render.js` passes `variantSeed`. Mark was given
the command to apply it directly, so it may have landed outside either lane. If
it has: un-mark your `{ todo }` gate in `test/facadeVariants.test.ts`, confirm
the count clears the floor, and **say the real number** — 16 variants reachable
is a different claim from 16 variants built.

If it still has not landed, leave the request open and do not edit that file.
Note in your handover that it is now three runs old, because that is a process
finding about the cross-lane protocol and not just a pending task.

---

## ITEM 1 — the browser items

In order, each time-boxed:

- **U2, mobile grounding.** Promoted to first. The largest open interface item,
  unattempted across three runs, and the one Mark has raised most often. World
  fills the screen, landscape works, prompt box findable, touch gestures work —
  including the `touch-action` gap on `#world-canvas` that was found, reported
  and never fixed. *Gate: measured on a real viewport. Never a stylesheet
  substring.*
- **K7.1** — the atlas gap, code-verified and never visually verified.
- **The wedge-wheel screenshot with panels closed.** `#tour-toggle-btn` against
  the real renderer before any fallback. One clean frame, not a perfect one.
- **U1's re-watch** in `e2e/panelOverlap.spec.ts`.

---

## ITEM 2 — look at the facades

If item 0 landed, this is the first chance anyone has had to see whether 16
variants actually reads as variety on a real street, or whether the atlas
resolution and camera distance make it invisible. **That is a real risk and
finding it out is worth more than adding a seventeenth variant.**

Shoot a street-level frame and a mid-distance frame. Judge honestly. If the
variety does not read at the distance a visitor actually looks from, say so —
that finding redirects the whole F-series.

---

## ITEM 3 — code-only work, if the floor is down

- **F2** — the rest of the K6 checklist, in K6's own priority order, minus what
  `425d20e` marked superseded.
- Facade depth beyond colour: storey-height variation, spandrel and mullion
  treatment. **1980–2000 stays excluded by construction.**
- The regression you caught in RUN 3 (a rotation angle overshooting the declared
  height budget at a typology's real maximum footprint) — is that shape of
  defect present in the other typologies? You surveyed for the flag-gating
  pattern; this is a different one.

---

## ITEM 4 — a process finding worth writing down

A corpus-wide reconciliation ran today across CALIPER, SQ.FT. and DATUM. Two of
its findings apply directly to this lane and are worth acting on rather than
just knowing:

1. **The blanket "nothing is ever deleted" rule is still stated as a live
   instruction in 34 documents**, six of which explicitly contradict
   `docs/QUARANTINE-POLICY.md` by extending the flat rule to git lock files —
   the exact case the policy was written to reverse. In this repository that
   includes `docs/BUILD-LOOP.md:165` and
   `docs/briefs/RUN-DATUM-2026-09-09.md:126`. If you touch either file for any
   other reason, fix the line and cite the policy. Do not go on a sweep for it.
2. **`docs/AUDIT-PROTOCOL.md` claims failure patterns "A through F" and defines
   only D, E and F.** A, B and C are cited by name across four documents and
   defined nowhere in CALIPER. If you cite a pattern letter in a report, say
   what you mean by it rather than assuming the reader can look it up.

---

## IF YOU REACH THE END

Say so in the handover and keep going on the most valuable thing you can name.
Re-read **WHEN YOU MAY STOP** before ending. If your reason is not on that list,
you are not finished.
