# RUN 3 — BLD LANE — 2026-09-09

Continues `docs/briefs/RUN2-BLD-2026-09-09.md`. Its routing, discipline and
handover sections still apply unchanged.

Read `docs/OVERNIGHT-RUN.md` first, including **WHEN YOU MAY STOP**.

RUN 2 was a strong run — four items closed, and three of the four found
something better than what they were sent to fix. The connectors discovery in
particular (an entire part category with no executable path to any geometry) is
the kind of finding that justifies the whole method.

---

## THE MEMORY ARRANGEMENT — read this before planning your order

Both lanes have been blocked on the same 4 GB floor for two consecutive runs,
because both were competing for it simultaneously. That is now sequenced.

**The CLI lane takes the visual window FIRST**, early in its run, to shoot the
B3 render that has been deferred twice. **You do code-only work during that
window** — items 3, 4 and 5 below.

When CLI's handover records the window as closed, **the floor is yours**: take
items 1 and 2, which have been waiting two runs. Check
`C:\Code\sandbox-spike\docs\audits\OVERNIGHT-CLI-2026-09-09.md` for that
signal — reading that sibling worktree is fine, writing to it is not.

If the window never opens, items 3–5 are a full run on their own. **Do not
stall waiting**, and do not kill processes.

---

## ITEM 0 — confirm the facade handoff landed

`docs/CROSS-LANE-REQUESTS.md` §1 asked CLI for the one line that makes your 12
facade variants reachable. Check whether `public/city-render.js` now passes
`variantSeed`. If it does, un-mark your own `{ todo }` gate in
`test/facadeVariants.test.ts` and confirm the count. If it does not, leave the
request open and carry on — do not edit that file yourself.

---

## ITEM 1 — the browser items, when the window opens

In this order, each time-boxed:

- **K7.1** — the atlas gap, code-verified and never visually verified.
- **The wedge-wheel screenshot with panels closed.** `#tour-toggle-btn` against
  the real renderer before any fallback; the broad fallback destabilised the
  server and destroyed a good capture last time. One clean frame is the
  deliverable, not a perfect one.
- **U1's re-watch** in `e2e/panelOverlap.spec.ts` — `434c0fb` has structural and
  mutation verification but no browser confirmation.

---

## ITEM 2 — U2, mobile grounding

The largest open interface item and still not started across three runs. World
fills the screen, landscape works, the prompt box is findable, touch gestures
work — including the `touch-action` gap on `#world-canvas` that was found,
reported and never fixed.

*Gate:* measured on a real viewport. **Never a stylesheet substring** —
`test/navPad.test.ts` once asserted a CSS string appeared in the HTML and called
that a behaviour check.

This is the item Mark has raised most often and the one that decides whether a
link he sends is worth opening on a phone.

---

## ITEM 3 — finish what RUN 2 opened

- **The connectors gap.** You built the executor and closed part of it. Finish
  it, or state plainly which parts have a genuine structural reason to stay
  unused — that distinction is the whole value of the finding.
- **F2** — the rest of the K6 checklist, in K6's own priority order, minus what
  `425d20e` marked superseded.
- **The one remaining flag-gating instance** you found and correctly
  deprioritised: revisit only if it is cheap now.

---

## ITEM 4 — kitbash and facade depth

12 variants is a large improvement on 4 and still not much for 17,108
buildings. Push the variety further where it is authoring work: more grids per
era, storey-height variation, spandrel and mullion treatment.

**1980–2000 stays excluded by construction** — it is the gap between `postwar`
and `contemporary` and nothing should generate it.

---

## ITEM 5 — the two open decisions

- The **uncommitted `package.json` change** — inventory it, say what it does,
  and queue the decision. Do not silently commit or revert it.
- The **10 untracked `docs/pending-commits/*.txt`** — same. Nothing deleted;
  `docs/QUARANTINE-POLICY.md` governs, and these are Tier 2.

---

## ITEM 6 — Bug 2, close it honestly

The CSS rule that selector-matched and never painted. Two runs have failed to
explain it and it is worked around with inline styles. **An honest permanent
UNEXPLAINED is an acceptable close** — write it up as one, with what was ruled
out, and stop spending nights on it.

---

## IF YOU REACH THE END

Say so in the handover and keep going on the most valuable thing you can name.
Re-read **WHEN YOU MAY STOP** before ending. If your reason is not on that list,
you are not finished.
