# LANE BRIEF — BLD, 2026-09-15

**Terminal:** BLD · **Repository:** `C:\Code\sandbox-spike-codex` · **Branch:** `codex-lane`
**Mode:** autonomous, long run.

Linked worktree — `.git` is a FILE. Commit messages to
`$env:TEMP\COMMIT_MSG_BLD.txt`.

Policy is cited, never restated — `rule://lane-brief`.

---

## 0. PUSH FIRST. LAST NIGHT'S WORK IS ON ONE DISK.

Local `codex-lane` is at `aba191d`; `origin/codex-lane` is at `d19ddf5`. **The
cast shadows, the twenty-piece scene, the impostor bake and the massing bake are
not on origin.** Push before anything else.

Then `process_at("session-start")`, and again at every moment transition.
Generate this run's brief with the `lane_brief` prompt and commit it. Your
project id is `caliper-bld`.

## 1. MARK HAS SEEN THE SHOTS. HIS VERDICT, VERBATIM.

*"As far as style goes and overall finish of the buildings, no, I wouldn't call
them anywhere close to done — but I agree the lighting is much better and I think
that is what is bringing up the overall better."*

So the lighting work landed. `08-cast-shadows.png` is the shot he judged from;
`09` was correctly read as a throughput instrument and discounted.

**`L10`'s verdict stands but is now understood as narrower than it read.** The
scene does not look like a blockout at four pieces, close in. It has never been
judged at a camera anyone would play from, and the remaining gap is not shading.

## 2. THE OBJECTIVE — N1, AND IT IS NOT A SHADER TASK

**There is no scene.** A black void instead of a sky. A dirt plane that stops at
a hard edge. Nothing between the buildings. Four well-lit pieces floating in
black is not a city, and no further lighting mechanism fixes that.

Three parts, each its own commit, each with a before/after from **the same fixed
camera as `08`**:

**a. A sky.** Even a gradient. The void is doing more damage to the read than any
missing shader feature, and it is the cheapest thing on this list.

**b. A ground that does not end.** It should read as continuing past the frame
rather than stopping at a visible edge.

**c. Something at street level.** Kerbs, a path, one or two props. R8 —
*"interesting things happen where different things meet"* — applies to the
ground-meets-road-meets-building junction as much as to the single join already
treated, and R8 was the highest-yield thing in the whole look proof by your own
verdict.

**Gate: the pair of images, judged by Mark.** There is no numeric gate here, and
inventing one would be a check that cannot fail — which is the defect this
project names most often. Say plainly whether each part improved the read, and if
one did not, say that.

## 3. THEN BO7A — AND THE GATE IS CHECKABLE NOW

`BO7A` (catalogue entries for the new meshes) is still blocked on CLI's `A1`. It
already landed once and you could not see it: A1 is on `origin/scoring`, and last
night's brief said "on origin" without naming a ref. **That was the brief's
defect, not yours** — you checked, correctly found nothing, and moved on, which
is exactly right.

CLI is building `docs/EVENT-LOG.jsonl` this run (item V1) so that question
becomes answerable without guessing a branch. Until it exists, check
`origin/scoring` **by name**.

**Check; never wait.** `rule://stopping-authority` does not list "waiting for
another lane" as a reason to stop. If it is not there, carry on and say in the
handover that you checked and when.

## 4. FILES

`public/look-proof-scene.html`, `public/look-proof-material.js`,
`scripts/shoot-look-proof.mjs`, `scripts/normalise-kit-textures.mjs`, the asset
directories, `docs/look-proof-shots/`.

**Not `data/catalogue.json`** until §3's check passes.

## 5. ATTEMPT BUDGET

2 per item.

## 6. GUARDS WORTH ONE LINE EACH

A render is a long command and will be auto-promoted to background. **Exit 0 is
not evidence a shot exists — open the file and look at it.** Your own first
look-proof render was a blank white canvas with zero console errors and
`draw calls: 1`. You caught it; the trap is still there.

Do not let a memory reading stop a render. The 4 GB floor was tested and has no
source; a full regression render completed at 0.02 GB free.

Last run you overwrote a historical image by accident and caught it with
`git status` before trusting it. That is the process working — keep doing it.
