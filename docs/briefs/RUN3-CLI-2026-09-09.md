# RUN 3 — CLI LANE — 2026-09-09

Continues `docs/briefs/RUN2-CLI-2026-09-09.md`. Its routing, discipline and
handover sections still apply unchanged.

Read `docs/OVERNIGHT-RUN.md` first, including **WHEN YOU MAY STOP**. Tick in
`docs/specs/COMPLETION-PLAN.md`, which is now the canonical checklist.

RUN 2 landed 16 commits and was a good run. Two things it could not do are the
first two items here.

---

## ITEM 0 — the facade handoff. Three lines. Do this first.

The BLD lane built 12 real facade variants against the 4 window grids that
serve all 17,108 buildings — the traced root cause of Mark's oldest and
most-repeated complaint. **It is fully built, tested 7/7, and reachable by
nothing**, because `getFacadeMaterial` has exactly one production caller and it
is in a file this lane owns.

The exact diff, already written and verified against the real call site, is in
`C:\Code\sandbox-spike-codex\docs\CROSS-LANE-REQUESTS.md` §1. Read it there —
that worktree is a sibling checkout of this repository and reading it is fine;
writing to it is not.

In short: `public/city-render.js` around line 1930 passes `variantSeed: g.seed`
into `getFacadeMaterial`. `g.seed` is already in scope on the line above. The
parameter is optional and defaults to today's behaviour, so this is additive.

**Then un-mark the `{ todo }` gate** in `test/facadeVariants.test.ts` — "GATE:
distinct facade materials reachable from real placements, well above four" —
and confirm it passes without any other change. It already mirrors the real
placement path rather than a hoped-for one.

If the diff does not apply cleanly, say what changed and write your reply into
`docs/CROSS-LANE-REQUESTS.md` in **this** worktree. Do not edit BLD's file.

---

## ITEM 1 — the visual window. Look at B3.

`scripts/shoot.mjs` has now been deferred twice for memory. B3 landed
additively and **nobody has ever looked at what it draws.** Until someone does,
"the render path works" is a claim with no reference shot behind it — K5.5.

**The BLD lane is doing code-only work while you do this**, by arrangement, so
the 4 GB floor is yours for this window. Take it early in the run rather than
saving it for the end, because that is what happened last time.

Shoot the board-drawn world. Compare against the reference baselines
(`scripts/shoot-reference-baselines.mjs`). Report what you see plainly,
including if it looks wrong. **A bad render honestly reported is a result.**
When the window closes, say so in the handover so BLD can take its turn.

---

## ITEM 2 — finish B4

`propModel` is wired and product-reachable. Still open: **roads from
`roadkit`**, **building typologies from the kit**, and the **full props
manifest**. Not integration — the only way a piece can exist.

*Gate:* the dead-exports check, watched red for each, then green by real wiring.
Never by editing the allowlist by hand — `547b721` set that precedent
correctly.

---

## ITEM 3 — B3's exit condition

Once B4 is green: quarantine `public/city-render.js` to `_TO-DELETE/`, because
nothing reaches it any more. Then `city-plan.js` and `layout.js`.
`board-adapter.js` goes **only** after B2's pieces are what
`world-render-3d.js` reads — it is live today on P4.1's picking path.

Per `docs/QUARANTINE-POLICY.md` these are all **Tier 2**: moved and retained,
never purged. Ledger entries required.

---

## ITEM 4 — B2.8, now genuinely unblocked

`ca413a0` correctly established that most of the 38 could not be re-pinned
before B4, because they exercise the very files B4 replaces. Once item 2 lands,
that blocker is gone. Re-pin them, using `ca413a0`'s own categorisation.

Never loosen an assertion to make it pass. A vacuous test is worse than a red
one.

---

## ITEM 5 — C1 to 129 of 129

Nine NEVER RUN, one SURVIVED. The SURVIVED one is confirmed real — no
`node:test` assertion exists for it — so it needs a **decision**, not a re-run:
either write the missing control, or record why it cannot be written. Two more
have fixed `expect` strings and need a re-run each.

---

## ITEM 6 — C2, the breakdown by mechanism

2,762 entries with auto-generated reasons. **No number from this may be
published anywhere** until it is split by real mechanism: product, demo-only,
test-only, unreachable, and the fifth class — data-reachable, where a string key
selects from a registry and the import graph cannot see it. UMAA trigger 2.

---

## ITEM 7 — B7, the countryside

Farmland behind the mainland, the range framing rather than occupying, and the
greenery `contemporary` is supposed to carry.

*Gate:* settled land stays inside 20–40 km² while the countryside fills the
rest — so this cannot be satisfied by quietly spreading the city outward.

---

## ITEM 8 — C5, a decision

B2.5's CPU gate is honestly red at 40–291 s against 30 s, and generation is
offline now so nothing in `src/` is exposed to the ceiling regardless. It cannot
stay red forever without a decision. Either get under it, or retire it with a
written reason and a replacement gate that still proves generation is not on the
request path. Queue it to Mark if you would rather he chose — but take the least
irreversible path and keep going.

---

## IF YOU REACH THE END

Say so in the handover and keep going on the most valuable thing you can name.
Re-read **WHEN YOU MAY STOP** before ending. If your reason is not on that list,
you are not finished.
