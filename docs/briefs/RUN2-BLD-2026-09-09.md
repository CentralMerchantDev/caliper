# RUN 2 — BLD LANE — 2026-09-09

Continues `docs/briefs/OVERNIGHT-BLD-2026-09-09.md`. That brief's routing,
discipline, decision queue, handover and escape-clause sections **still apply
unchanged** — read them, do not re-derive them. This document replaces only its
§5 (the work) and §8 (memory).

**Item zero, small:** re-take the operating manual, which has gained a section
since you last pulled it.

```
git checkout b1-land -- docs/OVERNIGHT-RUN.md
git status --porcelain
```

Confirm only that path changed, read the new **WHEN YOU MAY STOP** section, and
commit it before starting.

---

## 1. WHERE

`C:\Code\sandbox-spike-codex`, branch `codex-lane`. Confirm before touching
anything.

---

## 2. WHY THIS LIST IS DIFFERENT

The last run finished in twenty-three minutes. Not because the work was done —
four of its seven items needed a browser, memory sat at 2.47–3.99 GB against
the 4 GB floor all session, and those four were never available. The list was
badly built for the machine it ran on. That was my error, not yours.

**This list is authoring work.** Almost none of it needs a browser. Where visual
confirmation is eventually required, the item says so and names a code-level
gate that can be satisfied first, so the work lands and only its verification
waits.

---

## 3. THE WORK — in order

### 1. Facade texture variety — the actual cause of "buildings read as basic"

Your own finding at `a349b05`: **four window-grid textures for 17,108
buildings.** That is the root cause of Mark's oldest and most-repeated
complaint, and it is authoring work — no browser needed to write it.

Build real variety in `facade-textures.js`: grids that differ by era
(`heritage`, `interwar`, `postwar`, `contemporary`), by storey height, by
window proportion, by spandrel and mullion treatment. `CHARACTER_SETS` already
carries the four eras and the atlas already has a caller — this is depth, not
new machinery.

*Gate, code-level, satisfiable without a browser:* the count of distinct
facade materials reachable from real placements, asserted against a floor well
above four, and measured from the real caller in `city-render.js` rather than
from the registry. Watch it red at today's number first.

**1980–2000 stays excluded by construction** — it is the gap between `postwar`
and `contemporary` and nothing should generate it.

### 2. The rest of the K6 buildings checklist

You extended it from `K6-BUILDINGS.md`'s own measurements-and-priority section
at `a349b05`. Work it, in the priority order that document already establishes.

### 3. Kitbash variety

62 parts across 6 categories, 40 canonical designs, and a rarity split of
35/25/40. Measure what actually reaches the world versus what exists in the
registry, then close the gap where it is real. Authoring work.

### 4. The comment-stripping sweep, finished

`bac6c1b` built the shared helper and swept nine files. Your own note says the
same blind spot plausibly exists elsewhere, unfound, because it was never looked
for as a category. Look for it as a category: every raw-source `assert.match` in
the suite, not just the nine.

### 5. K7.1 — the atlas gap, visual confirmation

Needs a render. Attempt when memory allows.

### 6. U2 — mobile grounding

Needs a browser. The largest interface item and still not started:
world-fills-screen, landscape, the prompt box findable, touch gestures, and the
`touch-action` gap on `#world-canvas` that was found, reported and never fixed.
Attempt when memory allows.

### 7. The wedge-wheel screenshot with panels closed, and U1's re-watch

Both need a browser. `#tour-toggle-btn` against the real renderer before any
fallback — the broad fallback destabilised the server and destroyed a good
capture last time. Time-box both.

---

## 4. MEMORY — rotate, do not stall

Items 1 through 4 need no browser. Items 5 through 7 do. Check free memory
before any render or browser attempt; below the 4 GB floor, go back to the
authoring items and check again later. **Do not kill processes** — the lanes run
inside Antigravity IDE, and Chrome is needed for items 5 to 7. Record PIDs and
reasoning instead.

If memory never clears, items 1 to 4 are a full run's work on their own, and
item 1 is the one Mark cares about most.

---

## 5. IF YOU REACH THE END OF THIS LIST

Say so in the handover, then keep going on the most valuable thing you can name
and justify. Running out of work is not a stopping condition. Re-read
`docs/OVERNIGHT-RUN.md`'s **WHEN YOU MAY STOP** before ending the run, and if
your reason is not on that list, you are not finished.
