<!-- COUNTS: modules=96 exports=3007 called=294 testOnly=187 dead=2526 -->
<!-- GENERATED by scripts/gen-module-map.mjs -- do not edit by hand. -->
# Module map

Every export in `public/` and `src/` (excluding `public/vendor/` and `*.generated.ts`), and every file that imports it. Read this before proposing to build a capability -- `docs/AUDIT-PROTOCOL.md`'s nine recorded pattern-E failures all shipped work beside a capability nobody checked already existed.

`UNCALLED` means no other file (besides tests) imports this export by name. It does not always mean dead: a function reached only through a dispatch table defined in its own file (never imported by name from elsewhere) reads the same way here as one nobody reaches at all -- see `test/deadExports.allowlist.json`'s own header for that distinction, and `test/deadExports.test.ts` (the gate this map shares its data with).

---

## `public/apply-and-persist.js`

- `applyAndPersist` -- called from `public/index.html`

## `public/apply-layers.js`

- `applyLayers` -- called from `public/city-render.js`

## `public/asset-registry.js`

- `ASSET_REGISTRY` -- called from `public/kit-contact-sheet.html`, `public/layout.js`, `public/model-library.html`, `src/index.ts`
- `deriveRank` -- **UNCALLED** (imported only by test: `test/libraryStructure.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `deriveScaleClass` -- **UNCALLED** (imported only by test: `test/libraryStructure.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/board-adapter.js`

- `boardPiecesById` -- called from `public/world-render-3d.js`
- `piecesFromWorld` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/board-region.js`

- `localBoard` -- called from `public/isolate.js`, `public/move-piece.js`

## `public/board.js`

- `checkedCellBounds` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createBoard` -- called from `public/board-region.js`
- `footCellRect` -- called from `public/board-region.js`, `public/isolate.js`
- `orientedWD` -- called from `public/move-piece.js`
- `verticalExtent` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/buildings.js`

- `CHARACTER_SETS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CIVIC_TYPOLOGIES` -- called from `public/model-library.html`
- `HEIGHT` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `MIN_HEIGHT` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `ROOFS` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TRIM_PATCH_U0` -- **UNCALLED** (imported only by test: `test/trimAtlasPatch.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TRIM_PATCH_USPAN` -- **UNCALLED** (imported only by test: `test/trimAtlasPatch.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TRIM_PATCH_V0` -- **UNCALLED** (imported only by test: `test/trimAtlasPatch.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TRIM_PATCH_VSPAN` -- **UNCALLED** (imported only by test: `test/trimAtlasPatch.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TYPOLOGY_FOOTPRINT_CELLS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `WALLS` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldApartmentWalkup` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldBusinessParkBlock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldHighStreetTerrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldMidrise` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldOffice` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldShop` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldTerrace` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldTower` -- called from `public/kit-contact-sheet.html`, `public/kitbash-comparison.html`, `public/props.js`
- `bldTownhouse` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldVilla` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldWarehouse` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `bldWorkshop` -- called from `public/kit-contact-sheet.html`, `public/props.js`
- `building` -- called from `public/city-render.js`, `public/layout-fits.js`, `public/props.js`
- `createCollector` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `emitBuilding` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pick` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `rnd` -- called from `public/city-render.js`

## `public/change-quest.js`

- `changeSomethingQuest` -- **UNCALLED** (imported only by test: `test/questCompletion.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/city-live-world.js`

- `QUARTER` -- called from `public/city.html`
- `buildLiveQuarter` -- called from `public/city.html`
- `loadLiveWorld` -- called from `public/city.html`

## `public/city-plan.js`

- `BANDS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BOARDWALK` -- called from `public/city-render.js`
- `BRIDGES` -- called from `public/city-render.js`
- `CAUSEWAYS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CENTRES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `COAST` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `DENSITY_LADDER` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `DISTRICTS` -- **UNCALLED** (imported only by test: `test/worldSpec.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `FREEWAYS` -- called from `public/road-network.js`
- `GRID` -- **UNCALLED** (imported only by test: `test/worldSpec.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `HARBOUR` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `HIGHWAYS` -- called from `public/road-network.js`
- `ISLAND` -- called from `public/road-network.js`
- `LANDMASSES` -- called from `public/road-network.js`
- `MARINA` -- called from `public/city-render.js`
- `PIER` -- called from `public/city-render.js`
- `PLOT_CLASSES` -- called from `public/buildings.js`, `public/city-render.js`
- `PLOT_RULES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `ROADS` -- called from `public/board-adapter.js`, `public/city-render.js`
- `SETTLEMENTS` -- called from `public/road-network.js`
- `SHORE_MARGIN` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `SUBURBS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `WORLD` -- called from `public/city-render.js`, `public/city.html`, `public/terrain.js`, `public/world-render-3d.js`
- `cityDemand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `classForBlock` -- **UNCALLED** (imported only by test: `test/planSeed.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `classForSettlementBlock` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `coastRoad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `coastlinePolygon` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `distanceToCoast` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `distanceToCoastExact` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateBlocks` -- **UNCALLED** (imported only by test: `test/planSeed.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateBridgeApproaches` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateCityPlan` -- called from `public/city-render.js`
- `generateRamps` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateRoads` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateSettlement` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateWorld` -- called from `public/city-render.js`, `public/world.js`
- `intensityAt` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `isOnLand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `landmassPolygons` -- called from `public/city-render.js`
- `landmassPolygonsDesign` -- called from `public/terrain.js`
- `offsetPolygon` -- called from `public/city-render.js`
- `rectIsBuildable` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `settlementCentres` -- called from `public/road-network.js`
- `signedArea2` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `splineOpen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `splinePolygon` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `subdivideBlock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/city-render.js`

- `LOOK` -- called from `public/city.html`
- `bandLevelAt` -- **UNCALLED** (imported only by test: `test/setPieceRefusal.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildEmbankmentSkirt` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildScenePlacements` -- **UNCALLED** (imported only by tests: `test/cityRenderScenePlacements.test.ts`, `test/runGenerateRequest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildWorld` -- called from `public/city.html`
- `buildWorldState` -- **UNCALLED** (imported only by tests: `test/cityRenderScenePlacements.test.ts`, `test/cityRenderWorldState.test.ts`, `test/pickSelection.test.ts`, `test/questCompletion.test.ts`, `test/runGenerateRequest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `gradeGroundBands` -- **UNCALLED** (imported only by tests: `test/cityWorld.test.ts`, `test/setPieceRefusal.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `groundOrRefuse` -- **UNCALLED** (imported only by test: `test/setPieceRefusal.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mergedPlatformGeometry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `platformCells` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/colour-grade.js`

- `BLOOM` -- called from `public/city.html`, `public/world-render-3d.js`
- `makeGradeShader` -- called from `public/city.html`, `public/world-render-3d.js`

## `public/describe-request.js`

- `makeDescribeRequest` -- called from `public/index.html`

## `public/facade-textures.js`

- `FACADE_FAMILIES` -- **UNCALLED** (imported only by test: `test/phaseDelta.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateFacadeAtlas` -- **UNCALLED** (imported only by test: `test/phaseDelta.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `getFacadeMaterial` -- called from `public/city-render.js`, `public/kitbash-assembler.js`, `public/kitbash-comparison.html`, `public/kitbash-parts.js`

## `public/features.js`

- `FEATURES` -- called from `public/city-plan.js`, `public/city-render.js`, `public/zoning.js`
- `RAIL_CORRIDOR_HALF_WIDTH` -- **UNCALLED** (imported only by test: `test/features.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `footprintOf` -- called from `public/city-plan.js`
- `placeFeatures` -- called from `public/city-plan.js`, `public/city-render.js`, `public/world-render-3d.js`

## `public/footprint.js`

- `DRY_ENOUGH` -- called from `public/city-render.js`
- `STEP` -- called from `public/land-use.js`
- `assessFootprint` -- called from `public/city-render.js`

## `public/generate-request.js`

- `MAX_REQUEST_TEXT_LENGTH` -- **UNCALLED** (imported only by test: `test/generateRequest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildGeometryPrompt` -- called from `public/run-generate-request.js`
- `verifyGeneratedGeometry` -- called from `public/run-generate-request.js`

## `public/grade.js`

- `GRADE` -- called from `public/city-render.js`
- `RAIL_ALIGNMENT` -- called from `public/city-render.js`, `public/features.js`, `public/land-use.js`
- `ROAD_GRADE` -- called from `public/city-render.js`, `public/road-network.js`
- `gradeRun` -- called from `public/city-render.js`, `public/land-use.js`

## `public/grid.js`

- `ATOM` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BLOCK` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CELL` -- **UNCALLED** (imported only by tests: `test/auditFindings.test.ts`, `test/grid.test.ts`, `test/landEndToEnd.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CELLS_ACROSS` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `DIVISION` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `LEVEL` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `QUADRANT` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `atomCentre` -- called from `public/board.js`
- `atomInWorld` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `atomOf` -- called from `public/board-adapter.js`
- `atomOrigin` -- **UNCALLED** (imported only by test: `test/board.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `atomRect` -- called from `public/board.js`
- `atomsFor` -- called from `public/board-adapter.js`
- `cellCentre` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cellOf` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cellOrigin` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cellRect` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cellsFor` -- **UNCALLED** (imported only by tests: `test/grid.test.ts`, `test/landEndToEnd.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createGrid` -- called from `public/world.js`
- `heightOf` -- called from `public/board.js`
- `inWorld` -- called from `public/world-render-3d.js`
- `levelsFor` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `snap` -- **UNCALLED** (imported only by tests: `test/grid.test.ts`, `test/landEndToEnd.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `step` -- **UNCALLED** (imported only by test: `test/grid.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/ground.js`

- `ACCEPTS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `AIR_BANDS` -- **UNCALLED** (imported only by test: `test/ground.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BEDROCK_Y` -- **UNCALLED** (imported only by test: `test/ground.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `STRATA` -- **UNCALLED** (imported only by test: `test/ground.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `SURFACE` -- **UNCALLED** (imported only by tests: `test/auditFindings.test.ts`, `test/ground.test.ts`, `test/landCompleteness.test.ts`, `test/landEndToEnd.test.ts`, `test/place.test.ts`, `test/umaaFindings.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bandAt` -- **UNCALLED** (imported only by test: `test/ground.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createGround` -- **UNCALLED** (imported only by tests: `test/auditFindings.test.ts`, `test/generateRequest.test.ts`, `test/ground.test.ts`, `test/landCompleteness.test.ts`, `test/landEndToEnd.test.ts`, `test/place.test.ts`, `test/runGenerateRequest.test.ts`, `test/transform.test.ts`, `test/umaaFindings.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `strataAt` -- **UNCALLED** (imported only by test: `test/ground.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/instance-groups.js`

- `partitionForInstancing` -- called from `public/city-render.js`

## `public/isolate.js`

- `NEIGHBOUR_MARGIN_M` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `applyIsolate` -- called from `public/world-render-3d.js`
- `neighboursOf` -- called from `public/world-render-3d.js`
- `planIsolate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `restoreIsolate` -- called from `public/world-render-3d.js`

## `public/kitbash-assembler.js`

- `assembleBuilding` -- called from `public/kitbash-district.html`
- `createRng` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/kitbash-exemplar.js`

- `buildKitbashTowerExemplar` -- called from `public/kitbash-comparison.html`

## `public/kitbash-parts.js`

- `CELL_M` -- **UNCALLED** (imported only by test: `test/kitbashParts.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `KITBASH_PARTS` -- called from `public/kitbash-assembler.js`, `public/kitbash-contact-sheet.html`, `src/kitbashRetrieval.ts`
- `createBevelledExtrusion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createChamferedRectShape` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `resolvePalette` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/kitbash-recipe-map.js`

- `DESIGN_RECIPE_MAP` -- called from `public/kitbash-district.html`

## `public/land-use.js`

- `BEACH_ABOVE` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `MIN_CORRIDOR_ON_LAND` -- called from `public/features.js`
- `ROAD_SLOPE_MAX` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `SLOPE` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `USE` -- called from `public/board-adapter.js`, `public/ground.js`, `public/settlement-fit.js`, `public/zoning.js`
- `buildAllowedAt` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `classifyAt` -- called from `public/board.js`, `public/ground.js`, `public/settlement-fit.js`, `public/zoning.js`
- `findCorridor` -- called from `public/features.js`
- `findFlattestSite` -- called from `public/features.js`
- `findQuay` -- called from `public/features.js`
- `findSite` -- called from `public/features.js`
- `makeDemand` -- called from `public/city-plan.js`
- `roadAllowedAt` -- called from `public/city-plan.js`
- `slopeAt` -- called from `public/ground.js`

## `public/layout-fits.js`

- `makeFits` -- called from `public/city-render.js`
- `measuredSeedFor` -- **UNCALLED** (imported only by test: `test/layoutGeometry.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/layout.js`

- `CHARACTERS` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `FOUNDATION_FOR_VERDICT` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `LIBRARY_TIERS_FOR_CLASS` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TYPOLOGIES_FOR_CLASS` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `characterFor` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cornerAt` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `groupByVariant` -- called from `public/city-render.js`
- `hash01` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `libraryModelFor` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `planBlock` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `planCity` -- called from `public/city-render.js`
- `planPlot` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `rowPositionAt` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `seedFor` -- called from `public/layout-fits.js`
- `situationOf` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `terraceUnitsFor` -- called from `public/layout-fits.js`
- `typologyFor` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `variantKeyOf` -- called from `public/city-render.js`
- `weightedPick` -- **UNCALLED** (imported only by test: `test/layout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/menus.js`

- `createCommandCentre` -- called from `public/index.html`
- `createMenus` -- called from `public/index.html`
- `registerDismisser` -- called from `public/workbench.js`

## `public/model-caller.js`

- `productionModelCaller` -- **UNCALLED** (imported only by test: `test/modelCaller.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/model-forge.js`

- `ALLOWED_GEOMETRY_CONSTRUCTORS` -- called from `public/generate-request.js`
- `FORBIDDEN_TOKENS` -- **UNCALLED** (imported only by test: `test/modelForge.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `FORGE_LIMITS` -- **UNCALLED** (imported only by test: `test/modelForge.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `describeVerdict` -- **UNCALLED** (imported only by test: `test/modelForge.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `findDisallowedApiSurface` -- **UNCALLED** (imported only by test: `test/modelForge.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `scanSource` -- **UNCALLED** (imported only by test: `test/modelForge.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `verifyModelSource` -- called from `public/generate-request.js`

## `public/model-registry.js`

- `createModelRegistry` -- called from `public/city-render.js`, `public/index.html`
- `populateRegistryFromTierModels` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/model-retrieval.js`

- `InMemoryVectorize` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildEmbeddingText` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `embedText` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `embedTextBatch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `entryFitsSpace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `findModels` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `handTunedPseudoEmbedding` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexRegistryInVectorize` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexRegistryWithPseudo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `lexicalSearch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pseudoVectorSearch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `vectorSearch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/move-piece.js`

- `MOVE_QUERY_MARGIN_M` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `moveEditFor` -- called from `public/world-render-3d.js`
- `tryMove` -- called from `public/world-render-3d.js`

## `public/navigate.js`

- `createNavigation` -- called from `public/index.html`

## `public/noise.js`

- `DEFAULT_SEED` -- called from `public/city-plan.js`, `public/city-render.js`, `public/terrain.js`, `public/world.js`
- `clamp` -- called from `public/buildings.js`, `public/city-plan.js`, `public/terrain.js`
- `createNoise` -- **UNCALLED** (imported only by test: `test/noise.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fbm` -- called from `public/buildings.js`, `public/city-plan.js`, `public/terrain.js`
- `hash01` -- called from `public/city-plan.js`
- `hash2` -- called from `public/terrain.js`
- `seedToInt` -- called from `public/terrain.js`
- `smooth` -- called from `public/terrain.js`
- `smoother` -- called from `public/city-plan.js`, `public/terrain.js`
- `valueNoise` -- called from `public/terrain.js`

## `public/place.js`

- `createPlacer` -- **UNCALLED** (imported only by tests: `test/auditFindings.test.ts`, `test/landCompleteness.test.ts`, `test/landEndToEnd.test.ts`, `test/place.test.ts`, `test/umaaFindings.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/prop-manifest.js`

- `PROPS` -- called from `public/prop-models.js`, `public/prop-placement.js`, `public/props.js`
- `hardPropIds` -- **UNCALLED** (imported only by test: `test/propManifest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `modelFor` -- **UNCALLED** (imported only by test: `test/landCompleteness.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propFootprint` -- called from `public/city-render.js`

## `public/prop-models.js`

- `VARIED` -- **UNCALLED** (imported only by test: `test/propModels.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `disposePropGeometry` -- **UNCALLED** (imported only by tests: `test/phaseDelta.test.ts`, `test/propModels.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `modelCoverage` -- **UNCALLED** (imported only by test: `test/propModels.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propGeometry` -- called from `public/city-render.js`
- `propModel` -- **UNCALLED** (imported only by test: `test/propModels.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/prop-placement.js`

- `createPropRegistry` -- **UNCALLED** (imported only by test: `test/propPlacement.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `placePropsChecked` -- **UNCALLED** (imported only by test: `test/propPlacement.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `tryPlaceProp` -- **UNCALLED** (imported only by test: `test/propPlacement.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/props.js`

- `MODELS` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `aircraft` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `assertModelDeclaration` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldApartmentWalkup` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldMidrise` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldOffice` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldShop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldTerrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldTower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldTownhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldVilla` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldWarehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldWorkshop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundary` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `boundaryBinStore` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundaryDriveway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundaryFrontWall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundaryGate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundaryPath` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `boundarySideReturn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeCableStayedSkybridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeCanalStepBridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeDiagridTubeSkyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeLivingArchAqueduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeLivingGreenViaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeTimberHyperboloidFootbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `building` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `computeBounds` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `countTriangles` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `facade` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `gardenFeature` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `getModel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `groundFurniture` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `industrialInfrastructure` -- called from `public/kit-contact-sheet.html`
- `mergeGeometries` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parkFeature` -- called from `public/kit-contact-sheet.html`
- `parkedCarRow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pathwayFlagstonePromenade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pathwayGravelMeander` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pathwayTimberBoardwalk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `person` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `personInAction` -- called from `public/kit-contact-sheet.html`
- `propBikeServiceStation` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propEvSuperchargerHub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propKineticSolarLamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propLivingWallTotem` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propPollinatorHabitatPost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propRainGardenPlanter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propShadedParkletDeck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propSmartWasteCompactor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propSolarCanopyBench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `propWaterRefillFountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roadwayBioswaleAvenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roadwayCycleSuperhighway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roadwayGreenTramwayLawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roadwayPermeablePavedStreet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roadwayWoonerfSharedSpace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roofClutter` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `sidewalkRainGardenCurb` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `sidewalkSolarPaverWalk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `streetFurniture` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `streetTreeSeasonal` -- called from `public/kit-contact-sheet.html`
- `tree` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `vehicle` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `vehicleService` -- called from `public/kit-contact-sheet.html`
- `verifyAllModels` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `verifyFamilyVariants` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `vessel` -- called from `public/kit-contact-sheet.html`, `public/model-library.html`
- `waterfrontModule` -- called from `public/kit-contact-sheet.html`

## `public/quest.js`

- `QUEST_STATE_FIELDS` -- **UNCALLED** (imported only by test: `test/quest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `VERDICT_KEYS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createQuestLog` -- **UNCALLED** (imported only by test: `test/quest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `evaluateQuest` -- **UNCALLED** (imported only by tests: `test/quest.test.ts`, `test/questCompletion.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `questState` -- **UNCALLED** (imported only by tests: `test/quest.test.ts`, `test/questCompletion.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `validateQuest` -- **UNCALLED** (imported only by test: `test/quest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/resolve-models.js`

- `resolveOverrideModels` -- called from `public/city-render.js`

## `public/road-network.js`

- `STANDARD_WIDTH` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildArterialNetwork` -- **UNCALLED** (imported only by test: `test/roadNetwork.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildBridgePieces` -- **UNCALLED** (imported only by test: `test/bridgePieces.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildCollectorLocalNetwork` -- **UNCALLED** (imported only by test: `test/collectorLocalNetwork.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildConnectivityBridges` -- **UNCALLED** (imported only by test: `test/connectivityBridges.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `verifyBridgeEnds` -- **UNCALLED** (imported only by test: `test/bridgePieces.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/roadkit-street-demo.js`

- `buildDemoStreet` -- called from `public/roadkit-street-demo.html`

## `public/roadkit.js`

- `MODULE_M` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `RAIL_GAUGE_M` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `RAIL_TRACK_ROW` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `ROAD_STANDARDS` -- called from `public/road-network.js`
- `bridgeAbutment` -- called from `public/props.js`
- `bridgeApproachRamp` -- called from `public/props.js`
- `bridgeArch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeCableStayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeChain` -- called from `public/props.js`
- `bridgeDeckSpan` -- called from `public/props.js`
- `bridgePier` -- called from `public/props.js`
- `bridgeSpan` -- called from `public/road-network.js`
- `busBay` -- called from `public/props.js`
- `causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `computeBounds` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `crossing` -- called from `public/props.js`
- `curve` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `gradeSeparation` -- called from `public/props.js`
- `intersection3Way` -- called from `public/props.js`
- `intersection4Way` -- called from `public/props.js`
- `junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `layby` -- called from `public/props.js`
- `levelCrossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `medianBreak` -- called from `public/props.js`
- `mergeGeometries` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `railPlatform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `railStraight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `railSwitch` -- called from `public/props.js`
- `rampDiverge` -- called from `public/props.js`
- `rampMerge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `roundaboutModern` -- called from `public/props.js`
- `slipLane` -- called from `public/props.js`
- `straight` -- **UNCALLED** (imported only by test: `test/roadkit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `testBridgeSpanInvariants` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `transformSocket` -- called from `public/road-network.js`
- `turningHead` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `turningPocket` -- called from `public/props.js`
- `verifyAllRoadKit` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `verifyModel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `verifySocketMating` -- called from `public/road-network.js`

## `public/run-generate-request.js`

- `runGenerateRequest` -- **UNCALLED** (imported only by test: `test/runGenerateRequest.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/selection.js`

- `createSelection` -- called from `public/world-render-3d.js`

## `public/settlement-fit.js`

- `fitSettlements` -- called from `public/city-plan.js`

## `public/showstoppers.js`

- `SHOWSTOPPERS` -- called from `public/props.js`
- `bldArtDecoSpire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldCascadingTerraces` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bldEcoBiophilicTownhouseRow` -- called from `public/props.js`
- `bldEcoBoscoVerticale` -- called from `public/props.js`
- `bldEcoCurvedRibbonVilla` -- called from `public/props.js`
- `bldEcoDiagridBiotower` -- called from `public/props.js`
- `bldEcoFloatingCanopyHub` -- called from `public/props.js`
- `bldEcoGreenPodOffice` -- called from `public/props.js`
- `bldEcoHelixTerrace` -- called from `public/props.js`
- `bldEcoHyperboloidTimberHQ` -- called from `public/props.js`
- `bldEcoSolarSpire` -- called from `public/props.js`
- `bldEcoStepGardenWalkup` -- called from `public/props.js`
- `bldGrandChateau` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeCableStayedTower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bridgeCalatravaHarpPylon` -- called from `public/props.js`
- `bridgeCalatravaRibArch` -- called from `public/props.js`
- `bridgeCalatravaSkeletalSpineOverpass` -- called from `public/props.js`
- `bridgeCalatravaSundialFootbridge` -- called from `public/props.js`
- `bridgeCalatravaTwinMastViaduct` -- called from `public/props.js`
- `bridgeCalatravaWingedSwingBridge` -- called from `public/props.js`
- `civicEcoBiomeDome` -- called from `public/props.js`
- `civicEcoHydroTransitTerminal` -- called from `public/props.js`
- `civicEcoOperaFlow` -- called from `public/props.js`
- `civicEcoSportsArena` -- called from `public/props.js`
- `civicEcoWaveLibrary` -- called from `public/props.js`
- `civicGrandCathedral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civicGrandTerminus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parkBotanicalPalmHouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parkObservationWheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `vehicleBulletTrain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `vesselSuperyacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/sim-baseline.generated.js`

- `applyAction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `chooseAction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `initialWorld` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `tick` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/sky.js`

- `createCitySky` -- called from `public/city-render.js`

## `public/spatial-index.js`

- `PLOT_BUCKET` -- called from `public/city-plan.js`
- `buildSpatialIndex` -- **UNCALLED** (imported only by tests: `test/cityWorld.test.ts`, `test/describeRequest.test.ts`, `test/pickSelection.test.ts`, `test/selection.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/stage-artefact.js`

- `describeStageOutcome` -- **UNCALLED** (imported only by test: `test/stageArtefact.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `stageArtefact` -- **UNCALLED** (imported only by test: `test/stageArtefact.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/terrain.js`

- `// Re-exported so existing consumers (city-render imports fbm from here) keep
  // working. Same functions` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BASINS` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BEACH_FADE_M` -- **UNCALLED** (imported only by test: `test/beachWidth.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BEACH_FULL_M` -- **UNCALLED** (imported only by test: `test/beachWidth.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `EDGE` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `GROUND_BANDS` -- called from `public/city-render.js`
- `LandField` -- called from `public/world.js`
- `SNOW_LINE` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TERRAIN` -- called from `public/grid.js`
- `TREE_LINE` -- called from `public/city-render.js`
- `WATERWAYS` -- called from `public/city-render.js`
- `beachWeight` -- called from `public/city-render.js`
- `bucketKeyInRange` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `cliffiness` -- called from `public/city-render.js`
- `edgeFalloff` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fbm` -- called from `public/city-render.js`
- `from noise.js.
  hash2` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `groundColor` -- called from `public/city-render.js`
- `makeHeightAt` -- called from `public/city-render.js`, `public/world.js`
- `one implementation` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `reliefAt` -- **UNCALLED** (imported only by test: `test/cityWorld.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `valueNoise` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `waterwayAt` -- called from `public/city-render.js`, `public/ground.js`, `public/world-render-3d.js`
- `waterwayInfoAt` -- called from `public/ground.js`
- `waterwaySurface` -- called from `public/city-render.js`

## `public/tier-models.js`

- `TIER_MODELS` -- called from `public/model-registry.js`, `public/props.js`
- `av_highend_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_highend_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_luxury_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_mid_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midhigh_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_midlow_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_advanced_fighter_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_aerobatic_monoplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_aircraft_hangar_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_airport_control_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_airport_radar_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_amphibious_flying_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_autogyro_gyrocopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_aviation_fuel_tanker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_baggage_cargo_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_bush_plane_taildragger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_cargo_feeder_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_corporate_twin_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_crop_duster_plane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_double_deck_jumbo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_evtol_airtaxi_passenger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_executive_twin_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_floatplane_lake` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_glider_sailplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_heavy_freight_cargo` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_heavy_lift_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_helipad_elevated_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_hot_air_balloon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_jumbo_jet_quad` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_light_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_light_single_engine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_long_range_scout_drone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_luxury_bizjet_heavy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_passenger_jetway_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_pushback_tug_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_regional_jetliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_runway_threshold_lighting` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_search_rescue_helicopter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_stealth_strategic_bomber` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_suborbital_spaceplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_supersonic_passenger_jet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_tiltrotor_military_transport` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_twin_utility_turboprop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_vintage_biplane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_widebody_airliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `av_showstopper_windsock_mast_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_alpine_chalet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_art_deco_skyscraper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_artisan_workshop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_biophilic_townhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_biotech_laboratory` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_brutalist_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_canopy_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_cantilever_penthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_corner_bodega_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_craftsman_bungalow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_crystalline_pavilion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_data_center_cube` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_diagrid_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_floating_cube_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_geodetic_eco_home` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_gothic_revival_manor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_greenpod_office` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_helix_terrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_hyperboloid_hq` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_industrial_warehouse_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_kinetic_facade_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_micro_apartment_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_midcentury_ranch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_modern_loft_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_modular_timber_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_neoclassic_mansion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_origami_cultural_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_parametric_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_ribbon_villa` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_row_brownstone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_shard_biotower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_shipping_container_living` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_skybridge_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_solar_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_stepgarden_walkup` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_suburban_split_level` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_terraced_courtyard_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_vertical_forest` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_waterfall_atrium` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f1_wave_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_alpine_chalet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_art_deco_skyscraper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_artisan_workshop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_biophilic_townhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_biotech_laboratory` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_brutalist_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_canopy_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_cantilever_penthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_corner_bodega_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_craftsman_bungalow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_crystalline_pavilion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_data_center_cube` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_diagrid_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_floating_cube_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_geodetic_eco_home` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_gothic_revival_manor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_greenpod_office` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_helix_terrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_hyperboloid_hq` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_industrial_warehouse_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_kinetic_facade_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_micro_apartment_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_midcentury_ranch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_modern_loft_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_modular_timber_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_neoclassic_mansion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_origami_cultural_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_parametric_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_ribbon_villa` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_row_brownstone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_shard_biotower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_shipping_container_living` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_skybridge_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_solar_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_stepgarden_walkup` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_suburban_split_level` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_terraced_courtyard_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_vertical_forest` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_waterfall_atrium` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f2_wave_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_alpine_chalet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_art_deco_skyscraper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_artisan_workshop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_biophilic_townhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_biotech_laboratory` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_brutalist_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_canopy_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_cantilever_penthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_corner_bodega_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_craftsman_bungalow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_crystalline_pavilion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_data_center_cube` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_diagrid_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_floating_cube_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_geodetic_eco_home` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_gothic_revival_manor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_greenpod_office` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_helix_terrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_hyperboloid_hq` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_industrial_warehouse_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_kinetic_facade_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_micro_apartment_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_midcentury_ranch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_modern_loft_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_modular_timber_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_neoclassic_mansion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_origami_cultural_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_parametric_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_ribbon_villa` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_row_brownstone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_shard_biotower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_shipping_container_living` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_skybridge_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_solar_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_stepgarden_walkup` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_suburban_split_level` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_terraced_courtyard_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_vertical_forest` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_waterfall_atrium` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f3_wave_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_alpine_chalet` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_art_deco_skyscraper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_artisan_workshop` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_biophilic_townhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_biotech_laboratory` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_brutalist_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_canopy_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_cantilever_penthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_corner_bodega_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_craftsman_bungalow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_crystalline_pavilion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_data_center_cube` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_diagrid_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_floating_cube_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_geodetic_eco_home` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_gothic_revival_manor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_greenpod_office` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_helix_terrace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_hyperboloid_hq` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_industrial_warehouse_hub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_kinetic_facade_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_micro_apartment_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_midcentury_ranch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_modern_loft_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_modular_timber_flat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_neoclassic_mansion` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_origami_cultural_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_parametric_residence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_ribbon_villa` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_row_brownstone` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_shard_biotower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_shipping_container_living` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_skybridge_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_solar_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_stepgarden_walkup` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_suburban_split_level` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_terraced_courtyard_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_vertical_forest` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_waterfall_atrium` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bld_f4_wave_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_highend_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_luxury_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_mid_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midhigh_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_midlow_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_acoustic_timber_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_bamboo_screen_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_boom_barrier_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_brick_courtyard_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_brick_pier_iron_infill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_cedar_privacy_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_chainlink_barbed_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_coastal_rope_bollard_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_concrete_blast_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_corrugated_metal_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_corten_steel_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_crowd_control_barricade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_decorative_chain_bollard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_decorative_hedge_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_drystone_field_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_gabion_mesh_seatwall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_gabion_rock_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_gabion_vegetated_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_glass_balustrade_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_ha_ha_sunken_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_lattice_garden_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_living_ivy_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_manicured_boxwood_hedge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_marble_parapet_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_modern_slat_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_motorized_sliding_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_ornamental_iron_railing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_palisade_security_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_picket_white_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_ranch_post_rail_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_retaining_wall_block` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_rustic_split_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_security_perimeter_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_security_turnstile_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_soundproof_highway_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_stone_balustrade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_stone_castle_wall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_temporary_construction_barrier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_wire_farm_fence` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bnd_showstopper_wrought_iron_estate_gate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_highend_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_luxury_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_mid_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midhigh_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_midlow_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_bascule_drawbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_bowstring_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_canal_aqueduct_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_cantilevered_lookout_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_concrete_girder_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_continuous_beam_highway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_covered_timber_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_culvert_channel_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_curved_box_girder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_double_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_fan_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_harp_cable_stayed` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_highway_overpass_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_masonry_arch_culvert` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_monumental_deck_truss` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_multi_span_concrete` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_pedestrian_flat_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_pedestrian_spiral_ramp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_pontoons_floating_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_pony_truss_river` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_rib_arch_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_rustic_log_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_signature_pedestrian_skyway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_single_girder_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_single_pylon_sundial` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_skeletal_spine_overpass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_steel_girder_span` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_stepped_canal_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_stepping_stone_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_stone_triple_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_suspension_footbridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_suspension_twin_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_through_arch_steel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_tied_arch_waterway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_tiered_aqueduct_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_timber_trestle_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_twin_mast_viaduct` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_warren_truss_rail` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_winged_swing_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `brg_showstopper_wooden_beam_bridge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_highend_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_luxury_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_mid_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midhigh_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_midlow_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_amphitheatre_shell` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_amusement_rollercoaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_animal_shelter_facility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_auditorium_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_biome_botanical_domes` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_botanical_orangery` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_cathedral_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_cemetery_chapel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_central_library_wave` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_central_terminal_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_civic_monument_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_civic_tower_clock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_community_art_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_contemporary_art_wing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_convention_expo_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_fire_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_harbor_pier_palace` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_hydro_transit_terminal` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_marina_clubhouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_municipal_courthouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_national_museum_canyon` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_observation_ferris_wheel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_observation_tower` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_olympic_aquatic_centre` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_olympic_stadium_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_opera_symphony_hall` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_parliament_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_planetarium_dome` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_plaza_grand_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_police_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_postal_headquarters` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_public_archives_rotunda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_public_pool_complex` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_scout_camp_lodge` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_skatepark_bowl` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_tennis_center_grandstand` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_town_hall_classic` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_triumphal_arch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_velodrome_arena` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `civic_showstopper_youth_recreation_center` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_highend_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_luxury_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_mid_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midhigh_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_midlow_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_ash_receptacle` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_bike_rack_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_bike_repair_station` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_brass_street_signpost` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_bronze_statue_plinth` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_bus_stop_shelter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_cafe_patio_table_chairs` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_chess_table_stools` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_curved_park_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_decorative_clock_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_digital_wayfinding_kiosk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_electric_vehicle_charger` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_emergency_phone_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_fire_hydrant_pillar` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_highmast_floodlight` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_illuminated_house_number` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_info_bulletin_board` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_modern_led_road_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_modern_waste_bin` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_outdoor_dining_parasol` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_parcel_drop_locker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_park_pergola_canopy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_parking_meter_dual` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_pedestrian_bollard_row` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_picnic_table_bench_set` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_plant_trellis_screen` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_public_barbecue_grill` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_public_telescope` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_roadside_mailbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_solar_bollard_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_stainless_trash_recycler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_stone_amphitheater_seat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_stone_drinking_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_street_vendor_cart` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_sun_lounger_deckchair` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_tiered_marble_fountain` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_traffic_stop_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_tree_surround_bench` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_victorian_street_lamp` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `fur_showstopper_wall_house_light` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_highend_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_luxury_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_mid_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midhigh_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_midlow_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_arctic_research_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_barge_flat_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_coastguard_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_commercial_trawler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_container_cargo_ship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_container_gantry_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_deepsea_submersible_tender` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_dry_cargo_coaster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_floating_drydock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_floating_fuel_dock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_floating_swim_platform` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_harbor_patrol_cutter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_harbor_pilot_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_harbor_tugboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_high_speed_catamaran` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_historic_ocean_tallship` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_hydrofoil_express` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_industrial_dredger_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_jet_ski_watercraft` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_kayak_canoe_rack` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_lobster_fishing_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_luxury_motor_yacht` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_mega_cruise_liner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_monumental_harbor_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_motor_launch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_navigational_buoy` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_ocean_observation_pier` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_ocean_oceanliner` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_offshore_supply_vessel` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_open_deck_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_paddle_riverboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_passenger_ferry_single_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_polar_icebreaker` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_rigid_inflatable_boat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_roll_on_roll_off_ferry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_rowboat_skiff` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_sailing_yacht_monohull` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_sportfishing_cruiser` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_superyacht_tri_deck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mar_showstopper_wooden_sailboat` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `mergeGeometries` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `normalizeGeometry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_highend_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_luxury_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_mid_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midhigh_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_midlow_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_90deg_road_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_alleyway_service_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_asphalt_cycle_track` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_bike_highway_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_brick_herringbone_lane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_cobblestone_plaza_crossing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_commercial_main_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_concrete_service_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_country_gravel_road` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_culdesac_turnaround` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_curved_park_drive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_curved_suburban_bend` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_curving_harbor_ring` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_diagonal_avenue_plaza` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_diagonal_turn_pocket` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_divided_highway_segment` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_expressway_interchange` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_flagstone_park_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_garden_stepping_stones` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_grand_boulevard_median` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_grand_canal_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_industrial_collector` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_monumental_roundabout` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_multi_tier_flyover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_pavers_pedestrian_walk` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_pedestrian_mall_walkway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_promenade_esplanade` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_residential_culdesac` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_standard_2lane_street` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_standard_3way_t_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_standard_4way_intersection` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_stone_arch_causeway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_suburban_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_sunken_rail_corridor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_terraced_stone_stairway` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_timber_boardwalk_path` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_tram_boulevard` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_tram_interchange_junction` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_tree_lined_avenue` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `road_showstopper_waterfront_boardwalk_run` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `tagGeometry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_highend_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_luxury_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_mid_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midhigh_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_midlow_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_ancient_oak` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_bamboo_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_blooming_sakura` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_blue_spruce` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_bonsai_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_boxwood_ball` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_coastal_redwood` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_cypress_spire` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_date_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_desert_prickly_pear` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_european_beech` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_fern_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_formal_rose_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_giant_saguaro` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_ginkgo_biloba` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_horse_chestnut` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_hydrangea_shrub` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_jacaranda` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_lavender_field` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_manicured_lawn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_maple_autumn` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_mossy_forest_boulder` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_olive_tree_grove` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_rowan_mountain_ash` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_royal_palm` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_sequoia_giant` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_silver_birch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_sunflower_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_topiary_peacock` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_topiary_spiral` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_umbrella_pine` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_urban_planter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_vineyard_grape_trellis` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_water_lily_pond_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_weeping_cherry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_weeping_willow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_wild_meadow_patch` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_wild_reed_marsh` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_wildflower_swathe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veg_showstopper_zen_rock_moss_garden` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_highend_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_luxury_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_mid_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midhigh_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_midlow_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_airport_shuttle_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_ambulance_emergency` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_armored_security_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_auto_rickshaw` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_box_delivery_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_cargo_delivery_van` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_city_bicycle_commuter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_city_transit_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_compact_crossover` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_compact_hatchback` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_concrete_mixer_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_crawler_crane` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_double_decker_tour_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_electric_articulated_bus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_electric_scooter_cluster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_executive_suv_4x4` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_family_suv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_fire_ladder_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_flatbed_cargo_hauler` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_forklift_warehouse` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_garbage_compactor_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_golf_cart_utility` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_grand_tourer_coupe` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_heavy_freight_locomotive` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_heavy_mining_dump_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_heavy_rotary_snowplow` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_high_speed_bullet_train` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_hypercar_concept` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_luxury_flagship_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_luxury_motorhome_rv` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_midsize_sedan` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_mobile_crane_transporter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_modern_city_tram` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_mopeds_scooter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_police_interceptor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_riding_mower_tractor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_semi_truck_sleeper_cab` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_street_sweeper` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_utility_pickup_truck` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `veh_showstopper_vintage_roadster` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/transform.js`

- `article` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `assessTransform` -- called from `public/run-generate-request.js`
- `bare` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `bearing` -- **UNCALLED** (imported only by test: `test/transform.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `describeTransform` -- **UNCALLED** (imported only by test: `test/transform.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `requirements` -- **UNCALLED** (imported only by tests: `test/generateRequest.test.ts`, `test/runGenerateRequest.test.ts`, `test/transform.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/typology-footprints.js`

- `CELL_M` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CLASSES_WITH_NO_TYPOLOGY_LARGE_ENOUGH` -- **UNCALLED** (imported only by test: `test/typologyFootprints.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TOWER_PLOT_CELLS_PROPOSED` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TYPOLOGIES_WITH_NO_REGISTERED_BUILDER` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TYPOLOGY_FOOTPRINT_CELLS` -- called from `public/buildings.js`
- `rangesOverlap` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `typologyFitsClass` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/undo.js`

- `undoLayer` -- called from `public/index.html`

## `public/waterways.js`

- `WATERWAYS` -- called from `public/city-plan.js`, `public/terrain.js`

## `public/workbench.js`

- `createWorkbench` -- called from `public/index.html`

## `public/world-model.js`

- `LAYER_OPS` -- **UNCALLED** (imported only by test: `test/worldModel.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `createWorldModel` -- called from `public/world.js`
- `layerFrom` -- called from `public/apply-and-persist.js`, `public/city.html`, `public/world-render-3d.js`
- `validateEdit` -- **UNCALLED** (imported only by test: `test/worldModel.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `validateLayer` -- called from `public/apply-and-persist.js`
- `worldModelFromJSON` -- **UNCALLED** (imported only by test: `test/worldModel.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/world-registry.js`

- `createWorldRegistry` -- called from `public/board.js`, `public/city-plan.js`, `public/prop-placement.js`

## `public/world-render-3d.js`

- `BUILDING_D` -- **UNCALLED** (imported only by test: `test/placementLayout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BUILDING_TYPE_SCALE` -- **UNCALLED** (imported only by test: `test/placementLayout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BUILDING_W` -- **UNCALLED** (imported only by test: `test/placementLayout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CAMERA_MAX_DIST` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CAMERA_MAX_DIST_CITY` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CAMERA_MIN_DIST` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CITY_ZONING` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `GRID_UNIT_X` -- **UNCALLED** (imported only by test: `test/placementLayout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `GRID_UNIT_Z` -- **UNCALLED** (imported only by test: `test/placementLayout.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `RENDER_TUNING` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `THREE` -- called from `public/index.html`
- `WorldRenderer` -- called from `public/index.html`, `public/world.html`
- `ZONE` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `focusDistance` -- **UNCALLED** (imported only by test: `test/focusDistance.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `renderWorld` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/world-render.js`

- `WorldRenderer` -- called from `public/index.html`, `public/world-render-3d.js`, `public/world.html`
- `sunFor` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/world-scale.js`

- `WORLD_SCALE` -- called from `public/city-plan.js`, `public/city.html`, `public/features.js`, `public/ground.js`, `public/terrain.js`, `public/world-render-3d.js`
- `sBounds` -- called from `public/city-plan.js`
- `sFields` -- called from `public/city-plan.js`, `public/terrain.js`
- `sPoint` -- called from `public/city-plan.js`
- `sm` -- called from `public/city-plan.js`, `public/city-render.js`, `public/terrain.js`
- `toDesign` -- called from `public/city-render.js`, `public/terrain.js`

## `public/world-store.js`

- `browserAdapter` -- called from `public/index.html`
- `createWorldStore` -- called from `public/index.html`
- `indexedDBAdapter` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `kvAdapter` -- **UNCALLED** (imported only by test: `test/worldStore.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `localStorageAdapter` -- **UNCALLED** (imported only by test: `test/worldStore.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `memoryAdapter` -- **UNCALLED** (imported only by tests: `test/applyAndPersist.test.ts`, `test/runGenerateRequest.test.ts`, `test/undo.test.ts`, `test/worldStore.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/world.js`

- `createWorld` -- called from `public/city-render.js`
- `worldFromJSON` -- **UNCALLED** (imported only by tests: `test/applyAndPersist.test.ts`, `test/runGenerateRequest.test.ts`, `test/undo.test.ts`, `test/world.test.ts`, `test/worldStore.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `public/zoning.js`

- `CHARACTER_SPACING` -- called from `public/city-plan.js`
- `DENSITY_BANDS` -- called from `public/city-plan.js`
- `PORT_BACKUP_DEPTH` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `RAIL_INDUSTRY_BAND` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `RAIL_PORT_REACH` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `makeZoning` -- called from `public/city-plan.js`
- `zoneCharacter` -- called from `public/city-plan.js`

## `src/attacks.ts`

- `ATTACK_PROBES` -- called from `src/index.ts`

## `src/changePipeline.ts`

- `MAX_PLAN_REPLIES` -- called from `src/index.ts`
- `SPEND_INPUT_CAP` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `SPEND_WORST_CASE` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `assertConvergedForReview` -- **UNCALLED** (imported only by test: `test/fail-open.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `assessReviewRound` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkAnswer` -- **UNCALLED** (imported only by test: `test/fail-open.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkDecision` -- **UNCALLED** (imported only by test: `test/fail-open.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkReplyBudget` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkStopped` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `decideStillFailing` -- **UNCALLED** (imported only by tests: `test/fail-open.test.ts`, `test/worldIntegrity.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `deriveHistoryReason` -- called from `src/index.ts`
- `describeFailure` -- **UNCALLED** (imported only by test: `test/evidence-forwarding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `describePassing` -- **UNCALLED** (imported only by test: `test/evidence-forwarding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `loadInstructions` -- called from `src/index.ts`
- `recordPlanDecision` -- called from `src/index.ts`
- `resolvePlanGate` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `reviewRoundsExhausted` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `runChangePipeline` -- called from `src/index.ts`
- `shouldKeepFixing` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/claude.ts`

- `ARTIFACT_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CHANGE_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `CODE_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `DEFAULT_MODEL` -- called from `src/changePipeline.ts`
- `MODELS` -- called from `src/index.ts`
- `PLAN_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `PRICING` -- called from `src/changePipeline.ts`, `src/grounding.ts`
- `RETROSPECTIVE_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `REVIEW_ASSESSMENT_SCHEMA` -- **UNCALLED** (imported only by test: `test/reviewLoop.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `STAGE_CALL_TIMEOUT_MS` -- called from `src/grounding.ts`, `src/openai.ts`
- `assessReview` -- called from `src/changePipeline.ts`
- `createWithTruncationGuard` -- called from `src/grounding.ts`
- `fixChange` -- called from `src/changePipeline.ts`
- `fixChangeAsEdit` -- called from `src/changePipeline.ts`
- `generateArtifact` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateBrief` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `generateFunctionBody` -- called from `src/index.ts`
- `generatePlan` -- called from `src/changePipeline.ts`
- `implementChange` -- called from `src/changePipeline.ts`
- `implementChangeAsEdit` -- called from `src/changePipeline.ts`
- `repairArtifact` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `repairFunctionBody` -- called from `src/index.ts`
- `runRetrospective` -- called from `src/changePipeline.ts`

## `src/clientWorkersAI.ts`

- `createWorkersAIClient` -- **UNCALLED** (imported only by tests: `test/kitbashRetrieval.test.ts`, `test/modelRetrieval.test.ts`, `test/modelRetrievalAnchor.test.ts`, `test/modelRetrievalDomain.test.ts`, `test/modelRetrievalSecurity.test.ts`, `test/planningCorpusRetrieval.test.ts`, `test/regressionGateBreak.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parseWranglerOauthToken` -- **UNCALLED** (imported only by test: `test/modelRetrievalSecurity.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/controlLayer.ts`

- `CONTROL_LIMITS` -- called from `src/changePipeline.ts`, `src/spendCounterDO.ts`
- `CircuitOpenError` -- called from `src/changePipeline.ts`
- `GENERATION_PROFILES` -- **UNCALLED** (imported only by test: `test/generationProfiles.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `PipelineLimitError` -- called from `src/changePipeline.ts`, `src/index.ts`
- `TruncatedResponseError` -- called from `src/claude.ts`, `src/openai.ts`
- `assertCircuitClosed` -- called from `src/changePipeline.ts`
- `assertUnderPipelineRateLimit` -- **UNCALLED** (imported only by test: `test/controlLayer.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `assertUnderPipelineSpendCap` -- called from `src/changePipeline.ts`
- `assertUnderRunCeiling` -- called from `src/changePipeline.ts`
- `callWithValidationRetry` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkGenerationSpend` -- **UNCALLED** (imported only by test: `test/generationProfiles.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `checkInputGuard` -- called from `src/index.ts`
- `claimPipelineRun` -- called from `src/index.ts`
- `classifyErrorPermanence` -- called from `src/changePipeline.ts`
- `getPipelineBudgetStatus` -- called from `src/index.ts`
- `getPipelineSpendStatus` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pipelineAvailability` -- called from `src/index.ts`
- `reconcilePipelineSpend` -- called from `src/changePipeline.ts`
- `recordPipelineRateLimitHit` -- **UNCALLED** (imported only by test: `test/controlLayer.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `recordProviderFailure` -- called from `src/changePipeline.ts`
- `recordProviderSuccess` -- called from `src/changePipeline.ts`
- `refundPipelineRun` -- called from `src/index.ts`
- `releaseActiveRun` -- called from `src/index.ts`
- `tryLeaseActiveRun` -- called from `src/index.ts`

## `src/criteria.ts`

- `CRITERION_SCHEMA` -- called from `src/claude.ts`
- `SIM_CALLABLE_FNS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `describeCriterion` -- called from `src/claude.ts`
- `validateProposedCriteria` -- called from `src/claude.ts`
- `validateProposedCriterion` -- **UNCALLED** (imported only by tests: `test/criteria.test.ts`, `test/gateHonesty.test.ts`, `test/plan-json-recovery.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/criteriaDryRun.ts`

- `findVacuousCriteria` -- called from `src/changePipeline.ts`

## `src/criteriaExecution.ts`

- `evaluateCriteria` -- called from `src/changePipeline.ts`, `src/criteriaDryRun.ts`
- `evaluateCriterion` -- **UNCALLED** (imported only by test: `test/criteriaExecution.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/generatedClaimChecks.ts`

- `buildingsClaimMismatch` -- **UNCALLED** (imported only by tests: `test/generatedClaimsAreCurrent.test.ts`, `test/publicClaims.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `claudeMdClaimMismatch` -- **UNCALLED** (imported only by tests: `test/claudeMdIsCurrent.test.ts`, `test/generatedClaimsAreCurrent.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `nodeTestsClaimMismatch` -- **UNCALLED** (imported only by tests: `test/generatedClaimsAreCurrent.test.ts`, `test/publicClaims.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `settlementsClaimMismatch` -- **UNCALLED** (imported only by tests: `test/generatedClaimsAreCurrent.test.ts`, `test/publicClaims.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `workerTestsClaimMismatch` -- **UNCALLED** (imported only by tests: `test/generatedClaimsAreCurrent.test.ts`, `test/publicClaims.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/grounding.ts`

- `GROUNDING_SCHEMA` -- **UNCALLED** (imported only by test: `test/schemaAudit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `GROUNDING_SYSTEM_PROMPT` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `decideGroundingOutcome` -- **UNCALLED** (imported only by test: `test/grounding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `formatGroundingForPlan` -- called from `src/changePipeline.ts`
- `formatGroundingHalt` -- **UNCALLED** (imported only by test: `test/grounding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `groundRequest` -- called from `src/changePipeline.ts`
- `groundingContextBlock` -- **UNCALLED** (imported only by test: `test/grounding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parseGroundingResponse` -- **UNCALLED** (imported only by test: `test/grounding.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/index.ts`

- `SpendCounterDO` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `default` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `liveRunsDisabledResponse` -- **UNCALLED** (imported only by test: `test/handlerRuntime.workers.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `liveRunsEnabled` -- **UNCALLED** (imported only by test: `test/handlerRuntime.workers.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/kitbashRetrieval.ts`

- `assembleFromBrief` -- **UNCALLED** (imported only by test: `test/kitbashRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildKitbashEmbeddingText` -- **UNCALLED** (imported only by test: `test/kitbashRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `findKitbashParts` -- **UNCALLED** (imported only by test: `test/kitbashRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexKitbashParts` -- **UNCALLED** (imported only by test: `test/kitbashRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/modelRetrieval.ts`

- `InMemoryVectorize` -- **UNCALLED** (imported only by tests: `test/kitbashRetrieval.test.ts`, `test/modelRetrieval.test.ts`, `test/planningCorpusRetrieval.test.ts`, `test/regressionGateBreak.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildEmbeddingText` -- **UNCALLED** (imported only by tests: `test/modelRetrieval.test.ts`, `test/modelRetrievalDomain.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `embedText` -- called from `src/kitbashRetrieval.ts`, `src/planningCorpusRetrieval.ts`
- `embedTextBatch` -- called from `src/kitbashRetrieval.ts`, `src/planningCorpusRetrieval.ts`
- `entryFitsSpace` -- **UNCALLED** (imported only by test: `test/modelRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `findModels` -- called from `src/index.ts`
- `handTunedPseudoEmbedding` -- **UNCALLED** (imported only by tests: `test/modelRetrieval.test.ts`, `test/modelRetrievalDomain.test.ts`, `test/regressionGateBreak.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `hasZeroTokenOverlap` -- **UNCALLED** (imported only by tests: `test/modelRetrieval.test.ts`, `test/modelRetrievalDomain.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexRegistryInVectorize` -- **UNCALLED** (imported only by test: `test/modelRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexRegistryWithPseudo` -- **UNCALLED** (imported only by test: `test/modelRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `lexicalSearch` -- **UNCALLED** (imported only by tests: `test/modelRetrieval.test.ts`, `test/modelRetrievalDomain.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `pseudoVectorSearch` -- **UNCALLED** (imported only by test: `test/modelRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `retrievalContentTokens` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `stemRetrievalToken` -- **UNCALLED** (imported only by test: `test/modelRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `vectorSearch` -- **UNCALLED** (imported only by tests: `test/modelRetrieval.test.ts`, `test/regressionGateBreak.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/openai.ts`

- `PRICING` -- called from `src/changePipeline.ts`
- `REVIEW_MODEL` -- called from `src/changePipeline.ts`
- `buildReReviewBlock` -- **UNCALLED** (imported only by test: `test/reviewLoop.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `costUsd` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parseFindings` -- called from `src/changePipeline.ts`
- `qaAgainstBrief` -- called from `src/changePipeline.ts`
- `resolveReview` -- **UNCALLED** (imported only by test: `test/fail-open.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `reviewArtifact` -- called from `src/changePipeline.ts`
- `reviewFollowedFormat` -- **UNCALLED** (imported only by tests: `test/fail-open.test.ts`, `test/gateHonesty.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/planningCorpusRetrieval.ts`

- `chunkPlanningSpec` -- **UNCALLED** (imported only by test: `test/planningCorpusRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `explainLayout` -- **UNCALLED** (imported only by test: `test/planningCorpusRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `indexPlanningCorpus` -- **UNCALLED** (imported only by test: `test/planningCorpusRetrieval.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/rateLimit.ts`

- `RateLimitExceededError` -- called from `src/index.ts`
- `assertUnderRateLimit` -- called from `src/index.ts`
- `recordRateLimitHit` -- called from `src/index.ts`

## `src/sandbox.ts`

- `DispatchNamespaceSandbox` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `DynamicWorkersSandbox` -- called from `src/index.ts`
- `WALL_CLOCK_ABORT_MS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `buildHarnessModule` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `default` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `runRawScriptInSandbox` -- called from `src/index.ts`

## `src/simBaseline.ts`

- `SIM_BASELINE_SOURCE` -- called from `src/changePipeline.ts`, `src/index.ts`, `src/spendCounterDO.ts`, `src/worldStructure.ts`

## `src/simRegression.ts`

- `SIM_REGRESSION_SUITE` -- called from `src/changePipeline.ts`, `src/index.ts`

## `src/simSandbox.ts`

- `buildSimHarnessModule` -- **UNCALLED** (imported only by test: `test/sandboxIntegrity.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `default` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `runSimTests` -- called from `src/changePipeline.ts`, `src/index.ts`

## `src/spendCap.ts`

- `SpendCapExceededError` -- called from `src/index.ts`
- `assertUnderCap` -- called from `src/claude.ts`
- `getCumulativeSpend` -- called from `src/index.ts`
- `recordSpend` -- called from `src/claude.ts`

## `src/spendCounterDO.ts`

- `SpendCounterLogic` -- called from `src/spendCounterDOClass.ts`
- `handleSpendCounterRequest` -- called from `src/spendCounterDOClass.ts`

## `src/spendCounterDOClass.ts`

- `SpendCounterDO` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

## `src/tasks.ts`

- `TASKS` -- called from `src/index.ts`
- `getTask` -- called from `src/index.ts`

## `src/worldEdit.ts`

- `WORLD_EDIT_SCHEMA` -- called from `src/claude.ts`
- `applyWorldEdit` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `browserOnlyReferences` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `parseRawWorldEdit` -- called from `src/claude.ts`
- `readWorldData` -- called from `src/worldStructure.ts`
- `runValidatedWorldEdit` -- called from `src/claude.ts`
- `topLevelSideEffects` -- **UNCALLED** (imported only by test: `test/sandboxIntegrity.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `validateWorldEdit` -- **UNCALLED** (imported only by test: `test/worldEdit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `worldEditPathAvailable` -- **UNCALLED** (imported only by test: `test/worldEdit.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `worldIntegrityChecks` -- called from `src/changePipeline.ts`

## `src/worldStructure.ts`

- `ACTIONS` -- **UNCALLED** (imported only by test: `test/worldStructure.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BUILDING_NAMES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `BUILDING_TYPES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `ENTITY_TYPES` -- called from `src/criteriaExecution.ts`
- `NOT_YET_PRESENT` -- **UNCALLED** (imported only by test: `test/worldStructure.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `OBJECT_TYPE_KEYS` -- **UNCALLED** (imported only by test: `test/groundingHonesty.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `OUTDOOR_OBJECT_TYPES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `STATIONS` -- **UNCALLED** (imported only by test: `test/worldStructure.test.ts`) -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `SURFACE_TYPES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `TICK_TOUCHES` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `WORLD_FIELDS` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*
- `objectTypeKeysFor` -- called from `src/criteriaExecution.ts`
- `structureSummary` -- called from `src/claude.ts`, `src/grounding.ts`
- `worldFacts` -- **UNCALLED** -- *SEEDED on the dead-export gate's first run, 2026-09-08 -- not individually reviewed. Replace this line with either a real reason this export is allowed to stand unwired, or wire/remove the export.*

