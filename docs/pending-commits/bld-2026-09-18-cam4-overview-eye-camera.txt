CAM-4: the third of CAM-1's own three cameras, finished -- the overview
page's own board view now shares CAM-2's real eye-level camera and
CAM-3's measured fog fix.

WHAT WAS WRONG: docs/briefs/BLD-2026-09-18.md named CAM-4 third in
"THE ORDER" (§1) with no dedicated section describing it anywhere in
the document -- checked directly, not assumed missing. Recorded as
genuinely undefined in this file's own prior commit rather than guessed
at, per rule://escape-clause. Scoped here from CAM-2/CAM-3's own real
findings, not invented: CAM-1's own original text named THREE cameras
assuming a compressed height range -- "the board, the contact sheet,
and the overview." CAM-2 rebuilt only the first. CAM-3's own
measurement, taken directly against overview-scene.html's own
setBoardCamera while investigating the fog, found the THIRD camera
(unchanged since CAM-1, never separately judged) is WORSE than CAM-1's
own original defect: for downtown (mega-tower-a, real storeys=84,
~376m), it computes a camera distance of ~502m, and with the SAME
uFogFar=230 CAM-3 already found wrong, that produces fogFactor=1.0 --
complete fog-out, confirmed by rendering: the tower appeared as a flat,
detail-less tan silhouette against a mismatched dark background, not
merely hazy.

THE FIX (public/overview-scene.html): setBoardCamera now takes the real
resolved pieces (buildBoardMesh stashes them on mesh.userData.resolved,
board-renderer.js's own resolveBoardPieces -- never a guessed height)
and builds the SAME eye camera CAM-2 built on look-proof-scene.html
(eyeCameraFromState, ported directly -- these two files share no module
to import it from, and building one was outside this item's own
budget). Widened FOV (45->65) on entering a board, reset back to 45 on
returning to overview -- a real gap CAM-2 did not have to handle, since
look-proof-scene.html never leaves BOARD_MODE mid-session. An area with
no real pieces yet stands at the board's own southern edge facing the
plot, the only sensible default with no real piece to anchor on. CAM-3's
own measured fix ported too: material.uniforms.uFogFar.value = 500,
scoped so the overview's own separate pad/ground MeshLambertMaterial is
never touched. Drag-to-look and wheel-to-pan-out wired here too, the
SAME click-vs-drag gating CAM-2 already proved (pointerdown starts a
drag-state, pointerup decides click-vs-drag).

FOUND BY RUNNING IT, NOT ASSUMED: unlike CAM-2's own first attempt on
look-proof-scene.html, the interactive driver's own existing test cell
(6,6) on the "hills" area kept working unchanged -- hills' own board is
small, house-a is the only piece, and the new eye anchor sits close
enough that (6,6) stays on screen. Checked directly rather than assumed
safe by analogy to CAM-2's own experience.

TESTS: test/overviewScene.test.ts -- CAM-1's own exact-formula test
retired (its own removal is now itself a gate, matching CAM-2's own
pattern). 8 new GATE (CAM-4) tests: setBoardCamera takes the real
resolved pieces and finds the tallest one the same way CAM-2 does;
buildBoardMesh exposes resolved on the mesh; the FOV widens and resets;
the fog fix is ported; drag-vs-click gating (the exact conditional
line, not just the threshold constant's own existence) and the wheel
listener. 32/32 pass. npx tsc --noEmit clean. Full suite: 1182 pass /
13 pre-existing unrelated fails, the same categories confirmed at
CAM-2/CAM-3's own baseline, no new ones.

MUTATED: node scripts/_mutcheck.mjs -- cam4-overview-setboardcamera-
must-take-resolved (renamed from cam1-overview-camera-must-stay-height-
aware, which went INCONCLUSIVE once CAM-4 replaced its own retired
maxHeight-param signature; retired and retargeted per rule://build-loop,
the same pattern GRD-1's own fix3-board-paving-radius retirement
already used), cam4-fogfar-must-stay-widened, cam4-tallest-piece-must-
stay-real all CAUGHT cleanly. cam4-drag-must-stay-gated-on-real-movement
SURVIVED on a first pass -- the test checked DRAG_THRESHOLD_PX's own
existence and the wheel listener but never the actual conditional that
USES the threshold, so mutating the check away unconditionally left
every checked pattern still present. Fixed the TEST, not the code, per
rule://build-loop ("a mutation SURVIVED means the test is wrong"),
re-ran: CAUGHT. All CAUGHT against a GREEN baseline, source restored
byte-identical.

LIVE, NOT JUST STATIC: scripts/interact-overview-scene.mjs extended
with the SAME drag/wheel checks scripts/interact-look-proof.mjs already
proved for CAM-2 -- a real drag changed live yaw/pitch and did NOT
place a piece; a real wheel event increased live dolly. "RC2/SHIP-1
GATE: pass".

MEASURED, RENDERED, LOOKED AT -- both shots carry their own five lines
per docs/briefs/BLD-2026-09-17.md §5.5, full text in CHECKLIST.md's own
CAM-4 entry: docs/look-proof-shots/49-cam4-overview-downtown-default.png
(the SAME area/piece CAM-3's own scoping measurement found rendering as
a flat tan silhouette -- now shows real deep-blue facade colour, real
window/trim detail, real ground grain at the base). docs/look-proof-
shots/48-cam4-overview-eye-panned.png (hills, mid-drag-and-wheel
sequence, produced by the interactive driver itself, not posed) -- shows
RDO-1's own live readout and ghost marker still rendering correctly
through a real drag/pan, real corroborating evidence this integrates
cleanly with SHIP-1's already-proven systems.

Disclosed, not fixed here: this page's own ground is still the plain,
un-paved earth texture -- GRD-1's own paving/kerb work was scoped to
look-proof-scene.html only, and porting it here is a real, separate,
un-scoped gap, named rather than silently left for someone to
rediscover.

This completes CAM-2/CAM-3/CAM-4, all three of CAM-1's own originally-
named cameras now sharing the real eye-level design and the real
measured fog range.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_012fvsDWLyvbN5Dj6csGYvbA
