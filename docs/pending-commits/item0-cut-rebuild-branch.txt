Item 0 of the Phase 1 rebuild lane brief -- cut the `rebuild` branch from
`b1-land` and commit the brief plus the plan it points to.

What was wrong: docs/specs/REBUILD-PLAN.md -- the document
docs/briefs/PHASE-1-rebuild.md's own first instruction says to read before
anything else -- was sitting untracked. A brief that tells its reader to read
a file first should not point at a file with no commit behind it.

How it is known to be fixed: `git switch -c rebuild` from `b1-land` at 46f0ec9
(confirmed by `git branch -vv`: both branches point at 46f0ec9 after the
switch, so b1-land did not move). `b1-land`'s tracked content was already
clean -- only untracked additions were present, no modified tracked file --
which is the part of item 0's "confirm b1-land is unchanged" that is checked
by the tree rather than assumed.

Re-grounded against a stale assumption in the brief: item 0 also says to
confirm b1-land "still matches origin". It does not -- b1-land is 24 commits
ahead of origin/b1-land, all prior autonomous-run work from this same lane,
already committed and gated on its own terms. Per rule://build-loop step 1,
the tree wins over the brief's assumption; recorded here rather than treated
as a blocker, since nothing about this item depends on b1-land matching
origin, only on b1-land being unmoved and its tracked tree clean, which it is.

Left alone, out of scope for this item: docs/briefs/CLI-2026-09-11-clear-the-site.md
(an earlier, superseded draft brief for the same clearing work, never
committed -- left untracked rather than deleted, per rule://quarantine's
"nothing is deleted" applying to draft files too), the seven untracked
docs/pending-commits/*.txt files from the prior autonomous run (orphaned
commit-message source files, already superseded by the commits they
produced), and the two root-level *.mjs measurement scripts (explicitly
marked "not committed" in their own header comments).

What is still open: everything else in the Phase 1 checklist -- the site
inventory (item 1) has not started.
