ITEM 0 -- regenerate the board, close the boardLoad red gate f01a324 left
open, and run the full suite once to find what else that commit moved.

WHAT WAS WRONG: f01a324 added roadClass to every road piece generateBoard()
produces but never regenerated the committed public/board.generated.json,
so test/boardLoad.test.ts's "committed board.generated.json matches the
seed it claims" gate failed a deep-equal comparison on every road piece.
Named in docs/specs/PIECE-CATALOGUE-ROADS.md's own §9 callout; fixed here.

HOW IT IS KNOWN TO BE FIXED: node scripts/gen-board.mjs regenerated the
committed asset (35,365 pieces, 42.9s; B2.7 crossings 16 pieces/0 bridges/
16 docks/6 refused, 0.1s -- unchanged from before, since ROAD_WIDTH is
still untouched, this is metadata-only). node test/run.mjs
test/boardLoad.test.ts: 7/7 GREEN, including both previously-red gates.

THE FULL SUITE, RUN ONCE, PER INSTRUCTION: node test/run.mjs (whole suite)
-- 1213 tests, 1149 pass, 48 fail, 15 skip, 1 todo, ~20 min (B2.5 alone
took 131,965ms this run against this session's earlier 39.6-39.8s readings
-- real, measured host memory-pressure variance). Compared against
test/testCount.generated.json's own pre-session record (1212/1150/46):
exactly TWO tests flipped from pass to fail, both in
test/mutationEvidence.test.ts. Traced to root cause, not guessed: f01a324
added a real, _mutcheck.mjs-verified mutation to test/mutations.json (134
entries) without a matching record in the committed
test/mutationSummary.generated.json (still 133) -- because the
authoritative scripts/mutate.mjs path that would record it cannot
currently run at all (47 unrelated baseline failures per
scripts/expected-red.mjs, PLUS a real, previously-unnamed regex bug in
mutate.mjs's own title-extraction that makes even ITS one allowlisted entry
register as unexpected). Full account: docs/DECISIONS-FOR-MARK.md #10 (new
this commit). No other test among the 46 pre-existing failures moved --
confirmed by the arithmetic, not assumed.

LEFT RED, NAMED, NOT WORKED AROUND: test/mutationEvidence.test.ts's two
sub-tests. Fixing them requires either a fix to mutate.mjs's own regex (a
real, separate, small task) or resolving 47 unrelated pre-existing
failures -- both out of scope for this item. Every subsequent
PIECE-CATALOGUE-ROADS.md §9 step tonight will use scripts/_mutcheck.mjs
(the BUILD-LOOP.md STEP 6-prescribed tool) and will add further neverRun
entries to this same gate -- named in advance in decision #10 so it is not
read as a new surprise each time.

GUARDS CHECKED: derived artefact regenerated by its own generator, not
hand-edited. Nothing deleted (a scratch counting script was moved to
_TO-DELETE/session-scratch-scripts/, never committed). Zero API spend. No
merge, no deploy, no PR.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01BPxmE9Xkfsd13S2rxiXEqf
