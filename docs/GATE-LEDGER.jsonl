{"project":"C:\\Code\\sandbox-spike","item":"C4","redEvidence":"node test/run.mjs test/publicClaims.test.ts: \"the test counts on the page are the test counts\" FAILS -- AssertionError \"page says 1087, generated record says 1212 -- run node scripts/gen-test-count.mjs\" (reproduced both before and after regeneration; the drift persists by design per DECISIONS-FOR-MARK.md #9, page deliberately not updated while 46 other node tests are red).","greenEvidence":"Same test, with public/index.html's claim temporarily set to 1212 (matching the freshly regenerated test/testCount.generated.json): node test/run.mjs test/publicClaims.test.ts -- 7/7 pass, \"the test counts on the page are the test counts\" GREEN. Proves the equality+floor check is genuinely sensitive to agreement, not vacuously red. Reverted via git checkout -- public/index.html, confirmed byte-identical.","commit":"64ee904","actor":"lane","recordedAt":"2026-09-11T01:42:34.252Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-generatedClaimsAreCurrent","redEvidence":"Static GATE test (\"checkAllGeneratedClaims's own INDEX read is actually wired through stripHtmlComments\") failed before the fix: asserted this file's own stripped source matches /const INDEX = stripHtmlComments\\(readFileSync\\(...\\)\\);/ and got the unwrapped bundled .mjs output instead -- 3 fail/8 pass. Separately, spanText(commentedOut, \"city-stat-buildings\") on raw HTML returns \"17108\" (the underlying vulnerability), confirmed live.","greenEvidence":"After wrapping INDEX with stripHtmlComments(readFileSync(...)): the static GATE test passes; stripHtmlComments(commentedOut) then spanText returns null. Full file: 9/11 pass (the 2 remaining failures are pre-existing published-claim drift, already classified brought-by-merge, unaffected by this change). rawSourceScan.test.ts 1/1 after removing the now-stale REVIEWED_EXCLUSIONS entry. tsc --noEmit clean.","commit":"568b88c","actor":"lane","recordedAt":"2026-09-11T19:24:58.246Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-reachability","redEvidence":"With isRedirectStubText's strip temporarily replaced with a no-op, both new vulnerability tests failed: a comment \"<!-- old version used to do location.replace('/x') -->\" and a JS comment \"// location.replace('/x')\" inside a real <script> block each caused isRedirectStubText to return true (falsely detected as a redirect stub) when it should be false -- 2 fail/7 pass.","greenEvidence":"After restoring stripHtmlComments(stripSourceComments(html)): both vulnerability tests pass (comment-only mentions correctly return false); both real-redirect tests still pass (location.replace( and refresh meta tag correctly return true after stripping); public/plan-preview.html, the one real stub, still correctly detected. Full file 9/9. rawSourceScan.test.ts 1/1 after removing the stale REVIEWED_EXCLUSIONS entry. tsc --noEmit clean.","commit":"28e1331","actor":"lane","recordedAt":"2026-09-11T19:29:28.659Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-thinkingDisabled","redEvidence":"Before stripping: findMessagesCreateCallsWithoutThinking() on a messages.create call missing a real thinking: setting, but containing the comment /* thinking: handled elsewhere, TODO verify */ inside its own arguments, returned [] (0 issues) instead of flagging it -- 1 fail/5 pass.","greenEvidence":"After stripping comments from source at the top of the function: the same fixture now returns exactly 1 issue. Full file 6/6. Real scan against src/ and scripts/ (7 createWithTruncationGuard sites + 1 direct messages.create call) unaffected -- none relies on a comment, confirmed by blind review. rawSourceScan.test.ts 1/1 after removing the stale exclusion. tsc --noEmit clean.","commit":"90a7bf5","actor":"lane","recordedAt":"2026-09-11T19:33:04.619Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-workerFirstRouting","redEvidence":"Before stripping: extractWorkerRoutes on a fixture with one real route plus a route mentioned only in a \"used to check ...\" comment returned both [\"/real-route\", \"/old-phantom-route\"] instead of just the real one -- 1 fail/4 pass.","greenEvidence":"After stripping comments from indexTsSource before matching: the phantom is no longer extracted, only [\"/real-route\"]; a separate test confirms a real uncommented route still extracts correctly. Full file 5/5. Blind review confirmed all 26 real routes in src/index.ts are unaffected (26 before, 26 after). rawSourceScan.test.ts 1/1 after removing the stale exclusion. tsc --noEmit clean.","commit":"9c338ff","actor":"lane","recordedAt":"2026-09-11T19:35:55.003Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-rendererStatic","redEvidence":"With countRendererLogDepthGap's strip temporarily replaced with a no-op: a fixture with one real WebGLRenderer construction missing its real flag, plus a comment elsewhere mentioning \"logarithmicDepthBuffer: true\", reported flagged (1) not less than constructions (1) -- the vulnerability test failed, confirming the comment masked the real regression.","greenEvidence":"After restoring stripHtmlComments(stripSourceComments(rawSrc)): the same fixture reports flagged(0) < constructions(1), correctly caught. A second test confirms a genuinely correct file (real flag, no phantom comment) still reports clean. A static GATE test confirms the real per-file check is wired to the same helper, not duplicating logic. Full file 6/6. Blind review confirmed real counts in world-render-3d.js and city.html (1 construction, 1 flag each, both real code) are unaffected. rawSourceScan.test.ts 1/1 after removing the stale exclusion. tsc --noEmit clean.","commit":"bc5a117","actor":"lane","recordedAt":"2026-09-11T19:39:14.812Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"rawSourceScan-boardGenerator-terrainLandmassOwnership","redEvidence":"Before stripping: boardGenerator.test.ts's fake sliced region with a comment claiming halfRoadFor(/roadWidthFor( were called (but no real call) matched /halfRoadFor\\(/ (confirmed true, the vulnerability). terrainLandmassOwnership.test.ts's fake source with only a comment mentioning \"export function landmassPolygonsDesign(\" matched the required pattern (confirmed true, the vulnerability).","greenEvidence":"After wrapping each test file's own source read in stripSourceComments(): both mutation tests confirm the comment-only mention no longer matches, while a real call/export still does. boardGenerator.test.ts 14/15 (1 pre-existing unrelated B2.5 CPU-gate failure, caliper-bld #3); terrainLandmassOwnership.test.ts 5/5; rawSourceScan.test.ts 1/1 after removing both stale exclusions. tsc --noEmit clean. No edits to public/board-generator.js or public/terrain.js (world source) were needed.","commit":"332c214","actor":"lane","recordedAt":"2026-09-11T19:44:08.359Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"K7.1","redEvidence":"Never looked at: docs/pending-commits/k7-1-trim-atlas-patch.txt's own text says \"this machine's free memory did not clear the 4 GB floor... no render was attempted\" -- the patch was code-verified only (UV region exists, grain in the source canvas, per-vertex sampling proven non-collapsed) with the visual claim explicitly stated as open and untested.","greenEvidence":"node scripts/shoot.mjs \"In the street\" \"Downtown corner\" -- two real renders, no console errors. Both show a textured, lighter cornice band at the roofline distinct from the flat brick wall and flat roof, plus a horizontal string-course band between floors on the first -- the reserved grained trim patch reading as intended, on two separate buildings in two separate renders. \"Street level\"/\"Downtown close\" were also rendered but too distant/oddly framed to resolve trim detail, reported honestly rather than counted as confirmation.","commit":"eb863ee","actor":"lane","recordedAt":"2026-09-11T22:35:41.718Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"B6-touch-action","redEvidence":"Mutation 1: commented out public/world-render-3d.js:6373's real `canvas.style.touchAction = \"none\";` (leaving a comment mentioning it) -- e2e test \"world-canvas disables default touch gestures...\" failed: Expected \"none\", Received \"auto\". Mutation 2: added `touch-action: none;` to public/index.html's shared #world-canvas,#world-canvas-2d CSS rule (the naive fix a first draft of this plan almost shipped) -- e2e test \"world-canvas-2d has no gesture JS of its own...\" failed, canvas2d wrongly reporting \"none\".","greenEvidence":"Both source mutations reverted (confirmed via git diff --stat showing no changes). Both e2e tests re-run in isolation, both pass: canvas3d computed touchAction \"none\" (supplied by _bindOrbitControls' inline style, not CSS), canvas2d computed touchAction \"auto\" (correct -- it has zero gesture listeners of its own, confirmed by full-file grep of public/world-render.js).","commit":"PENDING","actor":"lane","recordedAt":"2026-09-11T23:07:13.928Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"B6-touch-action (commit correction)","redEvidence":"Same as the prior B6-touch-action entry -- recorded again only because that entry's commit field was mistakenly written as the placeholder \"PENDING\" (the gate was recorded before the commit existed). This entry exists to carry the real hash; the prior entry's red/green evidence stands unchanged.","greenEvidence":"Same as the prior B6-touch-action entry -- see commit 88a752a's own message for the full account: both mutations watched red, both reverted and re-confirmed green in isolation.","commit":"88a752a","actor":"lane","recordedAt":"2026-09-11T23:07:40.882Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"wedge-wheel-screenshot","redEvidence":"Never verified with panels closed: docs/audits/U4-NAV-WHEEL.md's own prior text says the one confirmed-good render was seen \"against a page with the 'How it works' welcome content still open behind it... not the clean comparison frame Mark asked for\" -- and #tour-toggle-btn alone did not clear panels in a prior attempt.","greenEvidence":".shots/nav-wheel-open.png (reproducible, gitignored): real ring, 6 wedge divisions, legible labels (Orbit/Pan/Zoom/Focus/North/Rewind), world rendering correctly, no panel in frame. #tour-toggle-btn confirmed still insufficient even against the real renderer (matching the prior session's suspicion); direct #welcome-close-btn + dismiss-every-cross-mark sweep worked. State confirmed past pixels: #nav-wheel-hub aria-expanded=\"true\", .nav-wheel classList [\"nav-wheel\",\"open\"], 6 .nav-wheel-seg elements present.","commit":"33e7347","actor":"lane","recordedAt":"2026-09-11T23:22:20.377Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"ledger-tracked","redEvidence":"With .gitignore's old `_TO-DELETE/` rule (the whole directory ignored as one atomic unit): git status --short showed nothing for the ledger's real first entry (the 45,175,510-byte .wrangler/tmp purge). git check-ignore -v _TO-DELETE/LEDGER.jsonl confirmed the match: .gitignore:17:_TO-DELETE/.","greenEvidence":"With .gitignore changed to `_TO-DELETE/*` plus `!_TO-DELETE/LEDGER.jsonl`: git status --short shows `?? _TO-DELETE/LEDGER.jsonl`. Separately confirmed quarantine CONTENTS still stay ignored: a scratch file written to a test quarantine subdirectory does not appear in git status, matched by the _TO-DELETE/* rule.","commit":"e5a80ac","actor":"lane","recordedAt":"2026-09-12T00:51:47.905Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"ledger-tracked (anchoring regression fix)","redEvidence":"Immediately after commit e5a80ac (`_TO-DELETE/*`, a slash in the middle, anchors the pattern to the repo root): git status --short surfaced `?? test/_TO-DELETE/` -- test/mutate.test.ts's own nested scratch-backup directory (same name, different location) was no longer ignored, a real regression against the original unanchored `_TO-DELETE/` pattern. Confirmed directly: temporarily restoring the pre-e5a80ac .gitignore and re-running git check-ignore against the same nested file matched (`.gitignore:17:_TO-DELETE/`); the post-e5a80ac version did not.","greenEvidence":"After changing to `**/_TO-DELETE/*` (contents, any depth) plus `!/_TO-DELETE/LEDGER.jsonl` (root-anchored negation): git status --short no longer lists test/_TO-DELETE/ at all (git check-ignore confirms the match: .gitignore:32:**/_TO-DELETE/*); _TO-DELETE/LEDGER.jsonl remains tracked (git ls-files confirms); a scratch file written to a root _TO-DELETE/ subdirectory stays ignored. All three properties hold simultaneously.","commit":"7d1a9a0","actor":"lane","recordedAt":"2026-09-12T00:56:27.184Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"dev-lan-off-loopback","redEvidence":"Before this script existed: wrangler binds 127.0.0.1 by default and no --ip was configured anywhere in this repo (package.json's dev script, playwright.config.ts's webServer command) -- confirmed in the prior wrangler-deadlock diagnosis (docs/audits/WRANGLER-DEADLOCK-2026-09-11.md), where the only running instance's own workerd.exe command line showed --socket-addr=entry=127.0.0.1:8787.","greenEvidence":"PORT=8788 node scripts/dev-lan.mjs started successfully, resolved the real LAN address via os.networkInterfaces(), and printed \"Binding wrangler dev to 10.88.111.5:8788\". Independently confirmed: Get-NetTCPConnection -LocalPort 8788 shows LocalAddress 10.88.111.5 (not 127.0.0.1); workerd.exe's own command line shows --socket-addr=entry=10.88.111.5:8788. The pre-existing loopback-bound instance on 8787 (PID 28908) was left running and untouched throughout -- confirmed serving HTTP 200 afterward.","commit":"d315f8e","actor":"lane","recordedAt":"2026-09-12T00:57:05.849Z"}
{"project":"C:\\Code\\sandbox-spike","item":"R3.5-plan-correction-and-push","redEvidence":"Before this commit, docs/specs/COMPLETION-PLAN.md's MINIMUM SENDABLE CUT read \"the archipelago renders\" -- a sentence B3 alone satisfies while the board stays gated off (?board=1 required) for every real visitor; no R3.5 line existed naming the two perf gates (culling 65.8% vs <40%, 7,851 draw calls vs <=900) blocking default-on rendering. b1-land was 31 commits ahead of origin/b1-land with none of tonight's work pushed.","greenEvidence":"git log origin/b1-land -1 shows 2aea5c669e3a3fce6904493184de8fcda55da119, matching local HEAD exactly. COMPLETION-PLAN.md now has R3.5 in PART 4, a pointer from R4, and the MINIMUM SENDABLE CUT corrected to name R3.5 explicitly. Numbers verified against PART 1's own existing B3 line (exact match) before committing.","commit":"2aea5c669e3a3fce6904493184de8fcda55da119","actor":"lane","recordedAt":"2026-09-11T19:25:50.617Z"}
{"project":"C:\\Code\\sandbox-spike","item":"decision-10-mutate-regex-fix","redEvidence":"scripts/_mutcheck.mjs test/extractTestTitles.test.ts scripts/extract-test-titles.mjs test/mutations.json with the mutation applied (regex reverted to the buggy (?: \\\\(\\\\d|$) pattern): 4 of 6 tests failed, each on the same defect -- a title with an early parenthetical-containing-digit (e.g. B2.5's own '...ceiling (30,000 ms, wrangler.jsonc has no override)') was truncated before the trailing duration instead of captured whole.","greenEvidence":"Same command against the real, fixed source: baseline GREEN, mutation 'decision-10-extract-test-titles-anchors-on-trailing-duration' CAUGHT, source restored byte-identical. Separately: node test/run.mjs against extractTestTitles/expectedRed/mutateResume/mutateLock/claimSpansAreChecked -- 21/21 pass; re-ran _mutcheck.mjs against board-generator.js's own six existing mutations post-swap -- all six still CAUGHT, confirming no regression to _mutcheck.mjs's own already-working behavior.","commit":"a69027aea8ee58528d1056e6b9c3c87009d0062d","actor":"lane","recordedAt":"2026-09-11T19:47:56.978Z"}
{"project":"C:\\Code\\sandbox-spike","item":"C1-wooded-and-mainland-boundary-rerun","redEvidence":"Neither b2-settlement-table-wooded-exclusion-real nor b2-mainland-boundary-inland-direction-real appears anywhere in test/.mutate-results.json -- confirmed by direct lookup -- meaning neither mutation had ever been recorded through a tool, only \"verified by hand\" per their own prior note text.","greenEvidence":"node scripts/_mutcheck.mjs test/boardGenerator.test.ts public/board-generator.js test/mutations.json: baseline GREEN, both mutations CAUGHT, source restored byte-identical. Confirmed decision #10's regex fix does not change their scoring -- neither mutation's own expect string contains a parenthetical-with-a-digit, and _mutcheck.mjs's own extraction was already correct before that fix.","commit":"da0e139380c541d4b3a81b319101ec41d3b99b85","actor":"lane","recordedAt":"2026-09-11T19:57:49.242Z"}
{"project":"C:\\Code\\sandbox-spike","item":"R3-red-reconciliation","redEvidence":"Before this commit, no single document classified b1-land's 46 real failures -- two stale, unreconciled numbers were in circulation (46 measured earlier on b1-land, 7 on codex-lane), neither verified against this tree. A failure with no class beside it in a would-be reconciliation document is this item's own definition of RED.","greenEvidence":"docs/specs/R3-RED-RECONCILIATION.md: all 46 failures measured today (node test/run.mjs, redirected straight to disk) are classed -- 5 Class A (decision-numbered), 3 Class B (generated-claims staleness), 37 Class C (COMPLETION-PLAN.md's own B2.8 line, verified per-file), 0 Class D (genuinely new -- confirmed by diff against this session's own earlier run), 1 Class E (unknown-and-untraced, used honestly). Sum 5+3+37+0+1=46, matching the measured total exactly.","commit":"de78c6d02de28ca8df68d7b359fd0c2968de0f6f","actor":"lane","recordedAt":"2026-09-11T20:13:58.326Z"}
{"project":"C:\\Code\\sandbox-spike","item":"B4-2a-2b","redEvidence":"Watched red twice: (1) all 6 new/rewritten boardRender.test.ts + boardPicking.test.ts tests failed against pre-2b (post-2a) code, exactly and only the new/rewritten ones, before implementing grouping. (2) A real code mutation (per-instance transform loop reading the group's sample piece instead of the actual piece) turned the position-decode, bridge-height, and picking tests red; reverted and confirmed byte-identical after.","greenEvidence":"node test/run.mjs test/boardRender.test.ts test/boardPicking.test.ts -- 36/36 green, repeated after every edit. npx tsc --noEmit clean. Two partial full-suite runs (stopped by an external governor, not completed) both passed every board-related test (B2.6, B2.7, B3, B4 2a/2b, both static ?board=1 gates, boardPicking); only failures seen in either were pre-existing, self-documented, unrelated (B2.5 CPU-ceiling measurement, decision #7's named-RED gate).","commit":"266957b","actor":"lane","recordedAt":"2026-09-11T23:21:25.622Z"}
{"project":"C:\\Code\\sandbox-spike","item":"B4-2c","redEvidence":"Watched red three times, each a distinct real bug shape: (1) all 20 new/rewritten boardRender.test.ts assertions failed against pre-2c code, exactly those 20. (2) A mutation reproducing the blind review's own found bug (cap check and bench/bin alternation reading a stale count) was caught by exactly the tests naming those bugs. (3) A mutation in the shared helper reading the group's first item's position for every instance was NOT caught by any existing test (every position test used a single-item fixture) -- a new two-lamps-one-group test was added, watched red against this exact mutation, confirmed it is the only test that catches it.","greenEvidence":"node test/run.mjs test/boardRender.test.ts test/boardPicking.test.ts -- 39/39 green. npx tsc --noEmit clean. Full suite completed: 1230 tests, 1167 pass, 47 fail -- the one failure beyond R3's documented 46-failure baseline is cullingRatio.test.ts, confirmed via grep to be absent from R3-RED-RECONCILIATION.md despite being independently confirmed failing this same session (before any board-render.js work) by R3.5's own remeasurement -- a pre-existing gap in that document, not a regression. Every board/prop/picking test in the full run passed.","commit":"fc56d36","actor":"lane","recordedAt":"2026-09-11T23:59:04.318Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"look-proof-4.1-shared-material","redEvidence":"Two real regressions, each mutated and watched red in isolation (node test/run.mjs test/lookProofScene.test.ts): (1) removing glslVersion: THREE.GLSL3 from look-proof-material.js -- CAUGHT, matching this project's own first real render, which was a blank white canvas with zero console errors and draw calls: 1, triangles: 1270 (green on every measure available without a GPU, drawing nothing). (2) removing preserveDrawingBuffer: true from look-proof-scene.html's WebGLRenderer -- CAUGHT.","greenEvidence":"Both source mutations reverted (diff confirmed byte-identical against the pre-mutation file). node test/run.mjs test/lookProofScene.test.ts: 8/8 pass. node scripts/shoot-look-proof.mjs 01-baseline-material: real render, docs/look-proof-shots/01-baseline-material.png -- 4 real CC0 pieces (house, mid-rise, tower base, street tile) plus a grass ground plane, one shared DataArrayTexture material, draw calls: 1, triangles: 1270. Two different packs (Kenney Modular Buildings, Kenney City Kit Roads) both sample the same array texture in the same draw call, each pack's own albedo intact (no palette-atlas remap).","commit":"PENDING","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T06:20:00.000Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"look-proof-4.1-shared-material (commit correction)","redEvidence":"Same as the prior look-proof-4.1-shared-material entry -- recorded again only because that entry's commit field was written as the placeholder \"PENDING\" (the gate was recorded before the commit existed). This entry carries the real hash; the prior entry's red/green evidence stands unchanged.","greenEvidence":"Same as the prior entry -- see commit 99e0a8c8f95df78f4286aebfe9f7d1129e99f263's own message for the full account.","commit":"99e0a8c8f95df78f4286aebfe9f7d1129e99f263","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T06:25:00.000Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"L11","redEvidence":"Mutated shadowFactor out of the direct-light term (lightColor * lambert * shadowFactor -&gt; lightColor * lambert), watched test/lookProofScene.test.ts's own L11 check fail naming exactly that line. Before the mechanism existed at all: 07-join-decal.png (and every predecessor) shows zero cast shadows -- the house casts nothing onto the road tile, the tower's own recessed roof lid shows no self-shadowing.","greenEvidence":"Mutation reverted, diff-confirmed against the pre-mutation file (a git-checkout accident mid-step discarded the whole file once; reconstructed verbatim from this session's own transcript and re-verified against both the test suite and a fresh render before trusting it). node test/run.mjs test/lookProofScene.test.ts: 18/18 pass. node scripts/shoot-look-proof.mjs 08-cast-shadows: draw calls: 1, triangles: 12068, shadow-map cost measured at runtime (1024x1024x4 bytes = 4.00 MiB, shadow-pass render ~8.5-9.1ms, main-pass render ~5.5-6.8ms via performance.now()). docs/look-proof-shots/08-cast-shadows.png shows a real cast shadow from the house across the road tile and self-shadowing in the tower's roof recess.","commit":"08dc08f","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T08:52:57.631Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"L12","redEvidence":"Mutated the mega-tower's height cap out (Math.min((sx+sz)/2, 6) -> (sx+sz)/2), watched test/lookProofScene.test.ts's own L12 check fail naming exactly that line. Before the cap existed: the first uncapped render produced an approximately 87 m tower (an 8x8/32 m footprint's own 16x horizontal scale applied to height too) that dominated and misframed the whole shot, confirmed by looking at the actual image, not assumed from the arithmetic.","greenEvidence":"Mutation reverted via cp (not git checkout --, after that command discarded uncommitted work once already this session), diff-confirmed byte-identical. node test/run.mjs test/lookProofScene.test.ts: 23/23 pass. node scripts/shoot-look-proof.mjs 09-piece-count-20: draw calls: 1 (unchanged from the 4-piece scene), triangles: 32355 (up from 1270), zero console errors -- RED would have been draw calls rising past 1, and it did not. Shadow-map cost re-measured at this piece count: 4.00 MiB (fixed, unchanged, confirming L11's own prediction), shadow-pass render ~10.4-11.8ms (up from ~8.5-9.1ms at 4 pieces, scaling with triangle count as predicted).","commit":"312fb7d","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T09:07:40.540Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"I1","redEvidence":"Mutated the uCastShadows disable out (colorMaterial.uniforms.uCastShadows.value = false removed), watched test/impostorBake.test.ts's own I1 check fail naming exactly that line. Before the fix existed at all: the colour atlas rendered blank white with zero console errors -- confirmed by reading the actual PNG, not by trusting the reported stats.json (which computed a plausible byte count regardless of whether anything was drawn). Separately, before the per-frame renderer.setSize fix: capturing the live canvas mid-bake showed a real house silhouette in one small corner and a smeared, noisy accumulation across the rest of the 1024x1024 canvas -- the viewport/scissor corner-reuse approach only cleared its own small scissor rect each frame, so the compositing step copied garbage for most of the 64 angles.","greenEvidence":"Both mutations reverted via cp + diff-confirmed byte-identical (not git checkout --, after that command discarded uncommitted work once already this session). node test/run.mjs test/impostorBake.test.ts: 6/6 pass. node scripts/bake-octahedral-impostor.mjs: 64 angles, 3 passes, 192 real renders, colour/normal atlases show correct, distinct content (docs/look-proof-shots/impostor-bake/*.png) -- the depth atlas has real banding artifacts (RGBADepthPacking precision loss under SwiftShader, not resolved after tightening near/far to the object's bounding sphere), reported honestly rather than hidden. Texture memory measured two ways: 4.00 MiB per atlas uncompressed (12.00 MiB total, computed from real grid/cell size), and real on-disk PNG bytes via fs.statSync -- colour 416,654 B, normal 96,612 B, depth 614,872 B (the banding inflates its own compressed size), 1.08 MiB total.","commit":"b7f5e82","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T09:17:53.906Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"I2","redEvidence":"Mutated the box height to a hardcoded constant (10) in place of the real measured realHeight, watched test/lookProofScene.test.ts's own I2 check fail naming exactly that line -- a massing bake that used a guessed constant height per piece would misrepresent 'what is actually built there', W4's own wording.","greenEvidence":"Mutation reverted via cp, diff-confirmed byte-identical. node test/run.mjs test/lookProofScene.test.ts: 25/25 pass. node test/run.mjs test/reachability.test.ts: 7/7 pass. node scripts/shoot-look-proof.mjs 10-overview-massing overview-massing-scene.html: 242 triangles versus L12's own measured 32355 at full detail -- a 99.25% reduction, draw calls unchanged at 1. Real per-piece heights measured from each piece's own loaded geometry (not guessed from footprint class): mega-tower-8x8 26.88m, tower-base-6x6 18.83-22.58m, street tiles 0.12m. docs/look-proof-shots/10-overview-massing.png shows correct, recognisable massed silhouettes matching the detailed scene's own layout.","commit":"7f6633f","actor":"lane (codex-lane, BLD)","recordedAt":"2026-09-14T09:23:33.425Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"N1","redEvidence":"Before: 08-cast-shadows.png -- black void background (flat THREE.Color(0x0b1016)), ground plane ends at a visible hard edge within frame, no kerb/path/prop at the ground-road-building junction. Attempt-1 regressions caught before shipping: N1b's first far-ground render aliased into a visible checkerboard moire near the horizon (no mipmaps on the array texture); N1c's first kerb/path render showed visible rainbow banding (BoxGeometry stretching the road pack's own sprite-sheet atlas across each thin face) and its second prop (street-lamp-1x1) rendered as an unlabelled white shape with the dumpster occluded behind the tower.","greenEvidence":"After, all three sub-commits: 5f72578 (a: CanvasTexture gradient sky, 11-sky.png), 3f0861e (b: enlarged far-ground plane + retuned fog + mipmapped array texture, 12-ground.png), 09a9b23 (c: raised curb + paved path via GROUND's own layer + join-decal mechanism, one prop, 13-street-level.png). Same ?hero=1 camera as 08 throughout. test/lookProofScene.test.ts: 36/36 pass, each new mechanism mutation-verified (caught red, restored byte-identical via cp, not git checkout). npx tsc --noEmit clean. No numeric gate invented per the brief's own §2 -- N1c's own commit message states plainly that its kerb/path read as subtle, not dramatic, rather than overstating it. Not yet shown to Mark as of this run.","commit":"f9530da","actor":"lane","recordedAt":"2026-09-14T17:23:22.360Z"}
{"project":"C:\\Code\\sandbox-spike","item":"A1","redEvidence":"Before this item, three related failures existed: (1) commercial/civic/landmark entries carried adjacency: {} under S0's uniform table, switching off the model's stated primary value driver; (2) a flat civic-category bonus would have made substation-a raise nearby housing value -- the checklist's own named defect; (3) the migration script's import.meta.url-based path/entry-point logic crashed with ENOENT (test/data/catalogue.json, one level short of root) when bundled by scripts/_mutcheck.mjs, and a mutation test against data/catalogue.json reported a false SURVIVED because the whole process crashed before node:test printed any failing title, which the failure-title extractor cannot distinguish from zero failures.","greenEvidence":"node test/run.mjs catalogueValidator.test.ts -- 37/37 pass (up from 34), tsc clean. scripts/migrate-catalogue-s2-fields.mjs re-run twice confirmed idempotent (byte-identical output). 4/4 mutations CAUGHT via scripts/_mutcheck.mjs: scoring-catalogue-has-base-value, scoring-catalogue-has-adjacency, scoring-catalogue-adjacency-values-are-integers (public/catalogue-validator.js) and scoring-a1-substation-does-not-raise-housing-value (data/catalogue.json, only caught cleanly after the bundling bug was fixed via a basename(process.argv[1]) entry-point guard and a CLAUDE.md-walk-up path resolution). New tests assert both directions: every non-amenity civic entry (kiosk-a, substation-a, stadium-a) has no positive adjacency.residential, substation-a named directly, and every AMENITY_CIVIC_TYPE_IDS entry (small-civic-a, civic-6x6-a) DOES have one.","commit":"62650f4","actor":"lane","recordedAt":"2026-09-14T08:52:19.528Z"}
{"project":"C:\\Code\\sandbox-spike","item":"BO1","redEvidence":"Before this item: no area state machine existed at all -- nothing refused entry to a LOCKED area, nothing structurally prevented two areas being ACTIVE at once, and no cell address was scoped to its own area's board (a global-grid design would let a coordinate collide across areas). Mutation world-layer-locked-area-refuses-entry (disabling the LOCKED check in createAreaWorld.enter()) turned red exactly the test \"GATE: an area that is LOCKED cannot be entered\". Mutations world-layer-resolve-cell-scoped-to-its-own-area and world-layer-reenter-reloads-not-assumed-resident (public/world-layer.js) turned red exactly \"GATE: a cell address resolves ONLY within its own area's board\" and \"GATE (W5): re-entering after leaving reloads\" respectively.","greenEvidence":"public/area.js (W1/W2) and public/world-layer.js (W5/W6), commit d252ac7 on branch world-layer (merged to main at 3d8c90a). test/area.test.ts (14) + test/worldLayer.test.ts (12) = 26 tests, green. Camera-relative origin proven not to touch stored piece data (dedicated test). Cross-area resolution proven structurally impossible via a dedicated fixture with a REAL piece at the queried coordinate in the sibling area, not an empty cell a fallback bug could pass by accident. 3/3 named mutations CAUGHT via scripts/_mutcheck.mjs.","commit":"d252ac7","actor":"lane","recordedAt":"2026-09-14T08:53:51.560Z"}
{"project":"C:\\Code\\sandbox-spike","item":"BO2","redEvidence":"Before this item: no typed-array board existed. Two real bugs were found by the tests themselves before any audit ran: a non-integer piece id would have been silently coerced by the Int32Array occupancy index (NaN-to-0 on write), corrupting the index rather than refusing; a Float32-vs-float64 rounding gap made the slope-tolerance check refuse a placement exactly AT the tolerance value. Mutations world-layer-occupied-rect-rotation-swap (dropping the 270-degree case from the w/d swap) and world-layer-occupancy-matches-derived-rect (skipping the first cell when writing occupancy) turned red exactly the brief's own named gate tests. world-layer-corner-offset-per-corner-not-shared (a later blind-audit fix) turned red \"setCornerOffset writes exactly the ONE corner named\".","greenEvidence":"public/area-board.js (C2.1), commit d252ac7 on branch world-layer (merged to main at 3d8c90a). test/areaBoard.test.ts, 25 tests green (20 original + 5 for the corner-offset accessors the blind audit added). occupiedRect() is the SAME function board.place() and the ghost preview both call -- proven by a test that the ghost's verdict and commit's outcome always agree across valid/occupied/out-of-bounds/unknown-type cases. Both real bugs fixed (invalid-id refusal; epsilon-padded slope comparison matching grid.js's own idiom) and covered by dedicated boundary tests. 3/3 named mutations CAUGHT.","commit":"d252ac7","actor":"lane","recordedAt":"2026-09-14T08:53:57.471Z"}
{"project":"C:\\Code\\sandbox-spike","item":"BO3","redEvidence":"Before this item: no placement session existed, and a blind audit (fresh agent, no context) found a real HIGH-severity defect before it was fixed in the same commit -- loadBoard() silently discarded any placement that failed to re-apply at load time (a catalogue entry removed since save, a shrunk area, an id collision) with zero signal, reproduced directly with a scratch script against the real, unmutated module (two independent causes). Mutation world-layer-commit-inert-on-invalid-ghost (removing the invalid-ghost refusal in commit()) turned red exactly \"GATE (C2.2 item 3): commit is INERT on an invalid ghost\". world-layer-tombstone-blocks-regeneration (skipping the tombstone check in loadBoard) turned red exactly \"GATE (C2.5): a removal does NOT come back\". world-layer-loadboard-reports-failures-not-silent-drop (dropping the failures.push call) turned red the new failure-reporting test added for the HIGH finding.","greenEvidence":"public/placement.js (C2.2 Tier 1, C2.5), commit d252ac7 on branch world-layer (merged to main at 3d8c90a). test/placement.test.ts, 17 tests green (15 original + 2 for the loadBoard failure-reporting fix). Save format matches corrected-C2.5 exactly: {seed, generatorParams, tombstones, placements}. Full place/remove/reload cycle proven for both player and generated-with-tombstone pieces, including a CONTRAST test showing a generated piece with NO tombstone correctly reappears (proving the tombstone, not a coincidentally-absent generator, is what keeps a bulldozed piece gone). 3/3 named mutations CAUGHT.","commit":"d252ac7","actor":"lane","recordedAt":"2026-09-14T08:54:03.580Z"}
{"project":"C:\\Code\\sandbox-spike","item":"R0","redEvidence":"process_reconcile_plan against docs/specs/REBUILD-CHECKLIST.md, before this item: BO1, BO2, BO3 all appeared in the \\\"ticked with NO gate record\\\" list -- three real, completed world-layer deliverables (commit d252ac7) with no evidence entry in docs/GATE-LEDGER.jsonl, a genuine claim-with-nothing-behind-it gap the tool exists to surface.","greenEvidence":"Three process_record_gate calls added (BO1, BO2, BO3, all citing commit d252ac7 with specific mutation ids and test counts per item). Re-running process_reconcile_plan afterward: BO1/BO2/BO3 no longer appear in either the no-gate-record list or the gate-recorded-but-open list -- clean.","commit":"ced09f8","actor":"lane","recordedAt":"2026-09-14T08:54:28.115Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S1","redEvidence":"Before this item: no value function existed. Blind review hand-mutated six properties of the implementation (self-exclusion, Chebyshev boundary, dedup, NaN-guard, board-mutation, adjacency-key direction), each correctly turning red exactly the test naming it. Separately, the review found the ORIGINAL tests insufficient: a module-level cache keyed on (x,y) alone left both original path-independence GATE tests green while five unrelated tests failed for the wrong reason; a sign-flip mutation and a disabled-adjacency mutation both left the original \"multiple neighbours summed\" test green because its shop(+5)/factory(-5) fixture netted to zero. Both gaps closed with new/strengthened tests before commit, then re-verified: the coordinate-only-cache mutation now correctly fails the new GATE test; the sign-flip mutation now correctly fails the strengthened summed test.","greenEvidence":"node test/run.mjs scoring.test.ts catalogueValidator.test.ts -- 50/50 pass. npx tsc --noEmit clean. 3/3 mutations CAUGHT via scripts/_mutcheck.mjs: scoring-s1-piece-excludes-self, scoring-s1-dedup-multi-cell-piece, scoring-s1-missing-adjacency-key-is-zero-not-nan. Path-independence proven three ways: A-then-B vs B-then-A ordering, a simulated reload with different ids/order, and same-coordinate-different-arrangement queried back to back (the shape that specifically rules out a coordinate-keyed cache).","commit":"d889c1f","actor":"lane","recordedAt":"2026-09-14T09:05:28.768Z"}
{"project":"C:\\Code\\sandbox-spike","item":"V1","redEvidence":"Before this item: no cross-lane event log existed, and the real 2026-09-14 failure (BLD checked codex-lane for A1's commit, found nothing, and the gated item never ran) is the reproducible shape. Blind review, before commit, found and reproduced two real gaps in the FIRST draft: (1) a test named \"appendEvent never reads the log file\" passed even when appendEvent was replaced with the literal read-then-write-whole-file anti-pattern the checklist names, because JSON.stringify(JSON.parse(x)) round-trips a well-formed fixture byte-for-byte -- proven by hand, the mutated (wrong) implementation passed that test; (2) the spec's own claim \"an append-only log merges cleanly across branches\" was false under git's default merge strategy -- two branches each appending one distinct line to a shared JSONL file produced a real `CONFLICT (content)` in a disposable scratch repo, not a clean merge.","greenEvidence":"Both gaps fixed and re-verified before commit: the read/write claim now checked via a static source-level scan of appendEvent's own body (readFileSync(/writeFileSync( absent) rather than an output-equality inference that a read-rewrite implementation could satisfy by coincidence; the merge claim now backed by `merge=union` in .gitattributes for docs/EVENT-LOG.jsonl and docs/GATE-LEDGER.jsonl, re-verified in the same scratch-repo scenario -- now auto-merges cleanly with both lines present. node test/run.mjs eventLog.test.ts scoring.test.ts catalogueValidator.test.ts -- 64/64 pass. npx tsc --noEmit clean. 2/2 mutations CAUGHT via scripts/_mutcheck.mjs: v1-event-log-closed-list-enforced, v1-event-log-no-truncation-on-malformed-prior-line (the exact 2026-09-10 recordGate failure shape, reproduced and proven fixed). Real historical events backfilled via the actual CLI tool for A1/S1/R0/V1's own pushes -- docs/EVENT-LOG.jsonl is immediately queryable, not an empty mechanism.","commit":"4f27f42","actor":"lane","recordedAt":"2026-09-14T16:42:03.131Z"}
{"project":"C:\\Code\\sandbox-spike","item":"V3","redEvidence":"Blind review of the WRITTEN PLAN (before any code existed) found two real bugs by walking the actual area-board.js id-reuse logic and loadBoard()'s existing failure-handling pattern: (1) the plan's own remove() did not clear the proposed placedAt map, and area-board.js's place() genuinely allows an explicit id to be reused once its prior piece is removed (pieces.has(id) is the only guard, never checked against nextId) -- a stale timestamp would silently attach to an unrelated later piece; (2) the plan's own loadBoard() reconstruction of placedAt was unconditional, which would give a phantom timestamp to a placement that failed to re-apply (catalogue drift, already a real documented case per B2) and is therefore not actually on the reloaded board. Both reproduced directly via mutation after implementation: v3-remove-clears-placed-at and v3-loadboard-placedat-gated-on-success. Also found mid-work: refactoring broke the find string of a PRE-EXISTING mutation (world-layer-loadboard-reports-failures-not-silent-drop), which reported INCONCLUSIVE rather than CAUGHT until noticed and fixed.","greenEvidence":"Both plan-review findings fixed before commit (placedAt now deleted in remove(), matching the existing tombstones pattern; loadBoard()'s placedAt population now gated on result.ok, matching how failures already works). node test/run.mjs placement.test.ts eventLog.test.ts scoring.test.ts catalogueValidator.test.ts -- 91/91 pass (placement.test.ts alone: 17 -> 27). npx tsc --noEmit clean. 5/5 mutations CAUGHT via scripts/_mutcheck.mjs, including the repaired pre-existing one. value() in public/scoring.js confirmed untouched by grep -- nothing added here is ever read back by scoring, satisfying S1's path-independence gate.","commit":"4134dc7","actor":"lane","recordedAt":"2026-09-14T17:13:29.135Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S2","redEvidence":"Blind review of the WRITTEN PLAN (before any code existed) found two real bugs: (1) the plan's own value()-rewrite snippet dropped the existing NaN guard, which would compute undefined*falloff(distance)=NaN for the real, already-catalogued commercial/civic/landmark amenity case (adjacency:{}, per DECISIONS-FOR-MARK #12); (2) a pre-existing mutation (scoring-s1-missing-adjacency-key-is-zero-not-nan) is anchored to the exact line this item rewrites and would have gone INCONCLUSIVE with no plan line item to catch it. After implementation, mutation testing found a THIRD, different bug the plan review didn't catch: an off-by-one in nearestChebyshevDistance's exclusive-upper-bound handling turned a DIFFERENT, unrelated test red instead of the intended one, because every existing multi-cell-piece test queried only from the near side, where the off-by-one term stays negative and never affects Math.max() -- the far-side direction (the one direction the -1 term actually matters) had no test coverage at all.","greenEvidence":"All three fixed before commit: NaN guard restored explicitly; the stale mutation's find string updated to the new source and re-verified CAUGHT; the multi-cell-piece test strengthened with a second, far-side-query case, after which the off-by-one mutation is caught by the test that actually names the property under test. node test/run.mjs scoring.test.ts placement.test.ts eventLog.test.ts catalogueValidator.test.ts -- 96/96 pass. npx tsc --noEmit clean. 5/5 mutations CAUGHT via scripts/_mutcheck.mjs, including the checklist's own named gate verbatim: a hand-written linear ramp with the same two endpoints as the real curve fails the shrinking-consecutive-drops assertion the real exponential curve passes. falloff(0)=1 and falloff(R)=0.1 pinned directly against an independently-computed Math.exp(...) never imported from the module, so a bug inside the curve itself (not just its wiring into value()) is also caught.","commit":"84d4a16","actor":"lane","recordedAt":"2026-09-14T17:37:45.512Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"BO7A","redEvidence":"Before this item: data/catalogue.json's 50 abstract entries carried no mesh reference at all, and public/look-proof-pieces.js's 20 real L12 meshes carried no game category/adjacency -- the two systems were fully disconnected, so placement could never render a real mesh. Mutation 1: removed rule 9's own check from catalogue-validator.js -- both RULE 9 malformed-value tests (empty string, a number) correctly failed. Mutation 2: hand-corrupted small-house-a's own glb value in the real catalogue to a wrong path -- the drift-check test correctly failed, naming the exact entry and mesh mismatch. Mutation 3: added a fake dumpster-1x1 catalogue entry to the real file -- the prop-refusal test correctly failed, naming that a prop must not be a real catalogue entry.","greenEvidence":"edbb21b. scripts/link-catalogue-meshes.mjs links 12 of 50 catalogue entries to a real L12 mesh via a disclosed MESH_BINDINGS table (footprint mechanical, category read from the mesh's own id or source kit name, never invented); 3 \"-alt\" meshes left unbound as a named finding (UNMATCHED_MESHES); 5 meshes (street-lamp-1x1, utility-pole-1x1, dumpster-1x1, awning-1x1, parasol-1x1) are disclosed scene dressing, never catalogue pieces (PROP_MESH_IDS), per Mark's own direct scoping of this item (\"BO7A is a JOIN, not a creation\"). test/catalogueValidator.test.ts gained 9 checks, all three mutations above reverted via cp (not git checkout --) and diff-confirmed byte-identical. node test/run.mjs test/catalogueValidator.test.ts test/placement.test.ts test/areaBoard.test.ts test/scoring.test.ts test/worldLayer.test.ts test/lookProofScene.test.ts: 164/164 pass. npx tsc --noEmit clean.","commit":"edbb21b","actor":"lane","recordedAt":"2026-09-14T18:03:08.945Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RB1","redEvidence":"Before: look-proof-scene.html rendered PIECES, a literal hardcoded array, for every mode -- no code path read a real createAreaBoard's own state at all. Mutation 1: removed ...preparedBoardPieces from the mergeGeometries call (simulating board pieces silently dropped from the render) -- the RB1 merge-gate test correctly failed. Mutation 2: replaced resolveBoardPieces's own `for (const piece of board.pieces())` with `for (const piece of [])` (simulating a hardcoded-empty feed) -- 5 tests in test/boardRenderer.test.ts correctly failed, including the remove()-reflects-in-resolved gate.","greenEvidence":"6fb1cec. public/board-renderer.js (new, pure data resolution, GPU-free tests) resolves a real board's own board.pieces() into render-ready descriptors via the real data/catalogue.json (BO7A's glb field) and the real array-texture manifest (layerForGlb, one source of truth, not a second kit-name parser). look-proof-scene.html's ?board=1 places 4 real glb-bound pieces onto a real board via real board.place(); ?board=1&removeId=<id> calls the real board.remove() before rendering. 14-board.png (4 pieces) / 15-board-removed.png (tower-base-6x6-a visibly gone, others unchanged) -- one real board's own state transition, not two independently-scripted renders. Draw calls held at 2 (mesh + sky, unchanged) at both counts. HERO_MODE's own 13-street-level.png re-rendered and confirmed byte-identical after the shared-code changes -- no regression. test/boardRenderer.test.ts: 16/16 new. test/lookProofScene.test.ts gained 6 checks, 3 pre-existing updated for the new merge/decal arg lists. Both mutations above reverted via cp (not git checkout --) and diff-confirmed byte-identical. node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/catalogueValidator.test.ts test/areaBoard.test.ts test/placement.test.ts: 156/156 pass. npx tsc --noEmit clean.","commit":"6fb1cec","actor":"lane","recordedAt":"2026-09-14T18:30:15.052Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RB2","redEvidence":"Before: placement.js's own Tier 1 ghost (setGhost/commit/cancel/remove) had no render at all -- nothing showed a preview, valid or invalid. Mutation 1: GHOST_COLOR's valid and invalid set to the identical colour (0x4caf50 for both) -- the RB2 visibly-distinct-colours gate correctly failed. Mutation 2: resolveGhost's own returned `valid` hardcoded to `true` regardless of the real ghost's own value -- the RB2 invalid-ghost gate (which asserts valid:false with reason:\"occupied\") correctly failed.","greenEvidence":"727ca0e. board-renderer.js's resolveGhost(ghost, catalogue) reads a real session.getGhost()'s own valid/reason verbatim. look-proof-scene.html's ?board=1&ghost=valid/invalid build a real createPlacementSession, call the real session.setGhost() -- the invalid demo targets house-a's own real occupied cell, so the refusal is real, not staged. Rendered as a separate MeshBasicMaterial overlay: green (0x4caf50) valid, red (0xe53935) invalid. 16-ghost-valid.png / 17-ghost-invalid.png. \\\"Committing it changes nothing\\\" proven on the real board: GHOST-COMMIT ok=false reason=inert piecesBefore=4 piecesAfter=4 unchanged=true, from a real session.commit() call and real resolveBoardPieces counts taken before/after. Both mutations reverted via cp (not git checkout --) and diff-confirmed byte-identical. All three board-mode shots (14, 16, 17) re-rendered and confirmed byte-identical after the restores. node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/placement.test.ts test/areaBoard.test.ts test/catalogueValidator.test.ts: 167/167 pass. npx tsc --noEmit clean.","commit":"727ca0e","actor":"lane","recordedAt":"2026-09-14T18:35:56.194Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RB3","redEvidence":"Before: no readout render existed at all. The item's own real gate (\"move the cursor across cells of genuinely different value and the number changes\") could not be watched red against the real system since S4 (valueAt/valueIfPlaced) has not landed anywhere (checked origin/scoring and origin/main directly). Mutation, verified instead: resolveReadout changed to fake availability (hardcoded {available:true, current:0, ifPlaced:0}) when valueAt/valueIfPlaced are missing -- both the mock-based \"reports unavailable\" test and the LIVE test against the real, unmocked public/scoring.js correctly failed, the live one by design (it is written to start passing again only once resolveReadout genuinely reports unavailable against the real module, and to start FAILING the moment S4 actually lands, which is the intended future signal).","greenEvidence":"fb5b1f4. board-renderer.js's resolveReadout checks for valueAt/valueIfPlaced before calling either, never invents a number. look-proof-scene.html imports scoring.js as a namespace (never a named import of a not-yet-existing export, which would crash every other mode). ?board=1&readout=x,y renders the real, honest unavailable state -- 18-readout-unavailable.png, a deliberately muted grey marker, console: READOUT-STATE ... available=false reason=\\\"S4 not landed: ...\\\". Mechanism proven against a mock (two cells, two different numbers, same call path S4 will use). Zero edits to public/scoring.js. test/boardRenderer.test.ts +5, test/lookProofScene.test.ts +4 (3 pre-existing updated). node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/placement.test.ts test/areaBoard.test.ts test/catalogueValidator.test.ts test/scoring.test.ts: 195/195 pass. npx tsc --noEmit clean. TICKED [!] NOT [x] IN THE CHECKLIST -- the item's own real visual gate remains unverified until CLI's S4 lands, named plainly rather than hidden behind a green suite.","commit":"fb5b1f4","actor":"lane","recordedAt":"2026-09-14T18:42:31.821Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RB4","redEvidence":"Before: no reload render existed -- the render pipeline only ever read the original, never-reloaded board. Mutation: removed the `board = reloadedBoard` reassignment after a real loadBoard() call (simulating the render continuing to read the pre-reload board, exactly the class of defect this gate exists to catch) -- the RB4 gate test correctly failed with \"the reloaded board does not replace the original -- downstream resolution would still be reading the pre-reload board, proving nothing about the round trip\".","greenEvidence":"ef44341. ?board=1&reload=1 serializes the real session (session.serialize(), C2.5's save shape) and rebuilds via the real loadBoard(); `board` (declared `let`) is reassigned to the reloaded board before any downstream resolution runs. 19-reload.png is byte-identical to 14-board.png, confirmed with cmp. ?reloadShrink=1 reloads into a deliberately narrower board so mega-tower-a's own saved anchor genuinely fails to re-apply (real out-of-bounds refusal) -- console: RELOAD-FAILURES mega-tower-a(out-of-bounds), resolved 4->3, 20-reload-failure.png shows exactly that piece missing. loadBoard()'s own {board, failures} surfaced to console, not swallowed. test/lookProofScene.test.ts +4 checks, 2 pre-existing updated. Mutation reverted via cp (not git checkout --) and diff-confirmed byte-identical; both reload shots re-rendered and confirmed byte-identical after restore. node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/placement.test.ts test/areaBoard.test.ts test/catalogueValidator.test.ts test/scoring.test.ts: 199/199 pass. npx tsc --noEmit clean.","commit":"ef44341","actor":"lane","recordedAt":"2026-09-14T18:46:40.908Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RB5","redEvidence":"Before: 13-street-level.png -- the existing ground texture (manifest's \"ground-grass\") has a measured mean RGB of (172,148,121), a warm dirt/sand tone despite its own filename; the whole visible ground reads uniformly this way regardless of distance from a building, \"buildings in a dust bowl\" per Mark's own words. Mutation 1: the sky's own horizon gradient stop reverted to the old, no-longer-matching colour (0xf2ddb8) while uFogColor stayed at the new 0xe6dccb -- the fog/sky-match test correctly failed. Mutation 2: addGroundLayerAttribute's own per-vertex split hardcoded to always return earthLayer regardless of distance -- the per-vertex gate test correctly failed.","greenEvidence":"b4949c9. A second, already-vendored, already CC0-licensed ground layer (04-ground-paved.png, gravel, measured mean RGB (173,163,150), notably more neutral) assigned per-vertex within PAVING_RADIUS=6m of any real footprint (reusing 4.3's own distanceOutsideFootprint), earth beyond it. Fog colour and the sky's own horizon stop retuned together and matched exactly (0xe6dccb) so N1b's seamless fade holds. 13-street-level.png (Mark's own judged shot) deliberately left untouched -- caught and restored via git checkout HEAD-- after an unintended re-render, before anything was committed. 21-ground-material.png is the real after. RB1-RB4's own shots (14-20) refreshed to match the new global ground/sky (their own structural evidence -- draw calls, resolved/skipped counts, RELOAD-FAILURES, GHOST-COMMIT -- re-confirmed unchanged; 19-reload.png re-checked byte-identical to the refreshed 14-board.png). Both mutations reverted via cp (not git checkout --) and diff-confirmed byte-identical. node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/placement.test.ts test/areaBoard.test.ts test/catalogueValidator.test.ts test/scoring.test.ts: 204/204 pass. npx tsc --noEmit clean.","commit":"b4949c9","actor":"lane","recordedAt":"2026-09-14T18:56:31.712Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RC1","redEvidence":"test/pointerInteraction.test.ts: mutated handleClick to always route to session.commit() (removed the board.pieceIdAt routing branch) -- 2 tests failed on a real assertion ('commit' !== 'remove'), confirming the routing rule is what the test protects. Separately, running scripts/interact-look-proof.mjs BEFORE serializing rebuilds showed a real race: two POINTER-CLICK console lines with swapped/stale action labels when two clicks landed close enough together that the first click's async glb-fetch rebuild was still in flight when the second started -- whichever rebuild finished last won, even from an older board snapshot.","greenEvidence":"node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/pointerInteraction.test.ts test/placement.test.ts test/areaBoard.test.ts: 103/103 pass. npx tsc --noEmit clean. node scripts/interact-look-proof.mjs, after adding the rebuildChain promise queue: real page.mouse.move/click DOM events against the real page, board.pieces().length read back via window.__board -- pieces on load=4, hover(23,15) out-of-bounds reads invalid and click leaves board at 4, hover(2,6) reads valid and click brings it to 5 (the real placed piece is house-a at (2,6)), clicking it again removes it back to 4, Escape drops the ghost without touching the board. Console: 'RC1 GATE: pass', zero console/page errors. 22-interactive.png visually confirmed (Read tool) identical to 14-board.png.","commit":"472cb27","actor":"lane","recordedAt":"2026-09-14T19:41:42.464Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RC2","redEvidence":"scripts/interact-overview-scene.mjs, before adding the rebuild wait: 'clicking downtown enters board mode' failed (still 'overview') because page.mouse.click() does not wait for the async pointerdown handler -- fixed with waitForFunction, not a real defect. The REAL defect: mutated leaveBoardView to skip worldLayer.leave() (const left = worldLayer.active()?.id instead of destructuring the real leave() call) -- the script's own re-entry assertions went genuinely red: 'no area is active after leaving: expected null, got {id:downtown,...}' and 'loadCount still 1, not 2', proving W5's own residency check is real and not tautological.","greenEvidence":"node test/run.mjs test/overviewScene.test.ts test/lookProofScene.test.ts test/boardRenderer.test.ts test/pointerInteraction.test.ts test/worldLayer.test.ts test/area.test.ts test/areaBoard.test.ts test/placement.test.ts: 190/190 pass. npx tsc --noEmit clean. node scripts/interact-overview-scene.mjs: real clicks against the real page -- harbor (LOCKED) refused with reason 'locked', visible DOM refusal, stays in overview; downtown (OPEN) enters board mode, worldLayer.active()='downtown', real board has 1 real piece, loadCount=1; Escape leaves, no area active; re-entering downtown loadCount=2; entering hills while downtown still showing reassigns the single active slot to 'hills'. Console: 'RC2 GATE: pass', zero console/page errors. 23-overview-refused.png and 23-area-entered.png visually confirmed via Read.","commit":"3ce6a34","actor":"lane","recordedAt":"2026-09-14T19:51:13.347Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RC3","redEvidence":"First render of impostor-overview-scene.html: 12 solid black rectangles, zero console errors, plausible triangle stats -- caused by TextureLoader.load() returning before the atlas image decoded, so render() ran against a 1x1 placeholder. Separately, scripts/measure-impostor-triangles.mjs was mutated to make the impostors-on branch silently fall back to full detail geometry (baseGeom.clone() instead of a 2-triangle billboard quad) -- the script correctly failed: 'RED: no measurable triangle reduction -- off=4488, on=4488', proving the gate catches the exact named RED case (no measurable difference).","greenEvidence":"node test/run.mjs test/impostorOverviewScene.test.ts test/overviewScene.test.ts test/lookProofScene.test.ts test/boardRenderer.test.ts test/pointerInteraction.test.ts: 119/119 pass. npx tsc --noEmit clean. node scripts/measure-impostor-triangles.mjs after the loadAsync fix and mutation revert: off=4488 triangles, on=24 triangles, a 99.5% reduction across the same 12 distant instances, 1 draw call each way -- 'RC3 GATE: pass'. 24-impostors-off.png and 24-impostors-on.png visually confirmed via Read: full-detail houses vs 12 correctly-angled billboard cards sampling the real baked atlas.","commit":"bbb35f9","actor":"lane","recordedAt":"2026-09-14T19:58:17.870Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RC4","redEvidence":"14-board.png (before, unchanged): four small paved patches directly under each piece, everywhere else warm dirt -- Mark's own 'dust bowl' description, confirmed by looking at the image, not assumed. Measured, not guessed: real camera-to-ground distances for this camera are ~134-212m, already past HERO_MODE's own uFogNear=90, so fog was already blending -- the actual cause is that the fog colour (0xe6dccb) and the earth ground colour are both warm-toned, so fading between them only pales the dust read rather than changing it.","greenEvidence":"node test/run.mjs test/lookProofScene.test.ts: 72/72 pass. npx tsc --noEmit clean. 25-board-scene-pass.png (after, same camera as 14-board.png): BOARD_PAVING_RADIUS=20 (board-camera-only, HERO_MODE's own PAVING_RADIUS=6 unchanged) closes the gaps between all four demo pieces into one continuous paved plaza; uFogNear/uFogFar overridden to 100/220 via a caller-side uniform write (BOARD_MODE only, look-proof-material.js's own defaults unchanged, asserted by test). Downstream board-camera shots (15, 19, 20, 22) refreshed and their own structural evidence re-confirmed: 15 still BOARD-REMOVE id=2 ok=true resolved 4->3; 19 still byte-identical to 25 via cmp (RB4's own gate still holds); 20 still RELOAD-FAILURES mega-tower-a(out-of-bounds) resolved 4->3; 22 (scripts/interact-look-proof.mjs) still 'RC1 GATE: pass'. 14-board.png confirmed untouched via git status before rendering. No numeric gate by the checklist's own design -- presented as a pair for Mark's own visual judgment, same as N1's and RB5's own still-pending pairs.","commit":"2936b7e","actor":"lane","recordedAt":"2026-09-14T20:04:12.895Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S3","redEvidence":"Blind review of the WRITTEN PLAN (before any code existed) found a real, serious off-by-one: the plan's own exclusive-upper-bound formula (rect.xMax - 1 + radius) is correct as the INCLUSIVE rightmost dirty column but silently one column short once handed to area-board.js's own EXCLUSIVE cellsOf() convention -- reproduced by hand for a piece occupying columns 5-6 at radius 3: the true dirty set includes column 9 (Chebyshev distance exactly 3, falloff(3)=EDGE_FRACTION, nonzero), which the wrong formula drops. The same review also found the planned gate test itself insufficient: it only checked the NEGATIVE case (a cell one step past the true edge is excluded), which an under-inclusive dirty set still satisfies -- it would not have caught the off-by-one above.","greenEvidence":"Formula corrected to rect.xMax + radius (and yMax symmetrically). A new POSITIVE-case GATE test added (a cell at EXACTLY distance R IS included), which is what actually catches the bug shape. node test/run.mjs scoring.test.ts placement.test.ts eventLog.test.ts catalogueValidator.test.ts -- 103/103 pass. npx tsc --noEmit clean. 7/7 mutations CAUGHT via scripts/_mutcheck.mjs, including the checklist's own named gate verbatim: recomputeDirtySet's returned Map, when the dirty-set computation is mutated to scan the whole board, is caught by checking the Map's own membership (a far cell is not a key at all), not by checking values (which would still be correct even over a widened scan). The middle-of-board size/membership check is verified against an INDEPENDENT brute-force oracle (a per-cell scan, not the closed-form rectangle-expansion shortcut, and not importing the module's own distance helper) so a bug shared by both implementation and test oracle could not pass silently.","commit":"0fca9b5","actor":"lane","recordedAt":"2026-09-14T17:52:01.302Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S4","redEvidence":"Mutation scoring-s4-category-override-ignored (disables the categoryOverride branch in value()) turned \"GATE (S4): valueIfPlaced on an OCCUPIED cell correctly excludes the REAL current occupant...\" red -- CAUGHT via scripts/_mutcheck.mjs. Mutation scoring-s4-valueifplaced-must-not-mutate-board (adds a real board.place() call inside valueIfPlaced) survived against the original 3-cell byte-identical test because all 3 probe cells were unplaceable anyway; adding a 4th, genuinely-placeable vacant cell made \"GATE (S4): valueIfPlaced leaves the board byte-identical\" go red under the mutation -- CAUGHT. Mutation scoring-s4-unit-quality-formula-must-be-inverse-sqrt-tiers (swaps 1/sqrt(tiers) for the rejected 1/tiers) turned a new literal-value test in test/catalogueValidator.test.ts red -- CAUGHT; the inversion-gate test alone did NOT catch this (both curves preserve the inversion's direction). Also: catalogue-validator.js's rule 9 had zero test coverage before this item; deleting its check block and rerunning produced a real AssertionError before the tests were written to cover it.","greenEvidence":"Targeted suite (scoring.test.ts, catalogueValidator.test.ts, placement.test.ts, eventLog.test.ts) 119/119 passing. tsc --noEmit clean. All 3 new S4 mutations CAUGHT via scripts/_mutcheck.mjs, both touched files (public/scoring.js, scripts/migrate-catalogue-s2-fields.mjs) restored byte-identical after each. GATE (S4) valueIfPlaced-leaves-board-byte-identical test passes with 4 probe cells including one genuinely placeable. GATE (S4) house/condo inversion test passes on real catalogue shapes (small-house-a vs apartment-block-a): house wins per-unit (~1.22x), condo wins in total (~4.9x). Commit a27f595 on branch scoring, pushed to origin/scoring.","commit":"a27f595","actor":"lane","recordedAt":"2026-09-14T19:26:49.601Z"}
{"project":"C:\\Code\\sandbox-spike","item":"C1","redEvidence":"Blind review of the WRITTEN PLAN (before any code existed) found a serious defect: median([])=0 would let an EMPTY city (no residential cells at all) silently outrank a real, badly-planned city that legitimately scores negative (residential-on-residential adjacency is dilutive). Fixed to return null; a dedicated test proves the distinction (empty board -> score null, crammed board -> real negative number). Mutation city-score-median-not-mean (substitutes arithmetic mean for median) turned \"GATE (C1): median wealth reads as the MEDIAN, not the mean\" red -- CAUGHT. Mutation city-score-per-cell-not-per-piece (samples once per building instead of once per occupied cell) turned \"GATE (C1): medianWealth counts EVERY cell of a multi-cell residential piece separately\" red -- CAUGHT. Mutation city-score-registry-must-sum-not-take-last (last-term-wins instead of summing) turned \"GATE (C1): adding a second, trivial term requires no change to the registry or the first term\" red -- CAUGHT; a single-term-only test could not have caught this, which is why that gate test registers a genuine second term. Mutation city-score-must-exclude-non-residential and city-score-empty-must-be-null-not-zero also CAUGHT.","greenEvidence":"test/cityScore.test.ts (13 tests) plus targeted suite (scoring.test.ts, catalogueValidator.test.ts, placement.test.ts, eventLog.test.ts) -- 132/132 passing. tsc --noEmit clean. 5/5 new mutations CAUGHT via scripts/_mutcheck.mjs, public/city-score.js restored byte-identical after each. GATE test proves the registry combines median wealth (registered alone) and median wealth + a throwaway constant term (registered in a second, fresh registry) such that the first term's own value is byte-identical across both and the total equals first-term-value + second-term-value. Commit e8decb4 on branch scoring, pushed to origin/scoring.","commit":"e8decb4","actor":"lane","recordedAt":"2026-09-14T19:56:55.365Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S6","redEvidence":"The first draft of two of the four gate tests placed a shop-a amenity directly inside subdivision-a's own eventual 2x2 footprint; the redevelopment placement then silently failed (occupied) and both tests read a false pass off a vacant cell's value (0) without checking placed.ok -- caught by actually running the tests (2 of 4 failed with AssertionError), not assumed correct from the plan. Fixed by relocating the amenity outside the new footprint and adding an explicit assert.ok(placed.ok, ...) after every remove-then-place in the file.","greenEvidence":"test/developedValue.test.ts (4 tests) plus targeted suite (scoring.test.ts, catalogueValidator.test.ts, cityScore.test.ts, placement.test.ts, eventLog.test.ts) -- 136/136 passing. tsc --noEmit clean. All four gates hold: a neighbour's valueAt changes on development, the developed plot's own totalWorth increases, medianWealth moves, and developing via remove-then-place scores byte-identically (cell for cell and in medianWealth) to a board built with the final state from the start -- S1's own path-independence gate extended to V3's exact narrative. No new production code; confirmation-only, cross-checked against existing scoring.js mutations via scripts/_mutcheck.mjs (none flip this file's own assertions, expected since its claims are orthogonal to and already covered by scoring.test.ts's own controls). Commit 0171261 on branch scoring, pushed to origin/scoring.","commit":"0171261","actor":"lane","recordedAt":"2026-09-14T20:03:18.606Z"}
{"project":"C:\\Code\\sandbox-spike","item":"S7","redEvidence":"Before this commit, second-order lift and the build cost layer existed only inside docs/specs/SCORING-MODEL-2026-09-14.md's own decision record (§5 and §6, \"why NOT tonight\"), with no home in docs/specs/REBUILD-PLAN.md itself -- a future reader following the plan document alone (grep for \"second-order\" or \"build cost\" against REBUILD-PLAN.md before this commit) would not find either system, exactly the \"losing them is the failure mode this item exists to prevent\" the checklist names.","greenEvidence":"Two new subsections added to docs/specs/REBUILD-PLAN.md directly after S5, matching SCORING-MODEL's own relative placement. Each cross-references SCORING-MODEL §5/§6 by number (not duplicated prose) and adds forward-looking build detail SCORING-MODEL's own decision-record framing does not cover (the iterative-relaxation shape second-order lift would need; the separate-system shape and rationale the build cost layer would need). Documentation-only gate: confirmed no test parses REBUILD-PLAN.md's own content programmatically (grep for readFileSync targeting it across test/*.ts and scripts/*.mjs -- zero matches), tsc --noEmit clean, targeted suite 136/136 unaffected. Commit 6be5608 on branch scoring, pushed to origin/scoring.","commit":"6be5608","actor":"lane","recordedAt":"2026-09-14T20:05:46.856Z"}
{"project":"C:\\Code\\sandbox-spike","item":"U1","redEvidence":"Before the fix, node scripts/gen-city-summary.mjs threw an uncaught ERR_MODULE_NOT_FOUND (public/city-plan.js does not exist, deleted by item zero's purge), and since scripts/gen-claims.mjs calls it via execFileSync with no try/catch, npm run gen:claims crashed before ever reaching gen-test-count.mjs -- confirmed directly by running it before any fix existed. Test genCitySummaryRetired.test.ts written and watched red against the unmodified script. Mutation gen-city-summary-must-refuse-cleanly-not-crash (removes the existsSync guard) restores that exact crash and turns the exits-0 test red -- CAUGHT. Two real bugs in the test itself found by running it: a stderr-capture gap that hid the retirement message on the success path (execFileSync only returns stdout on exit 0), and a false pass where the end-to-end test's own 600000ms timeout silently killed the child at 600068ms and was mistaken for a genuine completion (signal was SIGTERM, never checked) -- both fixed before commit.","greenEvidence":"test/genCitySummaryRetired.test.ts (5 tests) passing: dependency absence confirmed real, exit 0, informative message (not a raw stack trace), src/citySummary.generated.ts left byte-identical, and gen-claims.mjs reaches gen-test-count.mjs within a reliable 60s bound (its own characteristic stdout line present, no ERR_MODULE_NOT_FOUND on stderr). tsc --noEmit clean. 1/1 new mutation CAUGHT, file restored byte-identical. Targeted suite 149 tests, 147 passing, 2 pre-existing deliberate skips. HONEST GAP, not closed by this commit: full end-to-end npm run gen:claims completion (a real regenerated test count matching a fresh measurement, the brief's own literal gate) could not be observed -- traced directly to U2's own already-documented cullingRatio.test.ts stall (a chrome-headless-shell.exe child pinned near 0% CPU past 30 minutes), not a defect in this item's own fix. Commit b116279 on branch scoring, pushed to origin/scoring.","commit":"b116279","actor":"lane","recordedAt":"2026-09-14T21:48:51.034Z"}
{"project":"C:\\Code\\sandbox-spike","item":"U2","redEvidence":"Before the fix, running node test/run.mjs cullingRatio.test.ts hung: process inspection confirmed a chrome-headless-shell.exe child still alive with its parent node process's CPU flat well past 30 minutes, against the test's own 240s internal timeout. Root cause traced directly: public/city.html (the page all three affected tests load) was deleted by this run's own item zero purge, so page.goto() 404s and window.__ready is never set. Deliberately re-broke cullingRatio.test.ts's own skip annotation and reran test/cullingSuiteUnblocked.test.ts: the 'no longer hangs' gate correctly failed (killed by its own 60s bound, signal SIGTERM, not silently passing) and the annotation-presence gate correctly failed naming the file -- both watched red before restoring. Mutations u2-quarantine-must-stay-on-culling-ratio/regression-gate/env-luminance (remove each skip annotation) all CAUGHT via scripts/_mutcheck.mjs.","greenEvidence":"Ran the actual default suite (node test/run.mjs, no arguments) end to end: completed in ~110s (duration_ms 109685.9517), 123 test files, the runner's own summary line reading ℹ tests 619 / ℹ pass 579 / ℹ fail 8 / ℹ skipped 32 -- the exact evidence this item's own gate names. All 8 remaining failures individually checked and confirmed pre-existing/unrelated to city.html (deadExports.test.ts's pre-existing 162-entry allowlist gap, a ground.test.mjs slope-tolerance test, decision #10's already-named mutate.mjs regex bug, a stale CLAUDE.md count line, and mutationEvidence.test.ts's already-documented manifest gap -- U3's own subject). test/cullingSuiteUnblocked.test.ts (3 tests) plus targeted suite 144/144 passing. tsc --noEmit clean. 3/3 new mutations CAUGHT, files restored byte-identical. Commit 660929f on branch scoring, pushed to origin/scoring.","commit":"660929f","actor":"lane","recordedAt":"2026-09-14T22:04:47.820Z"}
{"project":"C:\\Code\\sandbox-spike","item":"U1","redEvidence":"Follow-up to the U1 gate recorded at commit b116279 (which proved reachability only, honestly disclosing that full completion was blocked by U2's own cullingRatio.test.ts stall). Now that U2 (commit 660929f) fixed that stall, ran node scripts/gen-claims.mjs for real: it previously could not complete at all (crashed on the unresolvable public/city-plan.js import before this item's own fix, then was blocked from finishing by U2's separate defect even after this item's fix landed).","greenEvidence":"npm run gen:claims completed end-to-end for real: gen-city-summary.mjs retired cleanly, gen-test-count.mjs ran the full 123-file suite in well under a minute and wrote a fresh measurement to test/testCount.generated.json -- 619 node tests (8 fail, all pre-existing per U2's own commit), 12 worker tests (0 fail). Correctly refused to publish the page-facing claim (8 real failures exist beyond its own exemption) -- designed behaviour, not a defect. CLAUDE.md's stale '1087 node tests' line (matching neither the pre-takedown 1214 nor the current 619) updated to the real, fresh count; test/claudeMdIsCurrent.test.ts now passes on the real repo. This is the brief's own literal U1 gate, satisfied for real, not just reachability. Commit 08d563c on branch scoring, pushed to origin/scoring.","commit":"08d563c","actor":"lane","recordedAt":"2026-09-14T22:08:55.348Z"}
{"project":"C:\\Code\\sandbox-spike","item":"U3","redEvidence":"Before this run's own direct measurement, the only basis for a recommendation was the brief's own written premise (mutate.mjs 'has stalled on every attempt, one full suite has never completed') -- written before U2 landed and, checked directly, now only partly accurate. Ran node scripts/mutate.mjs --id gen-city-summary-must-refuse-cleanly-not-crash for real: it completed its own baseline run in 1m49s (previously this class of run never completed at all, per the brief) and correctly refused to proceed because the suite has 7 genuine pre-existing failures -- a real, current, checked fact, not an assumption carried forward from before U2's fix.","greenEvidence":"DECISIONS-FOR-MARK.md #18 written with the current, measured picture: mutate.mjs is not stalled, it is correctly gated behind a green suite and now runs in ~2 minutes per attempt. Recommendation (keep both tools -- _mutcheck.mjs for day-to-day, mutate.mjs as the stricter full audit once the 7 pre-existing failures are fixed) queued for Mark, per the brief's own explicit instruction not to unilaterally replace the instrument. Nothing retired, nothing replaced -- the least irreversible path. Commit 99a837f on branch scoring, pushed to origin/scoring.","commit":"99a837f","actor":"lane","recordedAt":"2026-09-14T22:13:19.714Z"}
{"project":"C:\\Code\\sandbox-spike","item":"U4","redEvidence":"Blind review of the WRITTEN PLAN (before any code existed) found real defects: (1) the plan composed adjacencyFor() directly against untrusted authored input, but that function throws by design on an unrecognized category -- would crash instead of refusing; (2) the plan's own fields shape omitted pivot, which validator rule 5 hard-requires -- every call would have failed deterministically; (3) rule 10 (all-or-nothing provenance) cannot alone enforce \"an authored entry must have provenance\" since 0-of-4 must stay legal (every shipped entry has zero). All three fixed before implementation. After implementation: watched rule 10 go red by temporarily removing its check block and rerunning catalogueValidator.test.ts (real AssertionError), then restored. 5 mutations (collision refusal, category-checked-before-adjacencyFor, multiplier actually applied, provenance actually required, rule 10's own enforcement) all CAUGHT via scripts/_mutcheck.mjs, files restored byte-identical.","greenEvidence":"test/catalogueRegistry.test.ts (15 tests) plus targeted suite -- 170/170 passing, 2 pre-existing deliberate skips. tsc --noEmit clean. B1 (\"the board cannot tell the difference\") proven directly: a board built from a registry snapshot places and scores an authored piece through valueAt/perUnitWorth/totalWorth with zero special-casing, and a companion test proves a STALE (pre-authoring) snapshot correctly cannot see the new piece -- the re-fetch requirement is tested, not assumed. B3's multiplier proven real: an authored piece's totalWorth is measurably UNIQUENESS_MULTIPLIER times an otherwise-identical shipped piece's. Two real gaps disclosed rather than hidden (DECISIONS-FOR-MARK.md #19): the overlay is in-memory only, not yet the durable store B2 calls \"persisted\"; the multiplier is baked in at authoring time with no re-tuning path, mitigated with a recorded uniquenessMultiplierApplied trail. Commit 2737c3b on branch scoring, pushed to origin/scoring.","commit":"2737c3b","actor":"lane","recordedAt":"2026-09-14T22:25:48.612Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"RC5","redEvidence":"RB3's own live-signal test (test/boardRenderer.test.ts, 'GATE (RB3): the REAL public/scoring.js... does NOT export valueAt/valueIfPlaced') fired genuinely red this run when origin/main was merged into codex-lane: 'S4 appears to have landed -- update board-renderer.js's own resolveReadout call and RB3's own render to use the real functions, this fallback path is no longer the honest state. true !== false'. Confirms the test was a real, live check, not a tautology.","greenEvidence":"node test/run.mjs test/lookProofScene.test.ts test/boardRenderer.test.ts test/pointerInteraction.test.ts test/overviewScene.test.ts test/impostorOverviewScene.test.ts test/scoring.test.ts test/catalogueValidator.test.ts test/cityScore.test.ts test/developedValue.test.ts: 225/225 pass. npx tsc --noEmit clean. New test cross-checks resolveReadout's real output against valueAt/valueIfPlaced called directly, with a real adjacency bonus proving ifPlaced > current (not a degenerate empty-fixture pass). Rendered on the real demo board: 26-readout-real-numbers.png, READOUT-STATE cell=7,4 available=true current=0 ifPlaced=2.3207944168063896 -- 2.32/5=0.464, matching Mark's own falloff-anchor question (~46%) almost exactly. Also measured (2,6) ifPlaced=-0.43 (real negative, dilutive residential) and (20,14) ifPlaced=0 (far baseline) -- three distinguishable real numbers, not degenerate. Zero edits to public/scoring.js.","commit":"2f98a4c","actor":"lane","recordedAt":"2026-09-14T21:02:57.068Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"CP2","redEvidence":"First render of catalogue-contact-sheet.html: labels correctly positioned, plausible draw-call/triangle counts, but the actual piece geometry was almost invisible -- fogged into the ground's own paved tone at this camera's real distance (the shared material's default fog range is tuned for HERO_MODE's much closer ~40-90m composition). Caught by opening the image, not assumed from the clean stats.","greenEvidence":"node test/run.mjs test/catalogueContactSheet.test.ts: 4/4 pass. npx tsc --noEmit clean. After the uFogFar caller-side override (RC4's own pattern), 27-catalogue-contact-sheet.png shows all 12 bound pieces clearly, each correctly labelled with its own real catalogue id. Honest read written into the checklist: a real cross-kit palette split (warm modular-buildings vs cool city-kit-commercial vs neutral roads) is the dominant finding, directly answering R2/A8's 'coherent kit' question; a suspected green patch on small-commercial-a was checked directly and did not hold up, named so it is not re-chased.","commit":"810fca2","actor":"lane","recordedAt":"2026-09-14T21:08:01.588Z"}
{"project":"C:\\Code\\sandbox-spike-codex","item":"CP3","redEvidence":"Five real before/after pairs (N1a, N1b, N1c, RB5, RC4) existed but were scattered across docs/look-proof-shots/ with no single document gathering them -- each would have required Mark to open the shots directory separately, in five different sessions, to judge them.","greenEvidence":"docs/audits/BLD-2026-09-15-image-pairs-for-mark.md gathers all five by real path, with what changed and an honest read of each, formed by actually opening and comparing each pair (not re-summarising commit messages). No image was re-rendered or re-tuned. Read confirmed each pair's own real file exists and was viewed directly via the Read tool before writing its own summary.","commit":"6b07194","actor":"lane","recordedAt":"2026-09-14T21:09:55.942Z"}
{"project":"C:\\Code\\sandbox-spike","item":"ITEM-0","redEvidence":"Before this commit, docs/specs/PLAN.md cited docs/specs/RESEARCH.md by section (e.g. \\\"RESEARCH.md T2\\\", \\\"RESEARCH.md T1\\\") but the file did not exist on disk -- ls docs/specs/RESEARCH.md: No such file or directory. Every terrain/layout citation in PLAN.md resolved to nothing.","greenEvidence":"docs/specs/RESEARCH.md now exists with §R (RESEARCH FINDINGS), §A (ARCHITECTURE DECISIONS), §L (CITY LAYOUT RULES) and §T (TERRAIN) lifted intact from the now-quarantined REBUILD-PLAN.md, numbering preserved. REBUILD-PLAN.md, REBUILD-CHECKLIST.md and WORLD-RULES.md quarantined to _TO-DELETE/item0-plan-retirement/ with ledger entries; npx tsc --noEmit clean and node test/run.mjs's own summary line (ℹ pass 734, ℹ fail 11, cancelled 0, skipped 22, todo 1) unchanged from before the move. CLAUDE.md's stale WORLD-BUILD-PLAN.md pointer and Lanes table corrected to match PLAN.md §1.","commit":"fb0fd9d","actor":"lane (CLI, 2026-09-16 run)","recordedAt":"2026-09-15T01:42:55.481Z"}
