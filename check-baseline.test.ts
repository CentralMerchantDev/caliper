// This file was zero bytes and sat at the repo root, outside test/, where the
// runner never looked for it. It is not a test and never was.
//
// Kept as a one-line marker rather than deleted outright, per the standing rule
// that nothing is removed without review. A copy of the original (empty) file is
// in _TO-DELETE/empty-test-files/ with the reasoning.
//
// If a baseline check is wanted, it belongs in test/ where the runner can see it.
export {};
